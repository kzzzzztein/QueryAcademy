// =========================================================================
// C# Programming - Master Level
// =========================================================================
const CS_MASTER_BASIC = [
  {
    title: "Value types vs reference types",
    body: "Every type in C# is either a value type (like int, double, bool, and structs) or a reference type (like classes, arrays, and strings). Value types are typically stored directly wherever they're declared, and assigning one to another variable copies the actual value. Reference types instead store a reference - essentially an address - pointing to the actual data, which commonly lives in a separate memory area; assigning one reference-type variable to another copies the reference, not the underlying object, so both variables end up pointing to the exact same object. Understanding this distinction explains a huge amount of behavior that otherwise looks confusing, including why modifying an object through one variable can appear to affect a seemingly separate variable.",
    altExplain: "Value types (like int) copy their actual value when assigned. Reference types (like classes) copy just a pointer to the same underlying object - so two variables can end up sharing the same object.",
    visual: null,
    code: "using System;\n\nclass Box { public int Value; }\n\nclass Program {\n  static void Main() {\n    int a = 5;\n    int b = a;\n    b = 10;\n    Console.WriteLine(a); // still 5 (value type copied)\n\n    Box box1 = new Box { Value = 5 };\n    Box box2 = box1;\n    box2.Value = 10;\n    Console.WriteLine(box1.Value); // 10! (reference type shared)\n  }\n}",
    keyTakeaway: "Two reference-type variables can point to the exact same object - modifying it through one variable is visible through the other, since there's really only one object.",
    commonMistake: "Assuming assigning one object variable to another creates an independent copy, when it actually just creates a second reference to the same underlying object.",
  },
  {
    title: "Design patterns: Singleton",
    body: "A design pattern is a reusable, well-understood solution to a recurring software design problem, and Singleton is one of the simplest: it ensures a class has exactly one instance throughout the entire program, with a single global point of access to it. This is implemented by making the constructor private (so nothing outside the class can create a new instance directly) and providing a static property or method that creates the single instance the first time it's requested, and returns that same instance on every subsequent call. Singleton is useful for things that should genuinely be unique application-wide, like a configuration manager, though it's worth using deliberately rather than reaching for it by default, since it introduces global shared state that can make code harder to test.",
    altExplain: "Singleton guarantees only one instance of a class ever exists, accessed through one global point - done by making the constructor private and providing a static way to get the single shared instance.",
    visual: null,
    code: "using System;\n\nclass ConfigManager {\n  private static ConfigManager instance;\n  public string Setting = \"default\";\n\n  private ConfigManager() {}\n\n  public static ConfigManager Instance {\n    get {\n      if (instance == null) instance = new ConfigManager();\n      return instance;\n    }\n  }\n}\n\nclass Program {\n  static void Main() {\n    ConfigManager.Instance.Setting = \"production\";\n    Console.WriteLine(ConfigManager.Instance.Setting);\n  }\n}",
    refBox: {
      syntax: "private static C instance; / public static C Instance { get { ... } }",
      desc: "Singleton - a private constructor and a static accessor ensure only one instance ever exists.",
      example: "ConfigManager.Instance.Setting = \"production\";",
    },
    keyTakeaway: "A Singleton's constructor must be private - that's what actually prevents anyone from creating additional instances outside the class's own control.",
    commonMistake: "Overusing Singleton for things that don't truly need to be globally unique, introducing hidden shared state that makes code harder to test and reason about.",
  },
  {
    title: "Design patterns: Factory",
    body: "A Factory pattern centralizes the logic for creating objects, so calling code asks for what it needs conceptually - 'give me a Circle' - without knowing or caring about the exact construction details. This is especially useful when object creation involves choosing between several related types based on some condition: a factory method might return different Shape subclasses depending on an input string, keeping that decision logic in one place instead of scattered across every part of the program that needs to create a shape. It also makes it easier to change how objects are constructed later, since only the factory needs updating, not every call site.",
    altExplain: "A Factory centralizes object-creation logic in one place, so the rest of your code just asks for what it needs without knowing exactly how it gets built.",
    visual: null,
    code: "using System;\n\ninterface IShape { void Draw(); }\nclass Circle : IShape { public void Draw() { Console.WriteLine(\"Drawing a circle\"); } }\nclass Square : IShape { public void Draw() { Console.WriteLine(\"Drawing a square\"); } }\n\nclass ShapeFactory {\n  public static IShape Create(string type) {\n    if (type == \"circle\") return new Circle();\n    if (type == \"square\") return new Square();\n    throw new ArgumentException(\"Unknown shape\");\n  }\n}\n\nclass Program {\n  static void Main() {\n    IShape shape = ShapeFactory.Create(\"circle\");\n    shape.Draw();\n  }\n}",
    refBox: {
      syntax: "static IType Create(string kind) { if (kind == \"a\") return new A(); ... }",
      desc: "A Factory centralizes object-creation decisions in one place.",
      example: "IShape shape = ShapeFactory.Create(\"circle\");",
    },
    keyTakeaway: "A factory keeps object-construction decisions in one centralized place, so adding a new type later means updating the factory, not every place that creates one.",
    commonMistake: "Scattering 'if type == X, create Y' construction logic across many different parts of a program instead of centralizing it in one factory.",
  },
  {
    title: "SOLID principles overview",
    body: "SOLID is an acronym for five widely respected object-oriented design principles. Single Responsibility means a class should have exactly one reason to change - one clear job. Open/Closed means code should be open to extension but closed to modification, ideally adding new behavior without editing existing, tested code. Liskov Substitution means a derived class should be usable anywhere its base class is expected, without breaking correctness. Interface Segregation favors several small, focused interfaces over one large do-everything interface. Dependency Inversion means depending on abstractions (interfaces) rather than concrete implementations directly, which is exactly what makes the Dependency Injection pattern, covered later in this course, possible.",
    altExplain: "SOLID is five design principles: one job per class (S), extend without modifying (O), subclasses should work wherever the base class does (L), small focused interfaces (I), and depend on abstractions, not concrete details (D).",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A class named ReportGenerator both calculates report data AND handles printing it to a physical printer. Which SOLID principle does this most clearly violate?",
      options: ["Liskov Substitution", "Single Responsibility - it has two unrelated reasons to change", "Interface Segregation", "None of them"],
      correct: 1,
      explanation: "Single Responsibility says a class should have one reason to change. This class would need changes both for report logic changes AND printer-related changes - two unrelated responsibilities bundled together.",
    },
    keyTakeaway: "SOLID isn't a strict checklist to force onto every class - it's a set of warning signs for design decisions likely to cause pain as a codebase grows.",
    commonMistake: "Treating SOLID as rigid rules to apply everywhere regardless of context, rather than as guidance to weigh against a specific situation's actual needs.",
  },
  {
    title: "Big O notation basics",
    body: "Big O notation describes how an algorithm's running time (or memory use) grows as the size of its input grows, focusing on the dominant trend rather than exact timing. O(1) means constant time - the same regardless of input size, like accessing an array by index. O(n) means time grows linearly with input size, like a single loop through a list. O(n^2) means time grows with the square of input size, common in nested loops that compare every element to every other element. This notation lets you reason about how an algorithm will scale before actually measuring it, which matters enormously once data sizes grow from a handful of items to millions.",
    altExplain: "Big O describes how an algorithm slows down as input grows. O(1) = same speed regardless of size. O(n) = grows proportionally. O(n^2) = grows much faster, common with nested loops.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A method has one loop nested inside another, both looping over the same list of size n. What's its Big O complexity?",
      options: ["O(1)", "O(n)", "O(n^2)", "O(log n)"],
      correct: 2,
      explanation: "Two nested loops, each running n times, means the inner loop body runs n times for every one of the n outer iterations - n * n = n^2 total operations, hence O(n^2).",
    },
    keyTakeaway: "Nested loops over the same data are a classic sign of O(n^2) complexity - worth noticing, since it scales much worse than a single loop as data grows.",
    commonMistake: "Assuming an algorithm that works fine on 10 test items will scale fine to 10,000 real items, without considering its actual Big O complexity.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "struct (value type) vs class (reference type)", desc: "Value types copy their data; reference types copy a reference to shared data.", example: "int a = 5; int b = a; // independent copies" },
      { syntax: "private static C instance; / public static C Instance { get { ... } }", desc: "Singleton - a private constructor and static accessor ensure only one instance.", example: "ConfigManager.Instance.Setting = \"x\";" },
      { syntax: "static IType Create(string kind) { ... }", desc: "A Factory centralizes object-creation decisions in one place.", example: "IShape shape = ShapeFactory.Create(\"circle\");" },
      { syntax: "SOLID: Single-responsibility, Open-closed, Liskov, Interface segregation, Dependency inversion", desc: "Five principles for maintainable object-oriented design.", example: "See the lesson for what each letter means." },
      { syntax: "Big O: O(1), O(n), O(n log n), O(n\u00b2)", desc: "Describes how an operation's cost grows with input size.", example: "Nested loops over the same data \u2192 O(n\u00b2)." },
    ],
  },
];

const CS_MASTER_INTERMEDIATE = [
  {
    title: "Multithreading basics",
    body: "By default, a C# program runs on a single thread, executing one instruction at a time in order. Creating additional threads lets separate pieces of work run concurrently, which can genuinely speed up CPU-heavy work on multi-core hardware, or keep an application responsive while a slow operation runs in the background. The Task class provides a higher-level, easier way to run work concurrently than managing raw Thread objects directly. When multiple threads access the same shared data at the same time, a lock statement ensures only one thread can execute a protected block at once, preventing the kind of data corruption that happens when two threads modify the same value simultaneously.",
    altExplain: "Multithreading runs separate pieces of work at the same time. A lock makes sure only one thread touches shared data at once, preventing threads from corrupting each other's work.",
    visual: null,
    code: "using System;\nusing System.Threading.Tasks;\n\nclass Program {\n  static int counter = 0;\n  static readonly object lockObj = new object();\n\n  static void Increment() {\n    lock (lockObj) {\n      counter++;\n    }\n  }\n\n  static void Main() {\n    Parallel.For(0, 1000, i => Increment());\n    Console.WriteLine(counter);\n  }\n}",
    keyTakeaway: "A lock ensures only one thread executes the protected block at a time - without it, concurrent access to shared data can silently corrupt results.",
    commonMistake: "Sharing mutable data across multiple threads without any lock, assuming it'll probably be fine - this kind of bug often works by accident in testing, then fails unpredictably in production.",
  },
  {
    title: "Recursion deep dive",
    body: "A recursive method calls itself, breaking a problem down into a smaller version of the same problem until it reaches a base case simple enough to answer directly without further recursion. Calculating a factorial is a classic example: factorial(n) equals n times factorial(n-1), with factorial(0) as the base case that stops the recursion. Every recursive method absolutely needs a base case - without one, it calls itself indefinitely until it exhausts the call stack and crashes with a StackOverflowException. Recursion often produces more elegant, readable code for naturally recursive problems (like tree traversal), though an equivalent loop-based solution can sometimes be more memory-efficient.",
    altExplain: "A recursive method calls itself with a smaller version of the same problem, until a base case stops it. Forgetting the base case causes infinite recursion and a crash.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static int Factorial(int n) {\n    if (n == 0) return 1; // base case\n    return n * Factorial(n - 1);\n  }\n\n  static void Main() {\n    Console.WriteLine(Factorial(5)); // 120\n  }\n}",
    refBox: {
      syntax: "returnType Method(params) { if (base) return x; return Method(smaller); }",
      desc: "Recursion - a method calling itself, with a base case that stops it.",
      example: "int Factorial(int n) {\n  if (n == 0) return 1;\n  return n * Factorial(n - 1);\n}",
    },
    keyTakeaway: "Every recursive method needs a base case that stops the recursion - without one, it recurses forever until the program crashes.",
    commonMistake: "Writing a recursive method without a clear base case, or with a base case that's never actually reachable given how the recursive call shrinks the problem.",
    challenge: {
      prompt: "Write a recursive method that calculates the sum of numbers from 1 to n.",
      hint: "Base case: n == 0 returns 0. Recursive case: n + SumTo(n - 1).",
      solution: "using System;\n\nclass Program {\n  static int SumTo(int n) {\n    if (n == 0) return 0;\n    return n + SumTo(n - 1);\n  }\n\n  static void Main() {\n    Console.WriteLine(SumTo(5)); // 15\n  }\n}",
    },
  },
  {
    title: "Sorting algorithms",
    body: "Bubble sort is one of the simplest sorting algorithms to understand: it repeatedly steps through a list, comparing adjacent pairs and swapping them if they're in the wrong order, with larger values gradually 'bubbling' toward the end of the list over multiple passes. It's rarely used in real production code because its O(n^2) time complexity makes it slow on large lists compared to more sophisticated algorithms, but its simplicity makes it an excellent way to build intuition for how a sorting algorithm actually works step by step, before moving on to faster approaches like merge sort or quicksort that real-world code (and C#'s own built-in .Sort()) typically uses instead.",
    altExplain: "Bubble sort repeatedly compares neighboring pairs and swaps them if out of order, gradually moving big values to the end. It's simple to understand but slow (O(n^2)) - real code uses faster built-in sorting instead.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static void BubbleSort(int[] arr) {\n    for (int i = 0; i < arr.Length - 1; i++) {\n      for (int j = 0; j < arr.Length - i - 1; j++) {\n        if (arr[j] > arr[j + 1]) {\n          int temp = arr[j];\n          arr[j] = arr[j + 1];\n          arr[j + 1] = temp;\n        }\n      }\n    }\n  }\n\n  static void Main() {\n    int[] numbers = { 5, 2, 8, 1, 9 };\n    BubbleSort(numbers);\n    foreach (int n in numbers) Console.WriteLine(n);\n  }\n}",
    refBox: {
      syntax: "Array.Sort(arr) / list.Sort() / list.Sort((a,b) => a - b)",
      desc: "The built-in sort, O(n log n) - prefer this over a hand-written O(n\u00b2) sort in real code.",
      example: "int[] nums = { 3, 1, 2 };\nArray.Sort(nums);",
    },
    keyTakeaway: "Bubble sort is O(n^2) - great for learning how sorting works, but real code should use the built-in Array.Sort() or List<T>.Sort(), which are far faster on large data.",
    commonMistake: "Implementing a custom sort like bubble sort in real production code instead of using the built-in, well-optimized .Sort() methods C# already provides.",
  },
  {
    title: "Searching algorithms",
    body: "Linear search checks every element one at a time until it finds a match (or reaches the end), which works on any list but takes O(n) time in the worst case. Binary search is dramatically faster - O(log n) - but requires the list to already be sorted: it repeatedly checks the middle element, and based on whether the target is smaller or larger, eliminates half of the remaining elements from consideration each time, narrowing in on the answer exponentially faster than checking one element at a time. This tradeoff - needing sorted data in exchange for much faster lookups - comes up constantly in real software design.",
    altExplain: "Linear search checks every item one by one. Binary search is much faster but needs a sorted list - it eliminates half the remaining possibilities with each check.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static int BinarySearch(int[] sortedArr, int target) {\n    int low = 0, high = sortedArr.Length - 1;\n    while (low <= high) {\n      int mid = (low + high) / 2;\n      if (sortedArr[mid] == target) return mid;\n      if (sortedArr[mid] < target) low = mid + 1;\n      else high = mid - 1;\n    }\n    return -1;\n  }\n\n  static void Main() {\n    int[] numbers = { 1, 3, 5, 7, 9, 11 };\n    Console.WriteLine(BinarySearch(numbers, 7));\n  }\n}",
    keyTakeaway: "Binary search requires sorted data - running it on an unsorted list gives unreliable results, since its whole logic depends on the ordering.",
    commonMistake: "Using binary search on data that isn't actually sorted, producing incorrect or inconsistent results without any obvious error.",
  },
  {
    title: "Unit testing concepts",
    body: "A unit test verifies that one small, isolated piece of code - typically a single method - behaves correctly, by running it with known inputs and checking the output matches what's expected. The Arrange-Act-Assert (AAA) pattern structures a typical test clearly: Arrange sets up the inputs and any needed objects, Act calls the method being tested, and Assert checks the result matches expectations. Writing tests alongside your code catches bugs early, and just as importantly, gives you confidence to change or refactor code later without accidentally breaking something that used to work - a good test suite is what makes fearless changes possible.",
    altExplain: "A unit test checks that one small piece of code works correctly, using known inputs. Arrange-Act-Assert structures a test: set up inputs, run the code, check the result.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "In the Arrange-Act-Assert pattern, what happens during 'Act'?",
      options: ["Setting up test data and objects", "Calling the actual method or code being tested", "Checking whether the result is correct", "Cleaning up after the test"],
      correct: 1,
      explanation: "Arrange sets up the inputs and objects needed. Act calls the method under test. Assert then checks that the result matches what was expected - each step has a distinct, clear purpose.",
    },
    keyTakeaway: "A solid test suite is what makes it safe to refactor or change code later - without tests, every change carries the risk of silently breaking something that used to work.",
    commonMistake: "Only writing tests for the 'happy path' (expected, valid input) while never testing edge cases like empty input, negative numbers, or unexpected values.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Task.Run(() => { ... }) / await Task.WhenAll(tasks)", desc: "Runs work on another thread; WhenAll waits for several tasks to finish.", example: "await Task.Run(() => HeavyWork());" },
      { syntax: "returnType Method(params) { if (base) return x; return Method(smaller); }", desc: "Recursion - a method calling itself, with a base case that stops it.", example: "int Factorial(int n) {\n  if (n == 0) return 1;\n  return n * Factorial(n - 1);\n}" },
      { syntax: "Array.Sort(arr) / list.Sort()", desc: "The built-in sort, O(n log n) - prefer over a hand-written O(n\u00b2) sort.", example: "Array.Sort(nums);" },
      { syntax: "Array.BinarySearch(sortedArr, target)", desc: "Binary search (O(log n)) on already-sorted data.", example: "int idx = Array.BinarySearch(nums, 7);" },
      { syntax: "Arrange / Act / Assert", desc: "The standard structure for a unit test - set up, run, then check the result.", example: "Assert.AreEqual(5, Add(2, 3));" },
    ],
  },
];

const CS_MASTER_ADVANCED = [
  {
    title: "Dependency Injection",
    body: "Dependency Injection (DI) means a class receives the objects it depends on from the outside, rather than creating them itself internally - typically passed in through its constructor. Instead of a class directly writing new EmailService() inside itself, it accepts an IEmailService through its constructor, and something else decides which actual implementation to provide. This makes the class far easier to test (you can pass in a fake or mock implementation during testing) and easier to change later (swapping the real implementation doesn't require touching the class that depends on it) - it's the direct practical application of the Dependency Inversion principle from SOLID, covered earlier in this course.",
    altExplain: "Dependency Injection means a class receives what it needs from outside (usually through its constructor) instead of creating it internally - making the class easier to test and to swap implementations for later.",
    visual: null,
    code: "using System;\n\ninterface IMessageService {\n  void Send(string message);\n}\n\nclass EmailService : IMessageService {\n  public void Send(string message) {\n    Console.WriteLine($\"Emailing: {message}\");\n  }\n}\n\nclass Notifier {\n  private IMessageService service;\n  public Notifier(IMessageService service) {\n    this.service = service;\n  }\n  public void Notify(string msg) {\n    service.Send(msg);\n  }\n}\n\nclass Program {\n  static void Main() {\n    Notifier notifier = new Notifier(new EmailService());\n    notifier.Notify(\"Hello!\");\n  }\n}",
    keyTakeaway: "Depending on an interface (like IMessageService) instead of a concrete class lets you swap implementations - real or fake, for testing - without changing the class that depends on it.",
    commonMistake: "Creating dependencies directly inside a class with 'new' instead of receiving them from outside, making that class hard to test in isolation.",
  },
  {
    title: "Design patterns: Observer",
    body: "The Observer pattern lets one object (the subject) notify a list of other objects (observers) automatically whenever something changes, without the subject needing to know any specifics about those observers beyond the fact that they're listening. This is the same underlying idea behind C#'s built-in event system: a subject exposes a way to subscribe, and every subscribed observer gets called when the subject raises a change. It's especially useful whenever several unrelated parts of a system all need to react to the same change - a stock price update, a form field changing - without hard-wiring the subject to know about each specific reaction directly.",
    altExplain: "Observer lets one object notify a list of listeners whenever something changes, without needing to know exactly who's listening or what they'll do about it.",
    visual: null,
    code: "using System;\nusing System.Collections.Generic;\n\nclass StockTicker {\n  private List<Action<double>> observers = new List<Action<double>>();\n\n  public void Subscribe(Action<double> observer) {\n    observers.Add(observer);\n  }\n\n  public void SetPrice(double price) {\n    foreach (var observer in observers) observer(price);\n  }\n}\n\nclass Program {\n  static void Main() {\n    StockTicker ticker = new StockTicker();\n    ticker.Subscribe(price => Console.WriteLine($\"Price changed to {price}\"));\n    ticker.SetPrice(105.50);\n  }\n}",
    refBox: {
      syntax: "List<Action<T>> observers; / observers.Add(cb); / foreach (var cb in observers) cb(data);",
      desc: "The Observer pattern - a subject notifies every subscribed callback when something changes.",
      example: "ticker.Subscribe(price => Console.WriteLine(price));",
    },
    keyTakeaway: "The Observer pattern is the same core idea behind C#'s built-in event system - a subject notifies subscribers without knowing anything about what each one actually does in response.",
    commonMistake: "Hard-coding a subject to directly call specific other objects when something changes, instead of letting any number of observers subscribe and react independently.",
  },
  {
    title: "Garbage collection & memory",
    body: ".NET manages memory for reference-type objects automatically through garbage collection: when no part of your program can reach an object anymore (no variable references it, directly or indirectly), it becomes eligible for cleanup, and the garbage collector reclaims that memory at a time of its own choosing, without you needing to manually free it like some other languages require. This is enormously convenient, eliminating an entire category of memory-management bugs, but it isn't entirely free - garbage collection does consume some CPU time to run, and holding onto references longer than necessary (like an ever-growing list you forgot to clear) can prevent objects from ever being collected, causing a memory leak despite having full automatic garbage collection.",
    altExplain: ".NET automatically frees memory for objects nothing references anymore - you don't manually manage it. But holding onto references longer than needed (like a list that keeps growing) can still cause a memory leak.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A program keeps adding items to a static List<T> that's never cleared, even though those items are no longer needed. What happens?",
      options: ["The garbage collector removes them automatically since nobody 'needs' them", "They stay in memory indefinitely since the list still references them - a memory leak", "The program automatically clears the list every hour", "This is impossible in C#"],
      correct: 1,
      explanation: "The garbage collector only reclaims objects that are truly unreachable. As long as the static list holds a reference to those items, they remain 'reachable' and won't be collected - even automatic garbage collection can't prevent this kind of leak.",
    },
    keyTakeaway: "Garbage collection prevents forgetting to free memory manually, but it can't save you from a reference you're still holding onto longer than necessary - that's still a real memory leak.",
    commonMistake: "Assuming garbage collection makes memory leaks impossible in C# - an unintentionally retained reference (like an ever-growing cache) can still leak memory despite full automatic GC.",
  },
  {
    title: "Reflection basics",
    body: "Reflection lets a program examine its own types and objects at runtime - discovering a class's methods, properties, and fields dynamically, rather than knowing them all at compile time. obj.GetType() returns an object's actual runtime type, and from there, GetProperties() or GetMethods() can list everything that type exposes, even properties or methods your code never explicitly names. This is genuinely powerful for building generic tools - like a debugger, a serializer, or a testing framework - that need to work with types they've never seen before, though it comes with a real performance cost compared to normal, direct method calls, since it involves extra runtime lookup work.",
    altExplain: "Reflection lets code inspect its own types and objects while running - discovering methods and properties dynamically, instead of knowing them all ahead of time. Useful for generic tools, but slower than normal code.",
    visual: null,
    code: "using System;\n\nclass Person {\n  public string Name = \"Ana\";\n  public int Age = 28;\n}\n\nclass Program {\n  static void Main() {\n    Person p = new Person();\n    Type type = p.GetType();\n\n    Console.WriteLine(type.Name);\n    foreach (var field in type.GetFields()) {\n      Console.WriteLine($\"{field.Name} = {field.GetValue(p)}\");\n    }\n  }\n}",
    refBox: {
      syntax: "obj.GetType() / type.GetFields() / type.GetMethods()",
      desc: "Inspects a type's members at runtime - powerful for generic tools, slower than direct access.",
      example: "foreach (var f in type.GetFields()) { }",
    },
    keyTakeaway: "Reflection trades some runtime performance for the ability to inspect and work with types dynamically - use it deliberately for generic tooling, not routine everyday code.",
    commonMistake: "Reaching for reflection in ordinary application code where a direct method call or property access would be simpler, clearer, and faster.",
  },
  {
    title: "Capstone: putting it together",
    body: "Every concept across this course - classes, interfaces, collections, exception handling, and the design patterns covered in this final level - comes together when building any real application, rather than being used in isolation. A well-structured small program might define an interface for a capability, provide one or more concrete implementations, use dependency injection to wire a class up with the implementation it needs, store its data in the right collection type for the access pattern involved, and wrap risky operations in proper exception handling. This is genuinely the actual practice of software engineering: not using any single concept in isolation, but combining the right tools from everything you know to fit the specific shape of the problem actually in front of you.",
    altExplain: "Real programs combine everything from this course together - interfaces, dependency injection, the right collection type, proper error handling - chosen to fit the specific problem, not used in isolation.",
    visual: null,
    code: "using System;\nusing System.Collections.Generic;\n\ninterface INotifier { void Notify(string message); }\nclass ConsoleNotifier : INotifier {\n  public void Notify(string message) { Console.WriteLine($\"[Notify] {message}\"); }\n}\n\nclass TaskManager {\n  private List<string> tasks = new List<string>();\n  private INotifier notifier;\n\n  public TaskManager(INotifier notifier) { this.notifier = notifier; }\n\n  public void AddTask(string task) {\n    tasks.Add(task);\n    notifier.Notify($\"Task added: {task}\");\n  }\n}\n\nclass Program {\n  static void Main() {\n    TaskManager manager = new TaskManager(new ConsoleNotifier());\n    manager.AddTask(\"Finish the course\");\n  }\n}",
    keyTakeaway: "Real software engineering is about combining the right tools - interfaces, DI, the right collection, solid error handling - to fit a specific problem, not applying any one concept everywhere by default.",
    commonMistake: "Learning individual concepts in isolation without practicing how they combine together in a single, realistic small program.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "class C { public C(IDependency dep) { this.dep = dep; } }", desc: "Dependency injection - receive what a class depends on from outside, don't create it internally.", example: "new TaskManager(new ConsoleNotifier())" },
      { syntax: "List<Action<T>> observers; / foreach (var cb in observers) cb(data);", desc: "The Observer pattern - notify every subscribed callback when something changes.", example: "ticker.Subscribe(price => Console.WriteLine(price));" },
      { syntax: "GC.Collect() (rarely needed - automatic)", desc: "C#'s garbage collector frees memory for objects nothing references anymore.", example: "Usually left entirely to the runtime." },
      { syntax: "obj.GetType() / type.GetFields() / type.GetMethods()", desc: "Inspects a type's members at runtime - useful for generic tools.", example: "foreach (var f in type.GetFields()) { }" },
      { syntax: "interface + DI + right collection + try/catch", desc: "Real programs combine idioms together to fit the problem, not one technique everywhere.", example: "See the capstone lesson for a full example." },
    ],
  },
];

const CS_MASTER_QUIZ_BANK = {
  basic: [
    { q: "Assigning one reference-type variable to another...", options: ["Copies the full object independently", "Copies a reference, so both point to the same object", "Is not allowed in C#", "Only works with value types"], correct: 1 },
    { q: "A Singleton pattern ensures...", options: ["Multiple instances can be created freely", "Only one instance of a class exists, accessed through one global point", "A class can never be instantiated", "All methods are static"], correct: 1 },
    { q: "A Factory pattern's main benefit is...", options: ["Faster runtime performance always", "Centralizing object-creation logic in one place", "Removing the need for interfaces", "Making all classes static"], correct: 1 },
    { q: "The 'S' in SOLID stands for...", options: ["Static typing", "Single Responsibility", "Structured programming", "Simplicity"], correct: 1 },
    { q: "A nested loop over the same list of size n typically has Big O complexity of...", options: ["O(1)", "O(n)", "O(n^2)", "O(log n)"], correct: 2 },
  ],
  intermediate: [
    { q: "A 'lock' statement is used to...", options: ["Speed up a single-threaded program", "Ensure only one thread executes a block at a time", "Prevent a program from ever crashing", "Replace exception handling"], correct: 1 },
    { q: "Every recursive method needs...", options: ["No return statement", "A base case that stops the recursion", "At least two parameters", "To call a different method, never itself"], correct: 1 },
    { q: "Bubble sort's time complexity is generally...", options: ["O(1)", "O(log n)", "O(n^2)", "O(n^3)"], correct: 2 },
    { q: "Binary search requires the data to be...", options: ["Unsorted", "Sorted", "Exactly 10 items", "Stored in a Dictionary"], correct: 1 },
    { q: "In Arrange-Act-Assert, 'Assert' is where you...", options: ["Set up test data", "Call the method being tested", "Check the result matches what's expected", "Delete the test"], correct: 2 },
  ],
  advanced: [
    { q: "Dependency Injection means a class...", options: ["Creates all its own dependencies internally", "Receives its dependencies from outside, typically via constructor", "Never depends on anything else", "Can only depend on concrete classes"], correct: 1 },
    { q: "The Observer pattern lets a subject...", options: ["Directly control every observer's internal logic", "Notify a list of subscribers without knowing their specifics", "Prevent any object from listening", "Replace the need for events entirely"], correct: 1 },
    { q: "Garbage collection in .NET...", options: ["Makes memory leaks completely impossible", "Automatically frees genuinely unreachable objects, but can't prevent leaks from lingering references", "Only works for value types", "Must be triggered manually"], correct: 1 },
    { q: "Reflection is most useful for...", options: ["Everyday application logic for better performance", "Building generic tools that inspect types dynamically at runtime", "Replacing all interfaces", "Making code run faster"], correct: 1 },
    { q: "Real-world software engineering typically involves...", options: ["Using exactly one design pattern for everything", "Combining multiple concepts and tools to fit a specific problem", "Avoiding interfaces entirely", "Writing all code in a single method"], correct: 1 },
  ],
};

// Wire this course into the shared registry (COURSE_CONTENT is declared in data.js,
// loaded before this file - we're adding to it, not replacing it).
COURSE_CONTENT.csharp = {
  entry: {
    basic: CS_ENTRY_BASIC,
    intermediate: CS_ENTRY_INTERMEDIATE,
    advanced: CS_ENTRY_ADVANCED,
    quiz: CS_QUIZ_BANK,
  },
  professional: {
    basic: CS_PRO_BASIC,
    intermediate: CS_PRO_INTERMEDIATE,
    advanced: CS_PRO_ADVANCED,
    quiz: CS_PRO_QUIZ_BANK,
  },
  master: {
    basic: CS_MASTER_BASIC,
    intermediate: CS_MASTER_INTERMEDIATE,
    advanced: CS_MASTER_ADVANCED,
    quiz: CS_MASTER_QUIZ_BANK,
  },
};
