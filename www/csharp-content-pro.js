// =========================================================================
// C# Programming - Professional Level
// =========================================================================
const CS_PRO_BASIC = [
  {
    title: "Enums",
    body: "An enum defines a named set of related constant values, making code far more readable than using raw numbers or strings to represent a fixed set of options. Writing enum Status { Pending, Paid, Cancelled } creates a new type where a variable can only hold one of those three named values - Status.Paid is clearer and safer to write and read than an arbitrary number like 2 meaning the same thing. Under the hood, each enum value corresponds to an integer (Pending is 0, Paid is 1, and so on by default), but working with the named values instead of raw numbers avoids an entire category of 'what does this number even mean' confusion.",
    altExplain: "An enum is a named set of fixed options, like Status.Paid or Status.Cancelled - much clearer than using raw numbers or magic strings to represent the same idea.",
    visual: null,
    code: "using System;\n\nenum Status { Pending, Paid, Cancelled }\n\nclass Program {\n  static void Main() {\n    Status orderStatus = Status.Paid;\n    Console.WriteLine(orderStatus);\n\n    if (orderStatus == Status.Paid) {\n      Console.WriteLine(\"Order is paid!\");\n    }\n  }\n}",
    keyTakeaway: "Enums make a fixed set of options self-documenting - Status.Paid says what it means, unlike an unexplained raw number.",
    commonMistake: "Using raw integers or loose strings to represent a fixed set of states instead of an enum, making the code harder to read and easier to mistype.",
    challenge: {
      prompt: "Create an enum called Direction with North, South, East, West, and print one of the values.",
      hint: "enum Direction { North, South, East, West }",
      solution: "using System;\n\nenum Direction { North, South, East, West }\n\nclass Program {\n  static void Main() {\n    Direction d = Direction.East;\n    Console.WriteLine(d);\n  }\n}",
    },
  },
  {
    title: "Structs vs classes",
    body: "A struct looks similar to a class - both can hold fields and methods - but they differ in a fundamental way: a class is a reference type (a variable holds a reference to the object, stored elsewhere), while a struct is a value type (a variable holds the actual data directly). This means assigning one struct variable to another copies the entire value, so changing the copy doesn't affect the original - the opposite of how class objects behave, where both variables would point to the same underlying object. Structs are generally best suited for small, simple data groupings (like a Point with X and Y coordinates) where copy behavior is actually desirable, rather than for larger, more complex objects.",
    altExplain: "A struct copies its entire value when assigned to another variable. A class instead copies just a reference, so both variables point to the same underlying object.",
    visual: null,
    code: "using System;\n\nstruct Point {\n  public int X, Y;\n}\n\nclass Program {\n  static void Main() {\n    Point p1 = new Point { X = 1, Y = 2 };\n    Point p2 = p1; // this COPIES the value\n    p2.X = 99;\n\n    Console.WriteLine(p1.X); // still 1\n    Console.WriteLine(p2.X); // 99\n  }\n}",
    keyTakeaway: "Assigning a struct copies its full value; assigning a class object copies only a reference to the same underlying object.",
    commonMistake: "Using a struct for a large, complex object expecting reference-type behavior, then being confused when changes to a copy don't affect the original.",
  },
  {
    title: "Nullable types & null-conditional operators",
    body: "Value types like int normally can't hold a 'no value at all' state - they always hold some number. Writing int? age (with a question mark) makes it nullable, allowing it to hold either a valid int or null, representing 'no value provided.' The null-conditional operator ?. safely accesses a member only if the object isn't null, immediately short-circuiting to null instead of crashing if it is - person?.Name checks whether person is null before trying to access its Name, avoiding the common NullReferenceException that happens when accessing a member on a null reference directly.",
    altExplain: "int? lets a number-type variable also hold 'no value' (null). The ?. operator safely checks for null before accessing something, instead of crashing if it's null.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static void Main() {\n    int? age = null;\n    Console.WriteLine(age.HasValue);\n\n    string name = null;\n    Console.WriteLine(name?.Length ?? -1); // -1 if name is null\n  }\n}",
    keyTakeaway: "?. safely short-circuits to null if the object is null, avoiding a NullReferenceException - combine it with ?? to supply a fallback value.",
    commonMistake: "Accessing a member directly on an object that might be null (object.Property) without checking first, causing a NullReferenceException crash.",
  },
  {
    title: "Generics basics",
    body: "Generics let you write a class or method that works with any type, decided when it's actually used, rather than writing separate near-identical versions for each type you need. List<T>, which you've already used, is itself a generic class - List<int> and List<string> are the same underlying code, just specialized for different types. You can write your own generic methods too: static T FirstOrDefault<T>(List<T> items) works for a list of any type, with T standing in as a placeholder for whatever type is actually supplied at the call site.",
    altExplain: "Generics let you write one class or method that works with any type, instead of writing a separate copy for int, another for string, and so on.",
    visual: null,
    code: "using System;\nusing System.Collections.Generic;\n\nclass Program {\n  static T GetFirst<T>(List<T> items) {\n    return items[0];\n  }\n\n  static void Main() {\n    List<int> numbers = new List<int> { 5, 10, 15 };\n    Console.WriteLine(GetFirst(numbers));\n\n    List<string> names = new List<string> { \"Ana\", \"Marco\" };\n    Console.WriteLine(GetFirst(names));\n  }\n}",
    refBox: {
      syntax: "static T Method<T>(T param) { ... }",
      desc: "Generics let one method/class work with any type, decided at the call site.",
      example: "static T First<T>(List<T> items) {\n  return items[0];\n}",
    },
    keyTakeaway: "The same generic method works correctly for any type, with the compiler enforcing type safety for each specific use - T isn't just 'any object', it's a real type once used.",
    commonMistake: "Writing near-duplicate methods for different types (GetFirstInt, GetFirstString) instead of a single generic method that handles both.",
  },
  {
    title: "Dictionaries",
    body: "A Dictionary<TKey, TValue> stores key-value pairs, letting you look up a value instantly by its associated key instead of searching through a list one item at a time. Writing Dictionary<string, int> ages lets you store ages keyed by name, and ages[\"Ana\"] retrieves Ana's age directly. Keys must be unique within a dictionary - assigning to an existing key overwrites its value rather than adding a duplicate entry - and trying to access a key that doesn't exist throws an exception, which is why TryGetValue() is the safer choice when you're not certain a key is present.",
    altExplain: "A Dictionary stores key-value pairs, so you can look something up by name instantly instead of searching through a list one item at a time.",
    visual: null,
    code: "using System;\nusing System.Collections.Generic;\n\nclass Program {\n  static void Main() {\n    Dictionary<string, int> ages = new Dictionary<string, int>();\n    ages[\"Ana\"] = 28;\n    ages[\"Marco\"] = 34;\n\n    Console.WriteLine(ages[\"Ana\"]);\n\n    if (ages.TryGetValue(\"Liza\", out int lizaAge)) {\n      Console.WriteLine(lizaAge);\n    } else {\n      Console.WriteLine(\"Not found\");\n    }\n  }\n}",
    refBox: {
      syntax: "Dictionary<TKey, TValue> d = new(); / d.TryGetValue(key, out var v)",
      desc: "Key-value lookup. TryGetValue is the safe way to check a key that might not exist.",
      example: "if (ages.TryGetValue(\"Ana\", out int age)) { }",
    },
    keyTakeaway: "TryGetValue() is safer than direct indexing when a key might not exist - it returns false instead of throwing an exception.",
    commonMistake: "Accessing a dictionary key directly (dict[\"missing\"]) without checking it exists first, causing a KeyNotFoundException crash.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "static T Method<T>(T param) { ... }", desc: "Generics - one method/class works with any type, decided at the call site.", example: "static T First<T>(List<T> items) {\n  return items[0];\n}" },
      { syntax: "Dictionary<TKey, TValue> d = new(); / d.TryGetValue(key, out var v)", desc: "Key-value lookup - TryGetValue safely checks a key that might not exist.", example: "if (ages.TryGetValue(\"Ana\", out int age)) { }" },
    ],
  },
];

const CS_PRO_INTERMEDIATE = [
  {
    title: "LINQ basics",
    body: "LINQ (Language Integrated Query) lets you filter, transform, and query collections using a compact, readable syntax built directly into C#, rather than writing manual loops for common operations. numbers.Where(n => n > 10) returns only the elements matching a condition, and numbers.Select(n => n * 2) transforms every element according to a given expression. These methods can be chained together, and each one returns a new sequence rather than modifying the original collection - LINQ favors this non-destructive, composable style, which makes multi-step data transformations much easier to read than the equivalent nested loops.",
    altExplain: "LINQ lets you filter and transform collections with methods like Where and Select, instead of writing a manual loop for every operation - and you can chain several together.",
    visual: null,
    code: "using System;\nusing System.Linq;\nusing System.Collections.Generic;\n\nclass Program {\n  static void Main() {\n    List<int> numbers = new List<int> { 3, 8, 15, 20, 4 };\n    var bigOnes = numbers.Where(n => n > 10).ToList();\n\n    foreach (int n in bigOnes) Console.WriteLine(n);\n  }\n}",
    refBox: {
      syntax: "collection.Where(x => cond).Select(x => expr)",
      desc: "Where filters, Select transforms - both return a new sequence, chainable, no manual loop needed.",
      example: "numbers.Where(n => n > 10).Select(n => n * 2)",
    },
    keyTakeaway: "LINQ methods return a new sequence rather than modifying the original collection - always assign or use the result.",
    commonMistake: "Forgetting 'using System.Linq;' at the top of the file - without it, methods like Where and Select aren't available on collections.",
    challenge: {
      prompt: "Use LINQ to double every number in a list and print the results.",
      hint: "numbers.Select(n => n * 2)",
      solution: "using System;\nusing System.Linq;\nusing System.Collections.Generic;\n\nclass Program {\n  static void Main() {\n    List<int> numbers = new List<int> { 1, 2, 3 };\n    var doubled = numbers.Select(n => n * 2).ToList();\n    foreach (int n in doubled) Console.WriteLine(n);\n  }\n}",
    },
  },
  {
    title: "Delegates & events",
    body: "A delegate is a type that represents a method signature, letting you treat a method itself as a value - store it in a variable, pass it as a parameter, or call it indirectly through the delegate. This is the foundation events are built on: an event lets one part of a program notify other parts when something happens, without the notifying code needing to know exactly who's listening. A classic use case is a button click - the button doesn't need to know what should happen when clicked, it just raises a click event, and whatever code subscribed to that event runs in response.",
    altExplain: "A delegate lets you treat a method as a value you can store or pass around. Events build on this to let one part of code announce 'something happened' without knowing who's listening.",
    visual: null,
    code: "using System;\n\nclass Program {\n  delegate void Greeting(string name);\n\n  static void SayHello(string name) {\n    Console.WriteLine($\"Hello, {name}!\");\n  }\n\n  static void Main() {\n    Greeting greet = SayHello;\n    greet(\"Ana\");\n  }\n}",
    keyTakeaway: "A delegate variable can hold a reference to any method matching its signature - calling the delegate calls whatever method it currently points to.",
    commonMistake: "Confusing calling a method (SayHello(\"Ana\")) with assigning it to a delegate (Greeting greet = SayHello;) - the second doesn't run it immediately, just stores it.",
  },
  {
    title: "Lambda expressions",
    body: "A lambda expression is a compact, inline, unnamed function, most commonly used as a short-lived argument to another method - exactly what powers LINQ's Where and Select from earlier. The syntax n => n > 10 reads as 'given n, return whether n is greater than 10' - the part before => is the parameter, and the part after is the expression to evaluate. Lambdas can take multiple parameters (a, b) => a + b, or none at all () => Console.WriteLine(\"hi\"), and they're especially useful exactly when you need a small piece of custom logic just once, without the overhead of declaring a fully separate named method for it.",
    altExplain: "A lambda is a tiny inline function, written as (input) => expression. It's the syntax behind n => n > 10 used in LINQ's Where method.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static void Main() {\n    Func<int, int, int> add = (a, b) => a + b;\n    Console.WriteLine(add(3, 4));\n\n    Action greet = () => Console.WriteLine(\"Hi there!\");\n    greet();\n  }\n}",
    refBox: {
      syntax: "(param) => expression",
      desc: "A compact inline function - the part before => is the parameter, after is the expression.",
      example: "Func<int,int,int> add = (a, b) => a + b;",
    },
    keyTakeaway: "A lambda is just a shorthand way to write a small function inline - anywhere you see input => expression, that's a lambda.",
    commonMistake: "Writing an overly complex, multi-line lambda where a properly named separate method would actually be clearer and easier to debug.",
  },
  {
    title: "Extension methods",
    body: "An extension method lets you add a new method to an existing type - even one you don't own the source code for, like string or int - without modifying that type's original definition or creating a subclass. It's declared as a static method inside a static class, with the this keyword before the first parameter marking which type it extends: public static bool IsLong(this string s) adds an IsLong() method that can then be called directly on any string, as though it had always been part of the string type itself. This is purely a compile-time convenience - it doesn't actually change the original type, it just lets you call your method using familiar dot syntax.",
    altExplain: "An extension method lets you add a new method onto an existing type (even ones you don't own, like string), called with normal dot syntax, without actually modifying that type.",
    visual: null,
    code: "using System;\n\nstatic class StringExtensions {\n  public static bool IsLong(this string s) {\n    return s.Length > 10;\n  }\n}\n\nclass Program {\n  static void Main() {\n    string text = \"Hello there, world!\";\n    Console.WriteLine(text.IsLong());\n  }\n}",
    keyTakeaway: "Extension methods must be static, live in a static class, and mark their first parameter with 'this' - that's what makes the dot-syntax call work.",
    commonMistake: "Forgetting the 'this' keyword on the first parameter - without it, it's just a regular static method, not an extension method callable via dot syntax.",
  },
  {
    title: "String formatting & parsing",
    body: "Converting between text and other data types is a routine task: int.Parse() and double.Parse() convert a string into a number, throwing an exception if the text isn't actually a valid number, while int.TryParse() attempts the same conversion but returns true or false instead of throwing, which is generally safer when the input might not be well-formed. Going the other direction, ToString() converts nearly any value into its text representation, and can accept a format string for more control - like value.ToString(\"F2\") to show exactly two decimal places, useful for formatting currency or measurements consistently.",
    altExplain: "Parse converts text into a number (and crashes on bad input). TryParse does the same but safely returns false instead of crashing. ToString converts a value back into text, with optional formatting.",
    visual: null,
    code: "using System;\n\nclass Program {\n  static void Main() {\n    string input = \"42\";\n    int number = int.Parse(input);\n    Console.WriteLine(number + 8);\n\n    double price = 19.5;\n    Console.WriteLine(price.ToString(\"F2\"));\n\n    if (int.TryParse(\"abc\", out int result)) {\n      Console.WriteLine(result);\n    } else {\n      Console.WriteLine(\"Not a valid number\");\n    }\n  }\n}",
    keyTakeaway: "Prefer TryParse over Parse whenever the input might not be a valid number - it fails safely instead of throwing an exception.",
    commonMistake: "Using Parse() on user-provided input without validating it first, letting an unexpected non-numeric input crash the whole program.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "collection.Where(x => cond).Select(x => expr)", desc: "LINQ - filter and transform collections, chainable, no manual loop needed.", example: "numbers.Where(n => n > 10).Select(n => n * 2)" },
      { syntax: "delegate returnType Name(params); / Name handler = Method;", desc: "A delegate treats a method as a value - store, pass, or call it indirectly.", example: "Greeting greet = SayHello;\ngreet(\"Ana\");" },
      { syntax: "(param) => expression", desc: "A compact inline function - used constantly as a LINQ/delegate argument.", example: "Func<int,int,int> add = (a, b) => a + b;" },
      { syntax: "int.Parse(s) / int.TryParse(s, out var n)", desc: "Parse converts text to a number (throws on bad input); TryParse fails safely.", example: "if (int.TryParse(\"abc\", out int n)) { }" },
    ],
  },
];

const CS_PRO_ADVANCED = [
  {
    title: "Abstract classes vs interfaces",
    body: "An abstract class is similar to a regular class but can't be instantiated directly - it's meant to be inherited from - and can mix fully-implemented methods with abstract ones that derived classes must provide themselves. This differs from an interface, which historically provided no implementation at all: an abstract class is the right choice when several related classes share some common, ready-to-use behavior alongside some behavior each must implement uniquely, while an interface fits better when you only need to guarantee a contract with no shared implementation at all. As a rule of thumb, prefer an interface for a pure capability contract, and an abstract class when there's genuinely shared code worth inheriting alongside that contract.",
    altExplain: "An abstract class can mix real, working methods with ones that must be filled in by subclasses. An interface is a pure contract with no implementation. Use an abstract class when there's real shared code to inherit.",
    visual: null,
    code: "using System;\n\nabstract class Shape {\n  public abstract double GetArea();\n\n  public void Describe() {\n    Console.WriteLine($\"This shape has area {GetArea()}\");\n  }\n}\n\nclass Rectangle : Shape {\n  public double Width, Height;\n  public override double GetArea() {\n    return Width * Height;\n  }\n}\n\nclass Program {\n  static void Main() {\n    Shape s = new Rectangle { Width = 4, Height = 5 };\n    s.Describe();\n  }\n}",
    keyTakeaway: "An abstract class can be inherited from but never instantiated directly - trying 'new Shape()' on an abstract class is a compile error.",
    commonMistake: "Choosing an abstract class purely out of habit when an interface would be simpler and more flexible, since a class can implement many interfaces but inherit only one class.",
  },
  {
    title: "Custom exceptions",
    body: "While C# provides many built-in exception types, creating your own by inheriting from Exception lets you represent domain-specific error conditions clearly - like InsufficientFundsException for a banking application, rather than a generic Exception with a vague message. A custom exception class typically just needs a constructor that passes a message up to the base Exception class, and from there it can be thrown with throw new InsufficientFundsException(\"...\") and caught specifically with catch (InsufficientFundsException ex), letting calling code respond precisely to that particular business scenario rather than any general error.",
    altExplain: "You can create your own exception types by inheriting from Exception, giving errors specific to your program a clear, meaningful name instead of a generic message.",
    visual: null,
    code: "using System;\n\nclass InsufficientFundsException : Exception {\n  public InsufficientFundsException(string message) : base(message) {}\n}\n\nclass Program {\n  static void Withdraw(double balance, double amount) {\n    if (amount > balance) throw new InsufficientFundsException(\"Not enough funds!\");\n    Console.WriteLine(\"Withdrawal successful\");\n  }\n\n  static void Main() {\n    try {\n      Withdraw(100, 500);\n    } catch (InsufficientFundsException ex) {\n      Console.WriteLine(ex.Message);\n    }\n  }\n}",
    refBox: {
      syntax: "class MyError : Exception { public MyError(string msg) : base(msg) {} }",
      desc: "Creates a custom exception type, identifiable in a specific catch clause.",
      example: "catch (InsufficientFundsException ex) { }",
    },
    keyTakeaway: "A custom exception's constructor should call : base(message) to pass the message up to the built-in Exception class properly.",
    commonMistake: "Throwing a generic Exception for every error case instead of a specific custom type, making it impossible for calling code to catch and handle different failure scenarios differently.",
  },
  {
    title: "Working with text: StringBuilder",
    body: "Repeatedly combining strings with + inside a loop is surprisingly inefficient, since strings in C# are immutable - each concatenation actually creates an entirely new string in memory rather than modifying the original. StringBuilder solves this by providing a mutable buffer specifically designed for building up text incrementally: .Append() adds text without creating a new object each time, and .ToString() converts the final result into a regular string only once, at the end. For any loop that builds up a string piece by piece, especially over many iterations, StringBuilder is meaningfully more efficient than repeated string concatenation.",
    altExplain: "Strings can't actually change once created - combining them with + repeatedly creates a new string each time. StringBuilder is a more efficient tool built specifically for constructing text piece by piece.",
    visual: null,
    code: "using System;\nusing System.Text;\n\nclass Program {\n  static void Main() {\n    StringBuilder sb = new StringBuilder();\n    for (int i = 1; i <= 5; i++) {\n      sb.Append(i);\n      sb.Append(\", \");\n    }\n    Console.WriteLine(sb.ToString());\n  }\n}",
    keyTakeaway: "Strings are immutable in C# - every + concatenation creates a new string object. StringBuilder avoids this overhead for repeated text-building.",
    commonMistake: "Concatenating strings with + inside a loop that runs many times, instead of using StringBuilder - each iteration secretly builds and discards an entire new string.",
  },
  {
    title: "Collections deep dive",
    body: "Beyond List and Dictionary, C# provides several other collection types suited to specific patterns. HashSet<T> stores unique values with no duplicates and no guaranteed order, ideal for fast 'does this exist' checks. Queue<T> is first-in-first-out (FIFO) - items are removed in the same order they were added, like a real waiting line. Stack<T> is last-in-first-out (LIFO) - the most recently added item is removed first, like a stack of plates. Choosing the collection type that actually matches your access pattern, rather than defaulting to List for everything, often makes both your code's intent and its performance characteristics clearer.",
    altExplain: "HashSet stores unique values with no duplicates. Queue is first-in-first-out, like a line. Stack is last-in-first-out, like a stack of plates. Pick whichever matches how you'll actually use the data.",
    visual: null,
    code: "using System;\nusing System.Collections.Generic;\n\nclass Program {\n  static void Main() {\n    Queue<string> line = new Queue<string>();\n    line.Enqueue(\"Ana\");\n    line.Enqueue(\"Marco\");\n    Console.WriteLine(line.Dequeue()); // Ana (first in)\n\n    Stack<string> plates = new Stack<string>();\n    plates.Push(\"Plate 1\");\n    plates.Push(\"Plate 2\");\n    Console.WriteLine(plates.Pop()); // Plate 2 (last in)\n  }\n}",
    keyTakeaway: "Queue is FIFO (first in, first out); Stack is LIFO (last in, first out) - picking the wrong one for your access pattern leads to processing items in the wrong order.",
    commonMistake: "Defaulting to List<T> for everything and manually managing 'remove from the front' or 'remove from the end' logic, when Queue or Stack already does exactly that.",
  },
  {
    title: "Async/await basics",
    body: "Some operations - reading a file, calling a web service - take time to complete, and running them synchronously would freeze the rest of the program while waiting. async and await let you write code that waits for a slow operation without blocking everything else: marking a method async and using await Task.Delay(...) (or any other async operation) tells the program 'pause here until this finishes, but let other work continue in the meantime.' The method's return type becomes Task (or Task<T> if it returns a value), signaling to callers that this method runs asynchronously.",
    altExplain: "async/await let a slow operation run without freezing the rest of your program while it waits - like waiting for a file to load without locking everything else up.",
    visual: null,
    code: "using System;\nusing System.Threading.Tasks;\n\nclass Program {\n  static async Task SayHelloAfterDelay() {\n    await Task.Delay(100);\n    Console.WriteLine(\"Hello after a short wait!\");\n  }\n\n  static async Task Main() {\n    Console.WriteLine(\"Starting...\");\n    await SayHelloAfterDelay();\n    Console.WriteLine(\"Done.\");\n  }\n}",
    refBox: {
      syntax: "static async Task Method() { await SomeAsyncCall(); }",
      desc: "await pauses until a slow operation finishes, without freezing the rest of the program.",
      example: "await Task.Delay(100);\nConsole.WriteLine(\"done waiting\");",
    },
    keyTakeaway: "An async method's return type is Task (or Task<T>) - this signals to whoever calls it that it should be awaited rather than treated as instantly complete.",
    commonMistake: "Calling an async method without await, which lets it run in the background unmonitored - the calling code moves on immediately, before the async work is actually done.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "abstract class C { public abstract void M(); } / interface I { void M(); }", desc: "Abstract classes can share implementation; interfaces are a pure contract.", example: "abstract class Shape { public abstract double Area(); }" },
      { syntax: "class MyError : Exception { public MyError(string msg) : base(msg) {} }", desc: "A custom exception type, catchable specifically by name.", example: "catch (InsufficientFundsException ex) { }" },
      { syntax: "StringBuilder sb = new(); sb.Append(text);", desc: "Efficient repeated string building - avoids creating a new string on every concatenation.", example: "sb.Append(\"a\").Append(\"b\").ToString();" },
      { syntax: "Queue<T> / Stack<T> / HashSet<T>", desc: "Specialized collections: FIFO, LIFO, and unique-values-only respectively.", example: "var seen = new HashSet<int>();" },
      { syntax: "static async Task Method() { await SomeAsyncCall(); }", desc: "await pauses until a slow operation finishes, without freezing the program.", example: "await Task.Delay(100);" },
    ],
  },
];

const CS_PRO_QUIZ_BANK = {
  basic: [
    { q: "What's the main benefit of using an enum?", options: ["Faster performance always", "A named, self-documenting set of fixed options instead of raw numbers", "It removes the need for classes", "It only works with strings"], correct: 1 },
    { q: "Assigning one struct variable to another...", options: ["Copies a reference to the same data", "Copies the entire value independently", "Is not allowed", "Only works with classes"], correct: 1 },
    { q: "int? allows a variable to hold...", options: ["Only positive numbers", "A valid int or null", "Only even numbers", "Text as well as numbers"], correct: 1 },
    { q: "Generics let you...", options: ["Write one method or class that works with multiple types", "Skip declaring types entirely", "Only work with numbers", "Avoid using classes"], correct: 0 },
    { q: "TryGetValue on a Dictionary is safer than direct indexing because it...", options: ["Is always faster", "Returns false instead of throwing when a key is missing", "Works with any data type only", "Automatically adds missing keys"], correct: 1 },
  ],
  intermediate: [
    { q: "LINQ's Where method...", options: ["Modifies the original collection directly", "Returns a new filtered sequence", "Only works with strings", "Requires no using statement"], correct: 1 },
    { q: "A delegate is best described as...", options: ["A type representing a method signature, so methods can be stored as values", "A kind of loop", "A built-in exception type", "A synonym for class"], correct: 0 },
    { q: "A lambda expression n => n > 10 is...", options: ["A full class definition", "A compact inline function", "A type declaration", "A LINQ keyword only"], correct: 1 },
    { q: "An extension method's first parameter is marked with...", options: ["static", "this", "extend", "ref"], correct: 1 },
    { q: "TryParse compared to Parse...", options: ["Always throws on bad input, just like Parse", "Returns false instead of throwing on invalid input", "Only works with strings", "Is deprecated"], correct: 1 },
  ],
  advanced: [
    { q: "An abstract class differs from an interface because it can...", options: ["Never be inherited from", "Include fully-implemented methods alongside abstract ones", "Only exist without any methods", "Be instantiated directly"], correct: 1 },
    { q: "A custom exception class should typically...", options: ["Avoid inheriting from Exception", "Inherit from Exception and pass a message to the base constructor", "Never be caught specifically", "Replace all built-in exceptions"], correct: 1 },
    { q: "Why use StringBuilder instead of repeated string concatenation in a loop?", options: ["Strings would be sorted incorrectly otherwise", "Strings are immutable, so repeated + creates many discarded strings", "It's required by the compiler", "It only matters for numbers"], correct: 1 },
    { q: "Queue<T> follows which order?", options: ["Last-in-first-out (LIFO)", "First-in-first-out (FIFO)", "Random order", "Alphabetical order"], correct: 1 },
    { q: "An async method's return type is typically...", options: ["void always", "Task or Task<T>", "string", "int"], correct: 1 },
  ],
};
