// =========================================================================
// Power BI - Entry Level
// =========================================================================
const PBI_ENTRY_BASIC = [
  {
    title: "What is Power BI & the BI workflow",
    body: "Power BI is Microsoft's business intelligence tool for connecting to data, modeling relationships between tables, and building interactive visual reports and dashboards - covering the same 'collect, clean, analyze, present' pipeline from the Data Analytics course, but through Power BI's own dedicated tooling. The typical workflow moves through three stages: Power Query imports and cleans raw data, the Data Model defines relationships and calculations between tables, and Reports present that data visually for a specific audience to explore. Understanding this three-stage flow up front makes everything else in this course click into place as a piece of one coherent pipeline.",
    altExplain: "Power BI connects to data, models relationships between tables, and builds interactive visual reports - the same 'collect, clean, analyze, present' pipeline from Data Analytics, using Microsoft's dedicated tooling.",
    visual: null,
    keyTakeaway: "Power BI's workflow has three stages: Power Query (import/clean), Data Model (relationships/calculations), Reports (visual presentation) - everything else in this course fits into one of these.",
    commonMistake: "Jumping straight to building visuals before the underlying data model and relationships are properly set up, leading to reports that are hard to fix once problems surface.",
  },
  {
    title: "Power BI Desktop overview",
    body: "Power BI Desktop, the free authoring tool, has three main views accessible from icons along the left side: Report view, where you build and arrange visuals; Data view, where you inspect the actual rows of your loaded tables directly, similar to looking at a spreadsheet; and Model view, where you see and manage the relationships between your different tables visually, as connecting lines between table boxes. Switching between these views throughout a project - checking raw data, adjusting a relationship, then returning to refine a visual - is completely normal, not a sign you're doing something wrong.",
    altExplain: "Power BI Desktop has three views: Report (build visuals), Data (inspect raw rows, like a spreadsheet), and Model (see and manage relationships between tables visually).",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You want to check whether two tables are actually related correctly before building a visual that uses both. Which view should you check?",
      options: ["Report view", "Model view, to see the relationship between the tables", "Data view only", "There's no way to check this visually"],
      correct: 1,
      explanation: "Model view specifically shows the relationships between tables as visual connecting lines, letting you confirm (or fix) how tables are actually related before relying on that relationship in a report visual.",
    },
    keyTakeaway: "Model view is where you visually confirm and manage how tables relate to each other - check here first whenever a report visual combining multiple tables isn't behaving as expected.",
    commonMistake: "Troubleshooting a visual that combines two tables by only looking at Report view, when the actual problem is often a missing or incorrect relationship, visible in Model view.",
  },
  {
    title: "Connecting to data sources",
    body: "Power BI can connect to a wide range of data sources - Excel files, SQL databases, web APIs, cloud services - through Get Data, which walks you through selecting a source type and providing connection details. Once connected, you choose which tables or data to actually load, and can apply transformations before the data ever lands in your report, through the same Power Query interface covered in the Excel course. A key early decision is Import mode (data is copied into Power BI's own optimized storage) versus DirectQuery (Power BI queries the live source directly each time), a tradeoff covered in more depth later in this course.",
    altExplain: "Get Data connects Power BI to sources like Excel, SQL databases, and web APIs. Import mode copies data into Power BI's own storage; DirectQuery queries the live source each time - a tradeoff covered more deeply later.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A report needs to reflect a source database's data that changes every few seconds, with the report always showing the absolute latest values. Which connection mode fits this need better at a basic level?",
      options: ["Import mode, since it's always current", "DirectQuery, since it queries the live source directly rather than a periodically refreshed copy", "Neither mode supports live data", "This requires a different tool entirely"],
      correct: 1,
      explanation: "Import mode copies data at refresh time, meaning it can become stale between refreshes. DirectQuery queries the live source directly on each interaction, better suited to a need for truly up-to-the-second data (with tradeoffs covered later in this course).",
    },
    keyTakeaway: "Import mode gives you a fast, optimized copy of the data as of the last refresh; DirectQuery reflects the live source at the cost of some performance and flexibility tradeoffs.",
    commonMistake: "Defaulting to DirectQuery for every report without a genuine need for live data, missing out on Import mode's significant performance advantages.",
  },
  {
    title: "Building your first visual",
    body: "Creating a visual in Power BI means selecting a visual type (bar chart, line chart, table) from the Visualizations pane, then dragging fields from your data onto that visual's Axis, Legend, and Values areas - the visual builds and updates live as you drag fields on or off, letting you experiment freely without needing to write any code or formulas for a basic chart. This drag-and-drop process directly mirrors the pivot table and chart concepts from the Data Analytics course - the same underlying 'pick a category, pick a metric, pick an aggregation' logic, just expressed through Power BI's specific visual interface.",
    altExplain: "Building a Power BI visual means picking a chart type, then dragging fields onto Axis/Legend/Values areas - the same 'category, metric, aggregation' logic from the Data Analytics course, in a drag-and-drop interface.",
    visual: null,
    widget: "pivot-builder",
    keyTakeaway: "Power BI's drag-and-drop visual building is the same underlying pivot-table logic from the Data Analytics course - group by a category, summarize a metric, choose an aggregation.",
    commonMistake: "Overcomplicating an early Power BI visual with too many fields at once, instead of starting simple (one category, one metric) and building complexity gradually.",
  },
  {
    title: "Tables, matrices, and cards",
    body: "A Table visual shows raw or lightly aggregated rows in a simple grid, good for detailed data a viewer might want to scan directly. A Matrix visual is Power BI's version of a pivot table, letting you group data by row and column categories with expandable hierarchies, directly matching the Data Analytics course's pivot table concept. A Card visual displays a single summarized number prominently - like total revenue - serving the same role as the KPI cards covered in the Data Analytics course, front and center where a viewer's eye naturally goes first.",
    altExplain: "A Table shows raw rows in a grid. A Matrix is Power BI's pivot table, with row/column grouping. A Card shows one prominent summary number - the same KPI card concept from Data Analytics.",
    visual: null,
    widget: "kpi-cards",
    keyTakeaway: "A Matrix visual is directly equivalent to the pivot table concept from the Data Analytics course - same underlying idea, Power BI's specific visual for it.",
    commonMistake: "Using a Table visual to show a single important summary number, when a Card visual would present that same number far more prominently and clearly.",
  },
  {
    title: "Filters & slicers",
    body: "A filter, applied at the visual, page, or entire report level, restricts what data is included, without needing to change the underlying visual's field configuration - a page-level filter for 'this year only' applies to every visual on that page at once. A slicer is a visual, interactive filter that a report viewer can click directly - selecting a region from a slicer instantly updates every other visual on the page that uses that field, without the viewer needing any Power BI skills at all. This directly extends the segmentation and dashboard interactivity concepts from the Data Analytics and Excel courses.",
    altExplain: "A filter restricts data at the visual, page, or report level. A slicer is a clickable, interactive filter a viewer can use directly - selecting a value instantly updates every connected visual, no Power BI skills required.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You want report viewers to be able to click a button to filter the entire page by region, without needing any Power BI knowledge themselves. What's the right tool?",
      options: ["A page-level filter only they can't see or use", "A slicer, giving them a clickable, visible way to filter interactively", "A Table visual", "This isn't possible in Power BI"],
      correct: 1,
      explanation: "A slicer is specifically designed as an interactive, visible filter that report viewers can click directly, instantly updating every connected visual on the page - exactly matching this self-service interactivity need.",
    },
    keyTakeaway: "A slicer gives report viewers hands-on, self-service interactivity - the same role a slicer plays in Excel's dashboards, covered earlier in this platform.",
    commonMistake: "Building a report with no slicers, forcing every filtering need to go through the report's original author instead of letting viewers explore interactively themselves.",
  },
  {
    title: "Publishing & sharing reports",
    body: "Once a report is built in Power BI Desktop, publishing sends it to the Power BI Service (the cloud-based platform at app.powerbi.com), where it can be viewed in a browser, shared with colleagues, and organized into workspaces - a shared area for a team's related reports and datasets. Sharing a report doesn't require giving someone the underlying file at all; they view it through their own browser or the Power BI mobile app, seeing the same interactive visuals and slicers without needing Power BI Desktop installed themselves.",
    altExplain: "Publishing sends a report from Power BI Desktop to the cloud-based Power BI Service, where it's organized into workspaces and shared - viewers see the same interactive report in a browser, no Power BI Desktop needed.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A colleague needs to view and interact with a published Power BI report but doesn't have Power BI Desktop installed. Can they still use it?",
      options: ["No, Power BI Desktop is required for anyone to view a report", "Yes, they can view and interact with it through a browser or the Power BI mobile app", "Only if they install a special plugin", "Reports can only be shared as static images"],
      correct: 1,
      explanation: "Once published to the Power BI Service, a report can be viewed and interacted with through a web browser or the Power BI mobile app - Power BI Desktop is only needed for building and editing, not for viewing a published report.",
    },
    keyTakeaway: "Publishing separates authoring (Power BI Desktop) from viewing (the Power BI Service, browser, or mobile app) - viewers never need the desktop application installed.",
    commonMistake: "Assuming every report viewer needs Power BI Desktop installed, when published reports are fully viewable and interactive through just a browser or the mobile app.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Connect \u2192 Model \u2192 Visualize \u2192 Publish/Share", desc: "The core Power BI workflow, in order.", example: "Import data, build relationships, add visuals, publish to the Service." },
      { syntax: "Get Data (import, connect, or live)", desc: "Different ways to bring data in - imported copies, direct queries, or a live connection.", example: "Import a spreadsheet vs. querying a live database." },
      { syntax: "Table / Matrix / Card visuals", desc: "Table shows flat rows; matrix supports row/column grouping like a pivot; card shows one summary number.", example: "A card showing 'Total Revenue: $1.2M'." },
      { syntax: "Filters pane vs. Slicer (on-canvas)", desc: "Both filter a report, but a slicer is visible and interactive right on the page.", example: "A region dropdown slicer viewers can click directly." },
      { syntax: "Publish (Desktop \u2192 Power BI Service)", desc: "Separates authoring from viewing - viewers only need a browser or mobile app.", example: "No Power BI Desktop needed just to view a published report." },
    ],
  },
];

const PBI_ENTRY_INTERMEDIATE = [
  {
    title: "Data modeling: relationships between tables",
    body: "A relationship connects two tables through a shared column - like a Sales table's ProductID matching a Products table's ID column - letting Power BI combine data from both when building a visual, without needing to manually merge them into one giant flat table first. Relationships have a direction and a cardinality (one-to-many is the most common, where one product can appear in many sales rows), and Power BI usually detects and creates these automatically when it recognizes matching column names, though verifying them in Model view remains a worthwhile habit rather than blind trust.",
    altExplain: "A relationship connects two tables through a shared column, letting Power BI combine their data without manually merging them first. Power BI often detects relationships automatically, but verifying them in Model view is still worth doing.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A Sales table has a ProductID column, and a separate Products table has an ID column with matching values. What connects these two tables in Power BI's data model?",
      options: ["Nothing, they must be manually merged into one table first", "A relationship between the two matching columns", "A slicer", "A calculated column only"], correct: 1,
      explanation: "A relationship links the two tables through their matching columns (ProductID and ID), letting Power BI combine data from both tables in visuals without requiring a manual merge into a single flat table.",
    },
    keyTakeaway: "A relationship lets Power BI combine data across tables without manual merging - but it's still worth verifying in Model view rather than assuming auto-detection always got it exactly right.",
    commonMistake: "Manually merging related tables into one flat table (as you might in Excel with VLOOKUP) instead of using Power BI's own relationship modeling, which handles this more efficiently at scale.",
  },
  {
    title: "Star schema vs flat tables",
    body: "A star schema organizes data into a central fact table (transactional data, like individual sales) surrounded by dimension tables (descriptive data, like products, customers, and dates), connected by relationships - resembling a star shape when diagrammed. This is generally preferable to one giant flat table combining everything, since it avoids repeating descriptive data (a product's name repeated on every single sale row) and lets Power BI's engine calculate more efficiently, especially as data volume grows. This mirrors the normalization concepts from the SQL course, applied specifically to how Power BI's own data model performs best.",
    altExplain: "A star schema has a central fact table (transactions) surrounded by dimension tables (descriptive data like products or dates), connected by relationships - more efficient than one giant flat table, echoing SQL's normalization concepts.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A dataset has one giant flat table repeating full product names, categories, and customer details on every single sales row. What issue does a star schema (fact + dimension tables) solve here?",
      options: ["Nothing, flat tables always perform better", "It avoids repeating descriptive data on every row and calculates more efficiently at scale", "Star schemas only work with SQL databases", "It removes the need for any relationships at all"],
      correct: 1,
      explanation: "A star schema separates the repeated descriptive data (products, customers) into their own dimension tables, connected via relationships to the fact table - avoiding redundant repetition and letting Power BI's engine perform more efficiently, especially as data grows.",
    },
    keyTakeaway: "A star schema (fact table + dimension tables) generally performs and scales better than one giant flat table, mirroring the same normalization principles from the SQL course.",
    commonMistake: "Building Power BI reports on one large, flattened table out of convenience, missing the performance and clarity benefits a properly structured star schema provides.",
  },
  {
    title: "Introduction to DAX: measures vs. columns",
    body: "DAX (Data Analysis Expressions) is Power BI's formula language, and understanding the difference between a calculated column and a measure is foundational. A calculated column computes a value for every row, stored in the table (like a column combining first and last name), while a measure computes a value dynamically based on whatever's currently being viewed - filtered by a slicer, grouped in a visual - recalculating on the fly rather than storing a fixed value per row. Most DAX formulas used in actual reports are measures, since they respond correctly to whatever filtering or grouping the report viewer is currently applying.",
    altExplain: "DAX is Power BI's formula language. A calculated column computes a fixed value per row, stored in the table. A measure computes dynamically based on current filtering/grouping - most real report formulas are measures, not columns.",
    visual: null,
    dax: "SUM(Sales[Amount])",
    refBox: {
      syntax: "Calculated column: fixed value per row. Measure: DAX formula, recalculates dynamically.",
      desc: "Most real report formulas should be measures - they respond correctly to filters and slicers.",
      example: "Total Sales = SUM(Sales[Amount])  // a measure",
    },
    keyTakeaway: "A measure recalculates based on current filters and grouping; a calculated column stores one fixed value per row - most report calculations should be measures, not columns.",
    commonMistake: "Building a calculated column when a measure was actually needed, producing a value that doesn't correctly respond to a viewer's slicer selections or visual groupings.",
  },
  {
    title: "Basic DAX functions: SUM, AVERAGE, COUNTROWS",
    body: "SUM(Table[Column]) totals a numeric column, exactly like Excel's SUM but written with DAX's Table[Column] syntax instead of a cell range. AVERAGE(Table[Column]) computes the mean the same way. COUNTROWS(Table) counts the number of rows in a table (or a filtered version of it), rather than counting values in a specific column. These three functions, combined with the CALCULATE function covered next, form the foundation the vast majority of real-world Power BI measures build on top of.",
    altExplain: "SUM and AVERAGE work like their Excel equivalents, but reference Table[Column] instead of a cell range. COUNTROWS counts a table's rows rather than values in one column - these are DAX's most foundational functions.",
    visual: null,
    dax: "AVERAGE(Sales[Amount])",
    keyTakeaway: "DAX's Table[Column] syntax replaces Excel's cell-range references - the same underlying aggregation logic, adapted to work across a full data model instead of individual cells.",
    commonMistake: "Trying to reference a DAX column the way you would an Excel cell (like A1) instead of the correct Table[Column] syntax DAX requires.",
    challenge: {
      prompt: "Try COUNTROWS(Sales) in the DAX practice tool above to count the total number of sales rows.",
      hint: "Type exactly: COUNTROWS(Sales)",
      solution: "COUNTROWS(Sales)",
    },
  },
  {
    title: "Sorting, grouping, and hierarchies",
    body: "Visuals can be sorted by any field, not just the one currently displayed - sorting a bar chart of products by total sales descending, even though 'total sales' isn't the axis label itself. A hierarchy lets a viewer drill from a broader category down into more specific detail within the same visual - like Year down to Quarter down to Month - without needing three separate charts, simply by clicking a drill-down arrow that Power BI adds automatically when it detects a natural hierarchy like a date field.",
    altExplain: "Visuals can sort by any field, not just what's displayed. A hierarchy lets a viewer drill from broad to specific (Year to Quarter to Month) within one visual, using a drill-down arrow, instead of needing separate charts for each level.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A bar chart shows products on the axis, but you want them sorted by total sales amount, not alphabetically. What's the right approach?",
      options: ["This requires rebuilding the chart from scratch", "Sort the visual by the sales amount field, even though products are the displayed axis", "It's not possible to sort by a different field than what's displayed", "Switch to a completely different visual type"],
      correct: 1,
      explanation: "Power BI visuals can be sorted by any available field, not just the one currently shown on the axis - sorting the product bar chart by total sales amount (a different field) reorders the bars by that value directly.",
    },
    keyTakeaway: "A visual can be sorted by a field other than the one it displays - useful for ordering categories by a meaningful metric rather than defaulting to alphabetical order.",
    commonMistake: "Assuming a chart can only be sorted by whatever field is currently on its axis, missing the option to sort by a more meaningful related metric instead.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the concepts and DAX introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "One-to-many relationship (linked by a key)", desc: "Connects tables so a filter on one flows through to related tables automatically.", example: "One Product links to many Sales rows." },
      { syntax: "Star schema: fact table + dimension tables", desc: "A central fact table (transactions) surrounded by dimension tables (lookups) - the standard BI model shape.", example: "Sales (fact) connects to Product, Date, Customer (dimensions)." },
      { syntax: "Calculated column (fixed) vs Measure (dynamic)", desc: "Most real report formulas should be measures - they respond correctly to filters.", example: "Total Sales = SUM(Sales[Amount])" },
      { syntax: "SUM(Table[Column]) / AVERAGE(Table[Column]) / COUNTROWS(Table)", desc: "DAX's foundational aggregation functions - Table[Column] replaces Excel's cell ranges.", example: "AVERAGE(Sales[Amount])" },
      { syntax: "Sort visual by any field, not just the displayed axis", desc: "A chart can be sorted by a meaningful metric different from what it shows.", example: "Sort products by total sales, not alphabetically." },
    ],
  },
];

const PBI_ENTRY_ADVANCED = [
  {
    title: "CALCULATE & filter context",
    body: "CALCULATE is arguably the single most important function in DAX: it evaluates an expression within a modified filter context, letting you compute something under a specific condition different from whatever's currently being viewed. CALCULATE(SUM(Sales[Amount]), Sales[Region]=\"North\") calculates total sales specifically for the North region, regardless of what other filters or slicers happen to be active elsewhere in the report - this ability to deliberately override or add to the current filter context is what makes comparisons like 'this region vs. everywhere else' or 'this year vs. last year' possible in DAX at all.",
    altExplain: "CALCULATE evaluates an expression under a modified filter context - like forcing a calculation to always consider only the North region, regardless of what else is currently filtered. It's the function that makes most advanced DAX comparisons possible.",
    visual: null,
    dax: "CALCULATE(SUM(Sales[Amount]), Sales[Region]=\"North\")",
    refBox: {
      syntax: "CALCULATE(expression, filter1, filter2, ...)",
      desc: "Evaluates an expression under a modified filter context - the foundation of most advanced DAX comparisons.",
      example: "CALCULATE(SUM(Sales[Amount]), Sales[Region]=\"North\")",
    },
    keyTakeaway: "CALCULATE lets a measure deliberately override the current filter context - the foundational mechanism behind almost every advanced DAX comparison or time-intelligence calculation.",
    commonMistake: "Trying to build a 'this vs. that' comparison measure without CALCULATE, when CALCULATE's filter-context override is specifically what makes that kind of comparison possible in DAX.",
    challenge: {
      prompt: "Modify the formula to calculate total sales specifically for Widget A.",
      hint: "CALCULATE(SUM(Sales[Amount]), Sales[Product]=\"Widget A\")",
      solution: "CALCULATE(SUM(Sales[Amount]), Sales[Product]=\"Widget A\")",
    },
  },
  {
    title: "Time intelligence basics",
    body: "Time intelligence functions in DAX (like TOTALYTD, SAMEPERIODLASTYEAR) handle common date-based comparisons - year-to-date totals, comparing this period to the same period last year - that would otherwise require complex manual date filtering. These functions require a properly marked 'Date' table in the data model (a dedicated table with one row per calendar date), since Power BI needs to understand your calendar structure to calculate things like fiscal year boundaries or month-over-month comparisons correctly.",
    altExplain: "Time intelligence functions (TOTALYTD, SAMEPERIODLASTYEAR) handle common date comparisons automatically, but require a proper Date table in your model so Power BI understands your calendar structure correctly.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A report needs a 'year-to-date sales' measure, but time intelligence functions are returning incorrect or blank results. What's a likely missing piece?",
      options: ["The report needs more slicers", "A properly configured Date table in the data model", "DAX doesn't support time intelligence at all", "The visuals need to be resized"],
      correct: 1,
      explanation: "Time intelligence functions depend on a properly marked Date table to understand the calendar structure - without one, functions like TOTALYTD often can't calculate correctly, since Power BI has no reliable way to know what a 'year' or 'period' means in the data.",
    },
    keyTakeaway: "Time intelligence functions require a properly configured Date table - this is one of the most common early stumbling blocks when these functions don't seem to work as expected.",
    commonMistake: "Attempting to use time intelligence functions without first setting up a proper Date table, leading to confusing incorrect or blank results with no obvious cause.",
  },
  {
    title: "Conditional formatting in Power BI",
    body: "Conditional formatting in Power BI, like its Excel counterpart, changes a visual's color or icons based on a value's magnitude - a table column shaded green for high performers and red for low performers, or data bars showing relative size directly within a table cell. This applies purely visual emphasis without altering the underlying value, exactly the same principle from the Excel course - the display changes, the actual number behind it never does, so any measure or calculation referencing that value still works with the true, unformatted number.",
    altExplain: "Power BI's conditional formatting, like Excel's, changes visual appearance (color, data bars) based on a value's magnitude, without changing the actual underlying number - purely visual, same principle as the Excel course.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A table applies conditional formatting to color cells red when sales are below target. Does this change the actual sales value stored for that cell?",
      options: ["Yes, it rounds the value down", "No, it's purely visual - the underlying value used in calculations stays the same", "Yes, it converts the number to text", "Conditional formatting isn't available in Power BI"],
      correct: 1,
      explanation: "Just like in Excel, Power BI's conditional formatting only changes visual appearance based on a rule - the actual stored, calculated value behind that cell remains completely unchanged.",
    },
    keyTakeaway: "Power BI's conditional formatting is purely visual, exactly like Excel's - never confuse a formatted display with an actual change to the underlying calculated value.",
    commonMistake: "Assuming conditional formatting has somehow altered the underlying data, when it only ever affects visual presentation.",
  },
  {
    title: "Bookmarks & drill-through",
    body: "A bookmark captures the current state of a report page - which filters are applied, which visual is selected - letting you return to that exact configuration later with one click, or build a guided, presentation-style walkthrough by chaining bookmarks together, each showing a different pre-configured view. Drill-through takes a viewer from a summary visual directly to a dedicated detail page, filtered automatically to whatever they clicked on - selecting a specific product on a summary chart and drilling through to see everything about just that product, without manually reconfiguring filters themselves.",
    altExplain: "A bookmark saves a report page's exact current state (filters, selections) for one-click return, useful for guided walkthroughs. Drill-through takes a viewer from a summary to an auto-filtered detail page based on what they clicked.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A viewer clicks on a specific product in a summary chart and is automatically taken to a detail page already filtered to just that product. What Power BI feature enables this?",
      options: ["A bookmark", "Drill-through", "Conditional formatting", "A slicer"],
      correct: 1,
      explanation: "Drill-through is specifically designed to take a viewer from a summary visual to a dedicated detail page, automatically filtered based on whatever was clicked - exactly this behavior.",
    },
    keyTakeaway: "Bookmarks save and restore a report's exact state for guided presentations; drill-through automatically filters a detail page based on what a viewer clicked on a summary visual.",
    commonMistake: "Confusing bookmarks (saved states for presentation) with drill-through (automatic contextual navigation to detail) - they solve genuinely different navigation needs.",
  },
  {
    title: "Row-level security basics",
    body: "Row-level security (RLS) restricts which rows of data a specific user can see when they view a shared report, based on their identity - a regional sales manager might see only their own region's data, while an executive sees everything, all from the exact same underlying published report, with no need to build and maintain separate reports per audience. RLS is configured through roles defined in Power BI Desktop, each with a DAX filter expression determining which rows that role can see, then assigned to specific users or groups once the report is published.",
    altExplain: "Row-level security restricts which data rows a specific user sees in a shared report, based on their identity - one report can serve many audiences correctly, instead of needing separate reports built for each.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A company wants each regional manager to see only their own region's data when viewing a shared sales report, without maintaining separate reports per region. What feature solves this?",
      options: ["Bookmarks", "Row-level security (RLS)", "Conditional formatting", "Drill-through"],
      correct: 1,
      explanation: "Row-level security is specifically designed to restrict which rows a user sees based on their identity, letting one single published report correctly serve many different audiences without duplicating it per region.",
    },
    keyTakeaway: "Row-level security lets one report correctly serve many audiences, each seeing only their own relevant data, without maintaining a separate copy of the report per audience.",
    commonMistake: "Building and maintaining separate report copies for each audience segment, when row-level security would let one single report correctly serve everyone.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the concepts and DAX introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "CALCULATE(expression, filter1, filter2, ...)", desc: "Evaluates an expression under a modified filter context - the foundation of most advanced DAX.", example: "CALCULATE(SUM(Sales[Amount]), Sales[Region]=\"North\")" },
      { syntax: "TOTALYTD(expression, dates) / SAMEPERIODLASTYEAR(dates)", desc: "Time intelligence functions for date-based comparisons - need a proper Date table.", example: "TOTALYTD(SUM(Sales[Amount]), 'Date'[Date])" },
      { syntax: "Format > Conditional formatting (rules or field-based)", desc: "Automatically colors/styles visual elements based on their value.", example: "Color cells red below target, green above." },
      { syntax: "Bookmarks + drill-through", desc: "Bookmarks save a report state to jump back to; drill-through jumps to a detail page for one item.", example: "Click a region, drill through to that region's detail page." },
      { syntax: "Row-level security (RLS) roles", desc: "Restricts which rows a user sees based on their identity - one report serves many audiences.", example: "A manager sees only their region's rows automatically." },
    ],
  },
];

const PBI_QUIZ_BANK = {
  basic: [
    { q: "Power BI's typical workflow moves through which three stages?", options: ["Design, Build, Test", "Power Query, Data Model, Reports", "Import, Export, Archive", "Filter, Sort, Publish"], correct: 1 },
    { q: "Model view is primarily used to...", options: ["Write DAX formulas only", "See and manage relationships between tables", "Publish a report", "Format visuals with color"], correct: 1 },
    { q: "DirectQuery differs from Import mode by...", options: ["Being always slower with no benefit", "Querying the live data source directly instead of a periodically refreshed copy", "Only working with Excel files", "Requiring no data model"], correct: 1 },
    { q: "Building a Power BI visual mainly involves...", options: ["Writing SQL queries", "Dragging fields onto a chosen visual's Axis/Legend/Values areas", "Manually coding each chart", "Only using pre-built templates with no customization"], correct: 1 },
    { q: "A Card visual is best used for...", options: ["Detailed row-level data", "A single prominent summary number", "Complex multi-table relationships", "Time intelligence formulas"], correct: 1 },
  ],
  intermediate: [
    { q: "A relationship in Power BI's data model connects...", options: ["Two visuals directly", "Two tables through a shared matching column", "A report to a workspace", "A slicer to a bookmark"], correct: 1 },
    { q: "A star schema is generally preferred over one giant flat table because it...", options: ["Always uses less total data", "Avoids repeating descriptive data and calculates more efficiently at scale", "Removes the need for any relationships", "Only works with small datasets"], correct: 1 },
    { q: "A DAX measure, compared to a calculated column, ...", options: ["Stores one fixed value per row", "Recalculates dynamically based on current filters and grouping", "Cannot use aggregate functions", "Is always slower to compute"], correct: 1 },
    { q: "COUNTROWS(Table) returns...", options: ["The sum of a column", "The number of rows in the table", "The average of a column", "A list of column names"], correct: 1 },
    { q: "A hierarchy in a visual lets a viewer...", options: ["Delete data permanently", "Drill from a broad category down into more specific detail", "Change the report's color scheme", "Publish the report"], correct: 1 },
  ],
  advanced: [
    { q: "CALCULATE's core purpose is to...", options: ["Format numbers with currency symbols", "Evaluate an expression within a modified filter context", "Connect to a new data source", "Create a new table"], correct: 1 },
    { q: "Time intelligence functions generally require...", options: ["No special setup at all", "A properly configured Date table in the data model", "Row-level security to be enabled", "DirectQuery mode specifically"], correct: 1 },
    { q: "Conditional formatting in Power BI...", options: ["Changes the actual underlying stored value", "Only changes visual appearance, not the underlying value", "Only works on Card visuals", "Requires DAX to configure"], correct: 1 },
    { q: "Drill-through specifically enables...", options: ["Saving a report's current filter state", "Automatic navigation to a detail page filtered by what was clicked", "Restricting data by user identity", "Changing a visual's chart type"], correct: 1 },
    { q: "Row-level security (RLS) is used to...", options: ["Speed up report loading", "Restrict which data rows a specific user sees based on their identity", "Format numbers as percentages", "Create bookmarks automatically"], correct: 1 },
  ],
};
