// =========================================================================
// JavaScript - Entry Level
// =========================================================================
const JS_ENTRY_BASIC = [
  {
    title: "What is JavaScript?",
    body: "JavaScript is the programming language that makes web pages interactive - it's what runs when a page validates a form, updates content without reloading, or responds to a click. Unlike SQL (which asks a database questions) or the compiled languages you may have seen elsewhere, JavaScript is interpreted and dynamically-typed: your code runs directly, line by line, without a separate compile step, and a variable's type is whatever value it currently holds. What makes JavaScript unusual is that it runs in two very different places - inside a web browser (where it can touch the page itself) and, via Node.js, directly on a server or a computer outside any browser. This course runs your code in a lightweight, sandboxed JavaScript engine, so everything you write here is genuinely executed, not just illustrated - the same language, just without direct access to an actual page.",
    altExplain: "JavaScript is the language that makes websites interactive. It runs directly without a separate compile step, and variables aren't locked to one fixed type. This course runs real JavaScript, just without a browser page attached to it.",
    visual: null,
    keyTakeaway: "JavaScript is interpreted and dynamically-typed - it runs directly, and a variable's type can change as the program runs.",
    commonMistake: "Assuming 'JavaScript' and 'Java' are related because of the name - they're almost entirely unrelated languages that happened to launch around the same time.",
  },
  {
    title: "Your first script",
    body: "console.log() is how a JavaScript program prints output - whatever you place inside the parentheses gets written out, whether it's text, a number, or the result of a calculation. Statements typically end with a semicolon, though JavaScript will often insert one automatically if you forget it - a convenience that's genuinely useful day to day, but worth knowing about so a missing semicolon doesn't surprise you later in edge cases. Comments start with // for a single line, or are wrapped in /* */ for a block spanning several lines, and are completely ignored when the code runs - they exist purely to explain your reasoning to the next person reading the code, who might well be you in six months.",
    altExplain: "console.log() prints something out. Statements usually end with a semicolon. // starts a single-line comment; /* ... */ wraps a multi-line one.",
    visual: null,
    jscode: "// This is a comment\nconsole.log(\"Hello, world!\");",
    keyTakeaway: "console.log() is your primary window into what a JavaScript program is actually doing - lean on it constantly while learning.",
    commonMistake: "Forgetting that console.log() only prints - it doesn't return a value you can use elsewhere in your code the way a function like Math.max() does.",
    challenge: {
      prompt: "Print your name, then print a second line saying what you're learning.",
      hint: "Two separate console.log() calls, one per line.",
      solution: "console.log(\"Your Name\");\nconsole.log(\"Learning JavaScript!\");",
    },
  },
  {
    title: "Variables: let, const, and var",
    body: "JavaScript gives you three ways to declare a variable, and which one you reach for actually matters. let creates a variable that can be reassigned later - use it for anything that's expected to change, like a running total or a counter. const creates a variable that can never be reassigned after its initial value is set, and modern JavaScript style strongly favors const by default, switching to let only when you specifically know a value needs to change. var is the original way to declare variables, predating let and const, but it has looser, more error-prone scoping rules that both of the newer keywords were introduced specifically to fix - you'll still see var in older code, but there's rarely a good reason to write new code with it today.",
    altExplain: "let = can be reassigned later. const = locked in, can't be reassigned. var = the old way, mostly avoided in new code because of confusing scoping rules.",
    visual: null,
    jscode: "let age = 25;\nconst name = \"Ana\";\n\nage = 26; // fine, let can be reassigned\nconsole.log(name, age);",
    refBox: {
      syntax: "let x = 5; / const y = 10;",
      desc: "let declares a variable you can reassign later; const declares one you can't. Default to const.",
      example: "let count = 0;\ncount = count + 1; // fine\n\nconst max = 100;\nmax = 200; // TypeError - can't reassign a const",
    },
    keyTakeaway: "Default to const; only switch to let when a value genuinely needs to be reassigned later in the program.",
    commonMistake: "Trying to reassign a const variable - JavaScript will throw a real error (TypeError: Assignment to constant variable) rather than silently allowing it.",
    challenge: {
      prompt: "Declare a const for a city name and a let for a temperature, then print both.",
      hint: "const city = \"...\"; let temperature = ...; then two console.log() calls.",
      solution: "const city = \"Manila\";\nlet temperature = 31;\nconsole.log(city);\nconsole.log(temperature);",
    },
  },
  {
    title: "Data types & typeof",
    body: "JavaScript has a handful of primitive types you'll use constantly: number (there's no separate int/float split - 5 and 5.5 are both just numbers), string (text, wrapped in quotes), boolean (true or false), undefined (a variable that's been declared but never given a value), and null (a value deliberately set to represent 'nothing here'). The typeof operator tells you a value's current type, which is genuinely useful while learning and while debugging - typeof 5 returns \"number\", typeof \"hi\" returns \"string\", and so on. One well-known quirk worth knowing early: typeof null actually returns \"object\", a long-standing historical bug in the language that's now permanently baked in for backward compatibility - it's not a mistake you're making, it's just how the language behaves.",
    altExplain: "number, string, boolean, undefined, and null are the basic types. typeof tells you a value's type - except typeof null oddly returns \"object\", a known quirk of the language.",
    visual: null,
    jscode: "let price = 19.99;\nlet name = \"Widget\";\nlet inStock = true;\n\nconsole.log(typeof price);\nconsole.log(typeof name);\nconsole.log(typeof inStock);",
    keyTakeaway: "typeof is a quick way to check what kind of value you're actually working with - use it liberally while debugging.",
    commonMistake: "Expecting typeof null to return \"null\" - it returns \"object\" instead, a famous historical quirk of the language.",
    challenge: {
      prompt: "Create a variable holding a number and print its type using typeof.",
      hint: "let quantity = 10; then console.log(typeof quantity);",
      solution: "let quantity = 10;\nconsole.log(typeof quantity);",
    },
  },
  {
    title: "Operators & expressions",
    body: "JavaScript supports the standard arithmetic operators (+, -, *, /, and % for remainder), along with ** for exponentiation, similar to Python. A distinctive quirk to know early: the + operator also performs string concatenation, and when one side of a + is a string, JavaScript converts the other side to a string too rather than throwing an error - \"5\" + 3 produces \"53\", not 8. This automatic type conversion, called coercion, is convenient in some cases and a genuine source of bugs in others, which is exactly why the next lesson's === matters so much. Comparison operators (==, ===, >, <) and logical operators (&&, ||, !) round out the basics you'll use in almost every program you write.",
    altExplain: "+, -, *, /, % work as expected, plus ** for exponents. + also joins strings together - \"5\" + 3 gives \"53\", not 8, because JavaScript converts the number to a string first.",
    visual: null,
    jscode: "let a = 7;\nlet b = 2;\nconsole.log(a / b);   // 3.5\nconsole.log(a % b);   // 1 (remainder)\nconsole.log(a ** b);  // 49\nconsole.log(\"5\" + 3); // \"53\" - string concatenation, not addition",
    keyTakeaway: "+ concatenates when either side is a string - this automatic coercion is one of the most common early sources of confusing bugs.",
    commonMistake: "Expecting \"5\" + 3 to add up to 8 - since one operand is a string, JavaScript concatenates instead of adding, producing \"53\".",
    challenge: {
      prompt: "Print the remainder of 17 divided by 5.",
      hint: "Use the % operator.",
      solution: "console.log(17 % 5);",
    },
  },
  {
    title: "Strings & template literals",
    body: "Template literals - strings wrapped in backticks (`) instead of quotes - let you embed variables and expressions directly inside text using ${...}, which is far more readable than manually joining pieces with +. They also preserve line breaks exactly as you type them, unlike regular quoted strings. Strings come with useful built-in methods too: .toUpperCase() and .toLowerCase() change casing, .trim() removes leading and trailing whitespace, and .split() breaks a string into an array of pieces based on a separator - each of these is called directly on the string with dot notation and returns a new string or array, leaving the original untouched, since strings in JavaScript are immutable.",
    altExplain: "Template literals use backticks and ${variable} to embed values into text, instead of joining pieces with +. Strings also have methods like .toUpperCase(), .trim(), and .split().",
    visual: null,
    jscode: "let name = \"Ana\";\nlet age = 28;\nconsole.log(`${name} is ${age} years old.`);\n\nlet messy = \"  Hello World  \";\nconsole.log(messy.trim().toUpperCase());",
    keyTakeaway: "Template literals (backticks + ${...}) are the modern, readable way to build strings out of variables - prefer them over manual + concatenation.",
    commonMistake: "Using regular quotes with ${...} inside them, expecting interpolation - that syntax only works inside backtick-delimited template literals.",
    challenge: {
      prompt: "Create a product name and price, then print a sentence using a template literal combining both.",
      hint: "const product = \"Widget\"; const price = 9.99; then a template literal with ${product} and ${price}.",
      solution: "const product = \"Widget\";\nconst price = 9.99;\nconsole.log(`${product} costs ${price}`);",
    },
  },
  {
    title: "Conditionals: if, else if, else",
    body: "JavaScript's if statement uses curly braces {} to mark which lines belong inside a block, similar to C# rather than Python's indentation-based approach. else if chains additional conditions to check in order, and else catches anything that didn't match a prior condition. A crucial related concept is the difference between == and ===: == compares values after converting them to a common type if needed (so \"5\" == 5 is true), while === compares both value and type with no conversion at all (so \"5\" === 5 is false). Because of the coercion quirks from a few lessons ago, using === is almost always the safer, more predictable choice, and experienced JavaScript developers reach for it by default.",
    altExplain: "if/else if/else works like other curly-brace languages. == compares loosely (converts types if needed); === compares strictly (no conversion). Prefer === - it avoids coercion surprises.",
    visual: null,
    jscode: "let score = 85;\nif (score >= 90) {\n  console.log(\"A\");\n} else if (score >= 80) {\n  console.log(\"B\");\n} else {\n  console.log(\"C or below\");\n}",
    refBox: {
      syntax: "if (cond) { ... } else if (cond) { ... } else { ... }",
      desc: "Runs the first matching block; else at the end catches everything that didn't match.",
      example: "if (temp > 30) {\n  console.log(\"hot\");\n} else if (temp > 15) {\n  console.log(\"mild\");\n} else {\n  console.log(\"cold\");\n}",
    },
    keyTakeaway: "Prefer === over == - strict equality avoids the surprising type coercion that == performs behind the scenes.",
    commonMistake: "Using == out of habit and getting an unexpected true from comparisons like \"\" == 0 or \"5\" == 5 - === avoids this entirely.",
    challenge: {
      prompt: "Write an if/else that prints \"Weekend\" if a day variable equals \"Saturday\" or \"Sunday\", otherwise prints \"Weekday\".",
      hint: "Use || to check for either Saturday or Sunday in one condition, and === for each comparison.",
      solution: "let day = \"Saturday\";\nif (day === \"Saturday\" || day === \"Sunday\") {\n  console.log(\"Weekend\");\n} else {\n  console.log(\"Weekday\");\n}",
    },
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "console.log(value)", desc: "Prints a value to the output.", example: "console.log(\"Hello\");" },
      { syntax: "let x = 5; / const y = 10;", desc: "let can be reassigned later; const cannot.", example: "let count = 0;\nconst max = 100;" },
      { syntax: "typeof value", desc: "Returns a value's type as a string.", example: "typeof 5; // \"number\"" },
      { syntax: "+ - * / % **", desc: "Arithmetic operators; % is remainder, ** is exponent.", example: "17 % 5; // 2" },
      { syntax: "`text ${expr} text`", desc: "Template literal - embeds a variable/expression into a string.", example: "`Hi, ${name}!`" },
      { syntax: "if (cond) { } else if (cond) { } else { }", desc: "Runs the first matching block.", example: "if (x > 0) { ... } else { ... }" },
      { syntax: "=== / !==", desc: "Strict equality/inequality - no type conversion, unlike == and !=.", example: "\"5\" === 5; // false" },
    ],
  },
];

const JS_ENTRY_INTERMEDIATE = [
  {
    title: "Arrays",
    body: "An array is an ordered list of values, created with square brackets: const fruits = [\"apple\", \"banana\", \"cherry\"]. Each item has a numeric index starting at 0, so fruits[0] is \"apple\" and fruits[2] is \"cherry\" - trying to access an index that doesn't exist returns undefined rather than throwing an error, which is worth knowing since it can silently hide a bug. The .length property tells you how many items an array holds, and arrays can hold a mix of any type, including other arrays, though in practice keeping an array's contents consistent (all numbers, all strings) makes code far easier to reason about. Even though fruits is declared with const, you can still change its contents (push, remove, reassign an index) - const only locks the variable from being reassigned to point at a completely different array.",
    altExplain: "Arrays are ordered lists, written with square brackets. Indexes start at 0. .length tells you how many items there are. const still lets you change an array's contents - it only stops you from reassigning the variable entirely.",
    visual: null,
    jscode: "const fruits = [\"apple\", \"banana\", \"cherry\"];\nconsole.log(fruits[0]);\nconsole.log(fruits.length);\nfruits.push(\"date\");\nconsole.log(fruits);",
    keyTakeaway: "const on an array locks the variable, not the contents - .push(), .pop(), and index reassignment still work fine.",
    commonMistake: "Assuming an out-of-range index like fruits[10] throws an error - it silently returns undefined instead.",
    challenge: {
      prompt: "Create an array of three numbers, then print the sum of the first and last item.",
      hint: "const nums = [4, 10, 6]; then console.log(nums[0] + nums[2]);",
      solution: "const nums = [4, 10, 6];\nconsole.log(nums[0] + nums[nums.length - 1]);",
    },
  },
  {
    title: "Loops: for, while, and for...of",
    body: "A classic for loop gives you full control with three parts - an initializer, a condition, and an update step - written as for (let i = 0; i < 5; i++) { ... }, which runs the block once for each value of i from 0 up to (not including) 5. A while loop instead runs as long as a condition stays true, and is a better fit when you don't know in advance exactly how many iterations you'll need. For working with arrays specifically, for...of is usually the cleanest option: for (const item of fruits) { ... } hands you each element directly, without the bookkeeping of tracking an index yourself. All three loops support break (exit the loop immediately) and continue (skip to the next iteration).",
    altExplain: "A for loop runs a set number of times using a counter. A while loop runs as long as a condition is true. for...of loops directly over an array's items, without needing an index. break exits early, continue skips ahead.",
    visual: null,
    jscode: "const fruits = [\"apple\", \"banana\", \"cherry\"];\n\nfor (const fruit of fruits) {\n  console.log(fruit);\n}\n\nlet i = 0;\nwhile (i < 3) {\n  console.log(\"count:\", i);\n  i++;\n}",
    refBox: {
      syntax: "for (const item of array) { ... }",
      desc: "Loops directly over an array's values - no index bookkeeping needed.",
      example: "for (const fruit of [\"apple\", \"banana\"]) {\n  console.log(fruit);\n}",
    },
    keyTakeaway: "for...of is the cleanest way to loop over an array's values when you don't specifically need the index too.",
    commonMistake: "Forgetting to update the loop's counter (like i++) inside a while loop, which causes an infinite loop that never terminates.",
    challenge: {
      prompt: "Loop over an array of numbers [1, 2, 3, 4] and print only the even ones.",
      hint: "Use for...of and check num % 2 === 0 inside an if.",
      solution: "const nums = [1, 2, 3, 4];\nfor (const num of nums) {\n  if (num % 2 === 0) {\n    console.log(num);\n  }\n}",
    },
  },
  {
    title: "Objects",
    body: "An object groups related values together under named keys, written with curly braces: const person = { name: \"Ana\", age: 28 }. Unlike an array's numeric indexes, you access an object's values by key, either with dot notation (person.name) or bracket notation (person[\"name\"]) - dot notation is more common and readable, while bracket notation is necessary when the key is stored in a variable or contains characters that aren't valid in dot notation. Objects can hold any type of value as a property, including arrays and other objects, which is how JavaScript naturally represents more complex, structured real-world data - a person with a name, an age, and a list of hobbies, for instance.",
    altExplain: "Objects group related values under named keys, using curly braces. person.name (dot notation) or person[\"name\"] (bracket notation) both read a value - dot notation is the common choice.",
    visual: null,
    jscode: "const person = {\n  name: \"Ana\",\n  age: 28,\n  hobbies: [\"reading\", \"chess\"],\n};\n\nconsole.log(person.name);\nconsole.log(person[\"age\"]);\nperson.age = 29;\nconsole.log(person);",
    keyTakeaway: "Dot notation (person.name) is the everyday way to access an object's properties; bracket notation is mainly for dynamic or variable keys.",
    commonMistake: "Using bracket notation without quotes for a literal key, like person[age] instead of person[\"age\"] - that tries to look up a variable named age, not the string \"age\".",
    challenge: {
      prompt: "Create an object for a book with title and pages properties, then print a sentence combining both using a template literal.",
      hint: "const book = { title: \"...\", pages: ... }; then a template literal using book.title and book.pages.",
      solution: "const book = { title: \"Dune\", pages: 412 };\nconsole.log(`${book.title} has ${book.pages} pages.`);",
    },
  },
  {
    title: "Functions & arrow functions",
    body: "A function packages up a reusable block of logic. The traditional form is function greet(name) { return `Hello, ${name}!`; }, and calling greet(\"Ana\") runs the block with name set to \"Ana\", producing whatever comes after return. Arrow functions are a more compact, modern syntax for the same idea: const greet = (name) => `Hello, ${name}!`; - when the body is a single expression, the return and curly braces can even be dropped entirely, since the expression's value is returned automatically. Both forms are genuinely common in real JavaScript code; arrow functions are especially popular for short, inline functions, which you'll see constantly once array methods like map and filter enter the picture in the next lesson.",
    altExplain: "A function is reusable logic you can call by name. function greet(name) { return ...; } is the traditional form. const greet = (name) => ...; is the shorter arrow-function form, common for short functions.",
    visual: null,
    jscode: "function greetFormal(name) {\n  return `Hello, ${name}!`;\n}\n\nconst greetArrow = (name) => `Hi, ${name}!`;\n\nconsole.log(greetFormal(\"Ana\"));\nconsole.log(greetArrow(\"Ben\"));",
    refBox: {
      syntax: "const fn = (param) => expression;",
      desc: "Arrow function shorthand - a single expression body returns automatically, no braces or return needed.",
      example: "const square = (n) => n * n;\nsquare(5); // 25",
    },
    keyTakeaway: "An arrow function with a single expression body automatically returns that expression's value - no explicit return keyword needed.",
    commonMistake: "Wrapping a single-expression arrow function body in curly braces but forgetting the return keyword - {} switches to a full function body, where return is required, not automatic.",
    challenge: {
      prompt: "Write an arrow function called square that takes a number and returns its square, then call it with 6 and print the result.",
      hint: "const square = (n) => n * n; then console.log(square(6));",
      solution: "const square = (n) => n * n;\nconsole.log(square(6));",
    },
  },
  {
    title: "Array methods: map, filter, and reduce",
    body: "These three methods are the backbone of everyday JavaScript array work, and each takes a function as an argument to describe what to do with each item. .map() transforms every item and returns a new array of the same length - [1, 2, 3].map(n => n * 2) produces [2, 4, 6]. .filter() keeps only the items where the given function returns true, producing a new, possibly shorter array - [1, 2, 3, 4].filter(n => n % 2 === 0) produces [2, 4]. .reduce() combines every item down into a single value, using an accumulator that carries forward between items - it's the most flexible of the three, and commonly used for totals: [1, 2, 3].reduce((sum, n) => sum + n, 0) produces 6. None of these methods change the original array; each returns a brand-new one.",
    altExplain: "map() transforms every item into something new. filter() keeps only items matching a condition. reduce() combines everything into one single value, like a total. None of them change the original array.",
    visual: null,
    jscode: "const nums = [1, 2, 3, 4, 5];\n\nconst doubled = nums.map(n => n * 2);\nconst evens = nums.filter(n => n % 2 === 0);\nconst total = nums.reduce((sum, n) => sum + n, 0);\n\nconsole.log(doubled);\nconsole.log(evens);\nconsole.log(total);",
    keyTakeaway: "map, filter, and reduce each return a brand-new array (or value) - none of them modify the original array in place.",
    commonMistake: "Forgetting reduce's starting value (the second argument, like 0 for a sum) - without it, reduce uses the array's first item as the starting accumulator, which can produce subtly wrong results.",
    challenge: {
      prompt: "Given [10, 15, 20, 25], filter out values below 18, then print the sum of what's left.",
      hint: "Chain .filter(n => n >= 18) then .reduce((sum, n) => sum + n, 0).",
      solution: "const nums = [10, 15, 20, 25];\nconst total = nums.filter(n => n >= 18).reduce((sum, n) => sum + n, 0);\nconsole.log(total);",
    },
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "const arr = [1, 2, 3];", desc: "Array literal - an ordered list of values.", example: "arr[0]; // 1\narr.length; // 3" },
      { syntax: "for (const item of array) { ... }", desc: "Loops directly over an array's values.", example: "for (const f of fruits) { console.log(f); }" },
      { syntax: "while (cond) { ... }", desc: "Runs as long as the condition stays true.", example: "while (i < 3) { i++; }" },
      { syntax: "const obj = { key: value };", desc: "Object literal - groups values under named keys.", example: "obj.name / obj[\"name\"]" },
      { syntax: "const fn = (param) => expr;", desc: "Arrow function shorthand - returns a single expression automatically.", example: "const square = (n) => n * n;" },
      { syntax: "array.map(fn)", desc: "Transforms every item, returns a new array of the same length.", example: "[1,2].map(n => n*2); // [2,4]" },
      { syntax: "array.filter(fn)", desc: "Keeps only items where fn returns true.", example: "[1,2,3].filter(n => n>1); // [2,3]" },
      { syntax: "array.reduce(fn, start)", desc: "Combines every item into one value, using an accumulator.", example: "[1,2,3].reduce((s,n)=>s+n, 0); // 6" },
    ],
  },
];

const JS_ENTRY_ADVANCED = [
  {
    title: "Truthy, falsy, and === vs ==",
    body: "Every value in JavaScript is either 'truthy' or 'falsy' when used somewhere a boolean is expected, like an if condition. There are exactly six falsy values: false, 0, \"\" (empty string), null, undefined, and NaN - genuinely everything else, including \"0\" (a non-empty string!) and [] (an empty array), is truthy. This matters constantly in real code: if (name) checks not just that name isn't false, but that it isn't any of the six falsy values at once, which is a concise and common pattern for 'do we have a usable value here.' Combined with the === vs == distinction from earlier, understanding truthy/falsy is what lets you read real-world JavaScript conditions confidently instead of guessing.",
    altExplain: "Only six values count as 'falsy': false, 0, \"\", null, undefined, and NaN. Everything else - including \"0\" and [] - is truthy. if (name) is a common shorthand for 'is this a usable value.'",
    visual: null,
    jscode: "const values = [0, \"\", \"0\", null, undefined, NaN, [], \"hello\"];\nfor (const v of values) {\n  console.log(v, \"->\", Boolean(v));\n}",
    keyTakeaway: "Only six values are falsy: false, 0, \"\", null, undefined, and NaN - literally everything else, including \"0\" and [], is truthy.",
    commonMistake: "Assuming an empty array [] or the string \"0\" is falsy because they look empty or zero-like - both are actually truthy.",
    challenge: {
      prompt: "Write a function isUsable that returns true if a value is truthy, false otherwise, using Boolean(). Test it on 0 and \"hi\".",
      hint: "const isUsable = (v) => Boolean(v); then call it with console.log for both test values.",
      solution: "const isUsable = (v) => Boolean(v);\nconsole.log(isUsable(0));\nconsole.log(isUsable(\"hi\"));",
    },
  },
  {
    title: "Error handling: try, catch, and throw",
    body: "When something goes wrong while a program runs - dividing by an unusable input, calling a method on a value that doesn't have it - JavaScript throws an error, which stops execution unless it's caught. Wrapping risky code in a try block lets you catch that error in a matching catch block instead of letting the whole program crash: try { ... } catch (err) { console.log(err.message); }. You can also throw your own errors deliberately with throw new Error(\"message\"), which is useful for signaling that a function received input it genuinely can't work with. A finally block, if present, always runs after try/catch, whether an error occurred or not - handy for cleanup code that must happen either way.",
    altExplain: "try { risky code } catch (err) { handle it } stops an error from crashing the whole program. throw new Error(\"...\") lets you raise your own errors on purpose. finally always runs, error or not.",
    visual: null,
    jscode: "function safeDivide(a, b) {\n  try {\n    if (b === 0) throw new Error(\"Cannot divide by zero\");\n    return a / b;\n  } catch (err) {\n    console.log(\"Error caught:\", err.message);\n    return null;\n  }\n}\n\nconsole.log(safeDivide(10, 2));\nconsole.log(safeDivide(10, 0));",
    refBox: {
      syntax: "try { ... } catch (err) { ... }",
      desc: "Runs the try block; if it throws, catch handles the error instead of crashing the program.",
      example: "try {\n  throw new Error(\"oops\");\n} catch (err) {\n  console.log(err.message);\n}",
    },
    keyTakeaway: "throw new Error(\"...\") lets a function signal it received input it can't handle - catch it with try/catch instead of letting the program crash.",
    commonMistake: "Wrapping code in try/catch but doing nothing useful in the catch block - silently swallowing an error this way makes bugs much harder to track down later.",
    challenge: {
      prompt: "Write a function that throws an error if a given age is negative, and catches/logs a friendly message when it is.",
      hint: "Use try/catch around a check like if (age < 0) throw new Error(\"...\");",
      solution: "function checkAge(age) {\n  try {\n    if (age < 0) throw new Error(\"Age cannot be negative\");\n    console.log(\"Valid age:\", age);\n  } catch (err) {\n    console.log(\"Error caught:\", err.message);\n  }\n}\ncheckAge(-5);",
    },
  },
  {
    title: "Scope & closures",
    body: "Scope determines where a variable is visible. Variables declared with let or const inside a block ({ ... }) - like inside an if statement or a function - only exist within that block; trying to use them outside it causes an error. A closure happens when a function 'remembers' variables from the scope it was created in, even after that outer scope has finished running - it's a natural consequence of how JavaScript's scoping works, not a special separate feature you opt into. A classic example is a counter function: makeCounter() returns an inner function that still has access to a count variable defined inside makeCounter, and that access persists across every future call, letting the inner function keep incrementing the same private count.",
    altExplain: "Variables declared inside { } only exist inside that block. A closure is when an inner function keeps access to variables from where it was created, even after the outer function has finished running.",
    visual: null,
    jscode: "function makeCounter() {\n  let count = 0;\n  return function () {\n    count++;\n    return count;\n  };\n}\n\nconst counter = makeCounter();\nconsole.log(counter());\nconsole.log(counter());\nconsole.log(counter());",
    refBox: {
      syntax: "function outer() { let x; return function inner() { ... }; }",
      desc: "The returned inner function keeps access to outer's variables, even after outer has finished running.",
      example: "function makeId() {\n  let id = 0;\n  return () => ++id;\n}\nconst next = makeId();\nnext(); // 1\nnext(); // 2",
    },
    keyTakeaway: "A closure lets an inner function retain private access to variables from its creating scope - it's how makeCounter() gives you an independent, self-contained counter.",
    commonMistake: "Expecting a variable declared with let inside an if block to still be accessible right after the block ends - block scope means it's already gone.",
    challenge: {
      prompt: "Write a makeMultiplier function that takes a factor and returns a function that multiplies any number by that factor. Test it with a factor of 3.",
      hint: "function makeMultiplier(factor) { return (n) => n * factor; }",
      solution: "function makeMultiplier(factor) {\n  return (n) => n * factor;\n}\nconst triple = makeMultiplier(3);\nconsole.log(triple(7));",
    },
  },
  {
    title: "JSON: stringify and parse",
    body: "JSON (JavaScript Object Notation) is a text format for representing structured data - objects and arrays - that looks almost exactly like JavaScript's own object and array syntax, which is exactly why it became the standard way to send data between a browser and a server. JSON.stringify() converts a JavaScript object or array into a JSON-formatted string, ready to be sent somewhere or saved; JSON.parse() does the reverse, turning a JSON string back into a real, usable JavaScript object. This round trip - stringify to send, parse to receive - is one of the most common patterns in real-world JavaScript, especially anywhere data crosses a network boundary.",
    altExplain: "JSON is a text format for objects/arrays, used constantly to send data over the internet. JSON.stringify() turns an object into JSON text. JSON.parse() turns JSON text back into a real object.",
    visual: null,
    jscode: "const person = { name: \"Ana\", age: 28, hobbies: [\"chess\", \"reading\"] };\n\nconst jsonText = JSON.stringify(person);\nconsole.log(jsonText);\nconsole.log(typeof jsonText);\n\nconst parsedBack = JSON.parse(jsonText);\nconsole.log(parsedBack.name);\nconsole.log(typeof parsedBack);",
    keyTakeaway: "JSON.stringify() and JSON.parse() are inverses of each other - stringify to turn an object into text, parse to turn that text back into a real object.",
    commonMistake: "Trying to access a property directly on a JSON string before parsing it - jsonText.name is undefined, since jsonText is just text until you JSON.parse() it.",
    challenge: {
      prompt: "Create an object for a product with name and price, convert it to a JSON string and print it, then parse it back and print just the price.",
      hint: "Use JSON.stringify() then JSON.parse(), same pattern as the example above.",
      solution: "const product = { name: \"Widget\", price: 9.99 };\nconst text = JSON.stringify(product);\nconsole.log(text);\nconst back = JSON.parse(text);\nconsole.log(back.price);",
    },
  },
  {
    title: "Putting it together: chaining array methods",
    body: "Real JavaScript code frequently chains several array methods in a row, since each one (map, filter, reduce) returns a new array that the next method can immediately act on. A typical example: given a list of orders, you might .filter() down to only the completed ones, .map() each one into just its amount, then .reduce() those amounts into a single total - all in one readable chain, rather than three separate loops with intermediate variables. Reading a chain like this is a skill in itself: work through it left to right, and mentally track what shape the data is in after each step - an array of orders, then an array of numbers, then finally a single number.",
    altExplain: "You can chain map/filter/reduce together, since each returns a new array the next one can use. Read a chain left to right, tracking what the data looks like after each step.",
    visual: null,
    jscode: "const orders = [\n  { amount: 50, status: \"completed\" },\n  { amount: 30, status: \"pending\" },\n  { amount: 75, status: \"completed\" },\n];\n\nconst totalCompleted = orders\n  .filter(o => o.status === \"completed\")\n  .map(o => o.amount)\n  .reduce((sum, amt) => sum + amt, 0);\n\nconsole.log(totalCompleted);",
    keyTakeaway: "Chaining map/filter/reduce reads left to right as a pipeline - track what shape the data becomes after each link in the chain.",
    commonMistake: "Chaining methods in an order that doesn't make sense, like calling .reduce() before .filter() - once reduce collapses the array to a single value, there's nothing left to filter.",
    challenge: {
      prompt: "Given the orders array from the example, chain filter/map/reduce to find the total amount of pending orders instead of completed ones.",
      hint: "Same chain, just change the filter condition to status === \"pending\".",
      solution: "const orders = [\n  { amount: 50, status: \"completed\" },\n  { amount: 30, status: \"pending\" },\n  { amount: 75, status: \"completed\" },\n];\nconst totalPending = orders\n  .filter(o => o.status === \"pending\")\n  .map(o => o.amount)\n  .reduce((sum, amt) => sum + amt, 0);\nconsole.log(totalPending);",
    },
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Boolean(value)", desc: "Converts a value to true/false, following truthy/falsy rules.", example: "Boolean(0); // false\nBoolean(\"0\"); // true" },
      { syntax: "try { } catch (err) { }", desc: "Catches an error instead of letting it crash the program.", example: "try { throw new Error(\"x\"); } catch (e) { console.log(e.message); }" },
      { syntax: "throw new Error(\"message\")", desc: "Raises your own error deliberately.", example: "if (bad) throw new Error(\"invalid input\");" },
      { syntax: "function outer() { return function inner() {}; }", desc: "A closure - inner keeps access to outer's variables.", example: "makeCounter()() // increments a private count" },
      { syntax: "JSON.stringify(obj) / JSON.parse(text)", desc: "Converts an object to JSON text, and back again.", example: "JSON.stringify({a:1}); // '{\"a\":1}'" },
      { syntax: "array.filter(fn).map(fn).reduce(fn, start)", desc: "Chains array methods - each returns a new array the next acts on.", example: "nums.filter(n=>n>0).map(n=>n*2)" },
    ],
  },
];

const JS_QUIZ_BANK = {
  basic: [
    { q: "Which keyword should you default to when declaring a variable that won't be reassigned?", options: ["var", "let", "const", "static"], correct: 2 },
    { q: "What does typeof null return?", options: ["\"null\"", "\"undefined\"", "\"object\"", "\"boolean\""], correct: 2 },
    { q: "What does \"5\" + 3 evaluate to?", options: ["8", "\"53\"", "\"8\"", "An error"], correct: 1 },
    { q: "Which comparison avoids automatic type coercion?", options: ["==", "===", "=", "<>"], correct: 1 },
    { q: "Template literals are delimited using...", options: ["Single quotes", "Double quotes", "Backticks", "Square brackets"], correct: 2 },
  ],
  intermediate: [
    { q: "What does an out-of-range array index like arr[99] return?", options: ["An error", "null", "undefined", "0"], correct: 2 },
    { q: "Which loop is generally cleanest for iterating directly over an array's values?", options: ["for...in", "for...of", "do...while", "A classic for loop only"], correct: 1 },
    { q: "Which array method transforms every item and returns a new array of the same length?", options: ["filter", "reduce", "map", "push"], correct: 2 },
    { q: "What does .reduce()'s second argument typically provide?", options: ["The array's length", "A starting value for the accumulator", "The index to start from", "Nothing, it's optional and unused"], correct: 1 },
    { q: "In const greet = (name) => `Hi, ${name}`;, what kind of function is this?", options: ["A traditional function declaration", "An arrow function", "A class method", "An object property"], correct: 1 },
  ],
  advanced: [
    { q: "Which of these is NOT one of the six falsy values?", options: ["0", "\"\"", "[]", "null"], correct: 2 },
    { q: "What does a try/catch block let you do?", options: ["Prevent errors from ever occurring", "Handle an error instead of letting the program crash", "Make code run faster", "Skip writing tests"], correct: 1 },
    { q: "A closure occurs when...", options: ["A function loses access to its outer scope", "A function retains access to variables from where it was created", "A variable is declared with var", "An array is frozen"], correct: 1 },
    { q: "JSON.stringify() converts...", options: ["A JSON string into an object", "An object into a JSON-formatted string", "A number into a string", "Nothing, it's a validation-only function"], correct: 1 },
    { q: "When chaining .filter().map().reduce(), what does the final .reduce() typically produce?", options: ["A new filtered array", "A new mapped array", "A single combined value", "Always a boolean"], correct: 2 },
  ],
};
