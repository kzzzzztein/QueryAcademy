// =========================================================================
// Python - Entry Level
// =========================================================================
const PY_ENTRY_BASIC = [
  {
    title: "What is Python?",
    body: "Python is a general-purpose programming language known for readable syntax and a huge ecosystem of libraries, which is why it's a common first language and also a serious tool for data analysis, web development, automation, and machine learning. Unlike C#, Python is interpreted rather than compiled - your code runs line by line through a Python interpreter, without a separate compilation step first. It's also dynamically-typed, meaning a variable's type is determined by whatever value it currently holds, and can change as the program runs, which trades some of the safety net C#'s compiler provides for faster, more flexible day-to-day writing.",
    altExplain: "Python is a widely-used, readable programming language. Unlike C#, it runs directly without a separate compile step, and variables aren't locked to one fixed type.",
    visual: null,
    keyTakeaway: "Python is dynamically-typed and interpreted - code runs directly without a separate compile step, and a variable's type can change as the program runs.",
    commonMistake: "Assuming Python's flexibility with types means type mistakes can't happen - they still can, they just get caught while the program is running instead of before it starts.",
  },
  {
    title: "Your first program",
    body: "Python's print() function displays text to the screen, and unlike C#, there's no class or Main method required to get started - a Python file can simply contain statements that run from top to bottom as soon as the file executes. Comments start with # and are ignored by the interpreter, useful for explaining what a piece of code does. This minimalism is one of Python's defining traits: getting from an idea to running code takes noticeably less boilerplate than most other languages.",
    altExplain: "print() displays text. No class or Main method needed - a Python file just runs its statements top to bottom. # starts a comment.",
    visual: null,
    pycode: "# This is a comment\nprint(\"Hello, world!\")",
    keyTakeaway: "Python needs no class or entry-point method - a file's statements simply run in order from top to bottom.",
    commonMistake: "Adding unnecessary boilerplate (classes, main functions) out of habit from another language, when a simple script often doesn't need it in Python.",
    challenge: {
      prompt: "Print your name, then print a second line saying what you're learning.",
      hint: "Two separate print() calls, one per line.",
      solution: "print(\"Your Name\")\nprint(\"Learning Python!\")",
    },
  },
  {
    title: "Variables & data types",
    body: "A Python variable is created simply by assigning a value to a name - no type declaration needed: age = 25 creates an integer variable, price = 19.99 creates a float (Python's term for a decimal number), name = \"Ana\" creates a string, and is_active = True creates a boolean. The type is inferred from the value, and you can check any variable's current type with the built-in type() function. Unlike C#, a variable can later be reassigned to a completely different type of value - age = \"twenty-five\" is perfectly legal, though generally poor practice, since it can make code harder to follow.",
    altExplain: "Just assign a value to create a variable - no type keyword needed. int, float (decimal), str (text), and bool (True/False) are the basic types, inferred automatically.",
    visual: null,
    pycode: "age = 25\nprice = 19.99\nname = \"Ana\"\nis_active = True\n\nprint(name)\nprint(age)\nprint(type(age))",
    refBox: {
      syntax: "name = value",
      desc: "Creates a variable - no type keyword needed. The type is inferred from the value.",
      example: "age = 25\nname = \"Ana\"\ntype(age)  # <class 'int'>",
    },
    keyTakeaway: "type() tells you a variable's current type - useful for debugging when you're not sure what kind of value you're actually working with.",
    commonMistake: "Reassigning a variable to a completely different type partway through a program, which technically works in Python but makes code confusing to follow.",
    challenge: {
      prompt: "Create a variable for temperature set to 36.6 and print both its value and its type.",
      hint: "temperature = 36.6, then print(temperature) and print(type(temperature))",
      solution: "temperature = 36.6\nprint(temperature)\nprint(type(temperature))",
    },
  },
  {
    title: "Operators & expressions",
    body: "Python supports the standard arithmetic operators, plus a couple of distinctive ones: ** for exponentiation (2 ** 3 equals 8) and // for floor division (integer division that rounds down, discarding any remainder). Unlike C#, dividing two integers with a single / always produces a decimal result in Python 3 - 7 / 2 gives 3.5, not 3 - which is a common surprise for anyone coming from a language with C#'s stricter integer-division rules. Comparison operators (==, !=, >, <) and logical operators (and, or, not, written as words rather than symbols) round out the basics.",
    altExplain: "Python's / always gives a decimal result, unlike C#'s integer division. Use // if you specifically want the rounded-down whole number instead. ** is for exponents.",
    visual: null,
    pycode: "a = 7\nb = 2\nprint(a / b)   # 3.5 - always decimal in Python\nprint(a // b)  # 3 - floor division\nprint(a ** b)  # 49 - exponent",
    keyTakeaway: "Python's / always returns a decimal (float) result - use // specifically when you want integer-style rounded-down division.",
    commonMistake: "Expecting / to behave like C#'s integer division when both operands are whole numbers - in Python, / always produces a decimal result.",
    challenge: {
      prompt: "Print 2 to the power of 10.",
      hint: "Use the ** operator.",
      solution: "print(2 ** 10)",
    },
  },
  {
    title: "Strings & f-strings",
    body: "Python strings support f-strings for embedding variables directly into text, similar in spirit to C#'s string interpolation: prefixing a string with f and wrapping a variable in curly braces, like f\"Hello, {name}!\", inserts that variable's value at that position. Strings also support many useful built-in methods: .upper() and .lower() change casing, .strip() removes leading/trailing whitespace, and .split() breaks a string into a list of pieces based on a separator - these methods are called directly on the string itself using dot notation, and each returns a new string or list rather than modifying the original.",
    altExplain: "f-strings (f\"...{variable}...\") embed variables into text, like C#'s string interpolation. Strings also have useful built-in methods like .upper(), .strip(), and .split().",
    visual: null,
    pycode: "name = \"Ana\"\nage = 28\nprint(f\"{name} is {age} years old.\")\n\nmessy = \"  Hello World  \"\nprint(messy.strip().upper())",
    keyTakeaway: "String methods like .upper() and .strip() return a new string rather than modifying the original - strings in Python, like in C#, are immutable.",
    commonMistake: "Forgetting the f before the string's opening quote, causing {variable} to print literally instead of being replaced with its value.",
    challenge: {
      prompt: "Create a product name and price, then print a sentence using an f-string combining both.",
      hint: "product = \"Widget\"; price = 9.99; then f\"...{product}...{price}...\"",
      solution: "product = \"Widget\"\nprice = 9.99\nprint(f\"{product} costs {price}\")",
    },
  },
  {
    title: "Conditionals: if, elif, else",
    body: "Python's if statement works much like C#'s, but with a key structural difference: instead of curly braces {} to mark a block, Python uses indentation itself - consistent spacing (typically 4 spaces) tells the interpreter which lines belong inside the if block. elif (short for 'else if') chains additional conditions, and else catches everything else. Getting indentation right isn't just a style preference in Python - it's part of the language's actual syntax, and inconsistent indentation causes a real error, not just a warning.",
    altExplain: "if/elif/else works like other languages, but Python uses indentation (consistent spacing) instead of curly braces to mark which lines belong inside a block.",
    visual: null,
    pycode: "score = 85\nif score >= 90:\n    print(\"A\")\nelif score >= 80:\n    print(\"B\")\nelse:\n    print(\"C or below\")",
    refBox: {
      syntax: "if cond: / elif cond: / else:",
      desc: "Indentation (not braces) marks each block - inconsistent indentation causes a real error in Python.",
      example: "if x > 0:\n    print(\"positive\")\nelse:\n    print(\"non-positive\")",
    },
    keyTakeaway: "Indentation is part of Python's actual syntax, not just a style choice - inconsistent indentation causes a real IndentationError.",
    commonMistake: "Mixing tabs and spaces for indentation within the same file, which can cause a confusing indentation error even though the code visually looks aligned.",
    challenge: {
      prompt: "Write an if/else that prints \"Weekend\" if a day variable equals \"Saturday\" or \"Sunday\", otherwise prints \"Weekday\".",
      hint: "Use or to check for either Saturday or Sunday in one condition.",
      solution: "day = \"Saturday\"\nif day == \"Saturday\" or day == \"Sunday\":\n    print(\"Weekend\")\nelse:\n    print(\"Weekday\")",
    },
  },
  {
    title: "Getting user input",
    body: "The input() function pauses a Python program and waits for the person running it to type something and press Enter, returning whatever they typed as a string - very similar to C#'s Console.ReadLine(). Since input() always returns a string, converting typed numbers with int() or float() is necessary before doing math with them. In this course's playground, use the 'stdin' box below the code editor to provide in advance what input() calls should receive, since there's no way to type live into a running program on this page.",
    altExplain: "input() reads what someone types, always as a string - use int() to convert it into a number before doing math with it, just like Console.ReadLine() in C#.",
    visual: null,
    pycode: "name = input(\"What's your name? \")\nprint(f\"Hello, {name}!\")",
    keyTakeaway: "input() always returns a string, even if the person types a number - convert it explicitly with int() or float() before using it as a number.",
    commonMistake: "Trying to do math directly on the result of input() without converting it first, causing a TypeError since it's still text.",
    challenge: {
      prompt: "Read a name from stdin and print a greeting using it. Try putting a name in the stdin box before running.",
      hint: "Type a name into the stdin box below the code, then run - your input() call will receive it.",
      solution: "name = input()\nprint(f\"Welcome, {name}!\")",
    },
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "print(value)", desc: "Displays text to the screen.", example: "print(\"Hello\")" },
      { syntax: "name = value", desc: "Creates a variable - type is inferred, no keyword needed.", example: "age = 25\nname = \"Ana\"" },
      { syntax: "type(value)", desc: "Returns a value's type.", example: "type(25)  # <class 'int'>" },
      { syntax: "+ - * / // % **", desc: "Arithmetic operators; / always gives a decimal, // rounds down, ** is exponent.", example: "7 / 2  # 3.5\n7 // 2  # 3" },
      { syntax: "f\"text {expr} text\"", desc: "f-string - embeds a variable/expression directly into text.", example: "f\"Hi, {name}!\"" },
      { syntax: "if cond: / elif cond: / else:", desc: "Indentation marks each block - not braces.", example: "if x > 0:\n    print(\"positive\")" },
      { syntax: "input(\"prompt\")", desc: "Reads text typed by the user - always returns a string.", example: "name = input()" },
    ],
  },
];

// =========================================================================
// Deep-dive module: Loops (Python) - referenced by the isModuleStub entry at
// the top of PY_ENTRY_INTERMEDIATE below, instead of a single flat lesson.
// =========================================================================
MODULES["python-loops"] = {
  id: "python-loops",
  title: "Loops",
  icon: "layers",
  accent: "#157f3c",
  tagline: "Every way Python has to repeat work, from a basic for loop to writing one line that builds a whole list.",
  whatYouLearn: [
    "Write for loops over a range, a list, or a string",
    "Repeat with while, and stop safely with break and continue",
    "Use the loop else clause, a genuinely Python-only feature",
    "Nest loops, and loop over several sequences at once",
    "Turn a short loop into a one-line list comprehension",
    "Spot the loop mistakes that quietly slow code down or break it",
  ],
  lessons: [
    {
      title: "Why loops matter",
      body: "Without a loop, repeating an action means writing the same line of code once for every repetition - print a receipt line for item one, then item two, then item three, and so on, by hand, for however many items happen to exist. That falls apart the moment the number of items isn't known ahead of time, or is simply too large to type out. A loop replaces all of that repeated code with a single block that Python runs over and over, once per item or once per condition check, so the size of your code stops being tied to the size of your data. Every loop you'll see in this module is really just a different answer to the same question: repeat this, but how many times, and based on what?",
      altExplain: "Writing the same line five times is fine for five items. A loop is what you use instead of writing it five thousand times.",
      visual: null,
      pycode: "for day in [\"Mon\", \"Tue\", \"Wed\"]:\n    print(f\"{day}: open 9am-6pm\")",
      keyTakeaway: "A loop's whole job is to decouple the size of your code from the size of your data - one block, run as many times as needed.",
      commonMistake: "Copy-pasting a line of code several times with small changes instead of reaching for a loop - it works for 3 items, but breaks down completely at 300.",
    },
    {
      title: "The for loop and range()",
      body: "Python's for loop is built around iterating over a sequence directly, rather than manually tracking a counter the way many languages require. for i in range(5): runs its body 5 times, with i taking the values 0, 1, 2, 3, and 4 in turn - range's upper bound is exclusive, so range(5) stops just short of 5 itself. Everything indented beneath the for line is the loop's body, run once per value, exactly the same indentation rule that if statements use. This range()-based form is the right tool whenever what you actually need is a specific count of repetitions, rather than needing the loop to visit real data.",
      altExplain: "for i in range(5) means 'do this 5 times, with i going 0, 1, 2, 3, 4.' The 5 itself is never actually reached.",
      visual: null,
      pycode: "for i in range(1, 6):\n    print(f\"Count: {i}\")",
      refBox: {
        syntax: "for i in range(n):",
        desc: "Runs the body n times, with i taking values 0 through n-1. The upper bound is always exclusive.",
        example: "for i in range(3):\n    print(i)  # 0, 1, 2",
      },
      keyTakeaway: "range(5) produces 0 through 4, not 1 through 5 - the upper bound is exclusive, a near-universal early surprise.",
      commonMistake: "Expecting range(5) to include 5 - it actually stops just before that number, producing 0, 1, 2, 3, 4.",
      challenge: {
        prompt: "Write a loop that prints the numbers 1 through 5, inclusive.",
        hint: "You need range's upper bound to be one past the last number you want to see.",
        solution: "for i in range(1, 6):\n    print(i)",
      },
    },
    {
      title: "Looping directly over lists and strings",
      body: "range() is for counting, but most real loops are about visiting data you already have, and Python's for loop handles that directly: for fruit in [\"apple\", \"banana\", \"mango\"]: hands you each item in turn, with no index or counter involved at all. This is close in spirit to C#'s foreach, and it's the loop style you'll reach for far more often than range() once you're working with actual data instead of a plain count. Strings work exactly the same way, since a string is really just a sequence of individual characters - for letter in \"cat\": visits 'c', then 'a', then 't', one character per pass through the loop.",
      altExplain: "for item in my_list steps through every item directly, no counter needed - like C#'s foreach. A string loops character by character the same way.",
      visual: null,
      pycode: "fruits = [\"apple\", \"banana\", \"mango\"]\nfor fruit in fruits:\n    print(fruit.upper())",
      keyTakeaway: "When you need each item itself, not a count, loop directly over the sequence instead of reaching for range(len(...)).",
      commonMistake: "Writing for i in range(len(fruits)): fruits[i] out of habit, when for fruit in fruits: says exactly the same thing more simply.",
      challenge: {
        prompt: "Loop over the string \"hello\" and print each letter on its own line.",
        hint: "A string loops character by character, exactly like a list loops item by item.",
        solution: "for letter in \"hello\":\n    print(letter)",
      },
    },
    {
      title: "Counting with range(): start, stop, step",
      body: "range() actually accepts up to three arguments, not just one. range(5) alone starts at 0. range(2, 8) starts at 2 and stops before 8. range(10, 0, -1) adds a third argument, the step, which controls both the size of each jump and its direction - a negative step counts downward instead of up. range(0, 10, 2) counts by twos: 0, 2, 4, 6, 8. This three-argument form is what makes range() flexible enough to express nearly any counting pattern you'd need, from counting down a countdown timer to skipping every other item, without ever needing a manual counter variable that you increment or decrement by hand inside the loop body.",
      altExplain: "range(start, stop, step) is the full form. Leave out start and it defaults to 0. A negative step counts backward instead of forward.",
      visual: null,
      pycode: "for i in range(10, 0, -2):\n    print(i)",
      refBox: {
        syntax: "range(start, stop, step)",
        desc: "start defaults to 0, step defaults to 1. A negative step counts down; stop is always exclusive.",
        example: "range(0, 10, 2)  # 0, 2, 4, 6, 8",
      },
      keyTakeaway: "The third range() argument is the step - it can be negative, which is how you count downward without a manual counter.",
      commonMistake: "Forgetting that a downward range needs a negative step - range(10, 0) with no step at all produces nothing, since 10 is already past the stop value going upward.",
      challenge: {
        prompt: "Print every third number from 0 to 30, inclusive of 30.",
        hint: "The step goes in the third argument position; the stop needs to be one past 30 so 30 itself is included.",
        solution: "for i in range(0, 31, 3):\n    print(i)",
      },
    },
    {
      title: "The while loop",
      body: "A for loop is for a known sequence or count; a while loop is for repeating until a condition stops being true, without necessarily knowing in advance how many times that will take. while balance > 0: keeps running its body for as long as balance stays above zero, checking the condition again before every single pass. This makes while the right tool whenever the number of repetitions genuinely depends on something that changes while the loop runs, like waiting for a user to enter valid input, or draining a balance down through repeated withdrawals. The condition is checked before each iteration, including the very first one, so a while loop whose condition starts out false never runs its body at all.",
      altExplain: "while keeps going as long as its condition is true, checked fresh before every pass - useful when you don't know the count ahead of time.",
      visual: null,
      pycode: "balance = 100\nwhile balance > 0:\n    balance -= 30\n    print(f\"Remaining: {balance}\")",
      keyTakeaway: "A while loop's condition is checked before every pass, including the first - if it starts false, the body never runs at all.",
      commonMistake: "Forgetting to change the variable the condition depends on inside the loop body, which produces an infinite loop that never stops on its own.",
      challenge: {
        prompt: "Write a while loop that starts a counter at 1 and prints it, doubling the counter each time, stopping once it exceeds 100.",
        hint: "The loop condition should check the counter against 100, and the counter needs to be doubled somewhere inside the body.",
        solution: "n = 1\nwhile n <= 100:\n    print(n)\n    n *= 2",
      },
    },
    {
      title: "break and continue",
      body: "Both for and while loops support two extra control words for changing their flow mid-run. break exits the loop immediately, skipping any remaining iterations entirely - useful once you've found what you were looking for and there's no reason to keep checking. continue is gentler: it skips only the rest of the current pass and jumps straight to the next one, without leaving the loop. A common pattern is searching a list with a for loop and break: as soon as a match is found, break stops the search instantly instead of wastefully checking every remaining item. continue, by contrast, is typically used to skip over values that don't need processing, like ignoring blank entries while still continuing on to check every other item.",
      altExplain: "break leaves the loop entirely, right now. continue skips just this one pass and moves on to the next.",
      visual: null,
      pycode: "for n in [4, 12, 7, 21, 3]:\n    if n > 20:\n        break\n    if n % 2 != 0:\n        continue\n    print(n)",
      keyTakeaway: "break stops the whole loop; continue only skips the current iteration and keeps looping.",
      commonMistake: "Reaching for break when continue is what's actually needed, which ends the loop early and silently skips over items that still should have been checked.",
      challenge: {
        prompt: "Loop over [3, 7, 2, 9, 15, 4] and print each number, but stop entirely the moment you hit one greater than 10.",
        hint: "You want break here, not continue - the loop needs to end completely, not just skip that one number.",
        solution: "for n in [3, 7, 2, 9, 15, 4]:\n    if n > 10:\n        break\n    print(n)",
      },
    },
    {
      title: "The loop else clause",
      body: "This one has no equivalent in most other languages, including C#, so it's worth learning even though it's used less often than everything else in this module: both for and while loops can have an else block attached, and that block runs only if the loop finished naturally, without ever hitting a break. This makes else a clean way to express search patterns: loop through a list looking for something, break the moment it's found, and let the else block handle the 'not found at all' case without needing a separate flag variable to track whether a match happened. If the loop never runs break, the else always executes right after the loop finishes; if break does fire, the else block is skipped entirely.",
      altExplain: "A loop's else block runs only if the loop finished without a break - handy for 'searched everything and found nothing' logic.",
      visual: null,
      pycode: "numbers = [3, 7, 12, 5]\nfor n in numbers:\n    if n == 99:\n        print(\"Found it!\")\n        break\nelse:\n    print(\"99 wasn't in the list.\")",
      refBox: {
        syntax: "for ... in ...:\\n    ...\\nelse:\\n    ...",
        desc: "The else block runs only if the loop completes without hitting a break.",
        example: "for n in [1,2,3]:\n    pass\nelse:\n    print(\"done\")",
      },
      keyTakeaway: "A loop's else only skips when break fires - it's the 'this loop searched everything and found nothing' block.",
      commonMistake: "Assuming else always runs after a loop, the same way it would after an if - it specifically does not run if break was hit.",
      challenge: {
        prompt: "Loop over [2, 4, 6, 8] looking for an odd number. Print the odd number and break if you find one, otherwise use an else block to print \"All even.\"",
        hint: "Check n % 2 != 0 to test for odd, and remember the else attaches to the for line, not to the if.",
        solution: "for n in [2, 4, 6, 8]:\n    if n % 2 != 0:\n        print(n)\n        break\nelse:\n    print(\"All even.\")",
      },
    },
    {
      title: "Nested loops",
      body: "A loop can contain another loop entirely inside its body, and each pass of the outer loop runs the full inner loop from start to finish before moving on. This is exactly how you'd generate every combination of two things, like every row-and-column pair in a grid, or a small multiplication table: the outer loop picks a row, and for each row, the inner loop runs through every column. It's worth being deliberate with nested loops, since the total number of passes is the outer count multiplied by the inner count - a 10-by-10 nested loop runs its innermost line 100 times, and that multiplication compounds quickly if you nest a third loop inside the second.",
      altExplain: "A loop inside a loop runs the inner one completely for every single pass of the outer one - 3 outer passes times 4 inner passes is 12 total runs.",
      visual: null,
      pycode: "for row in range(1, 4):\n    for col in range(1, 4):\n        print(f\"{row} x {col} = {row * col}\")",
      keyTakeaway: "A nested loop's total number of passes is outer count times inner count, not outer plus inner - it grows fast.",
      commonMistake: "Reusing the same variable name for both loop counters (using i for both the outer and inner loop), which silently overwrites the outer counter and breaks the outer loop's logic.",
      challenge: {
        prompt: "Print a small 3-by-3 grid of asterisks, one row per line, using a nested loop.",
        hint: "The outer loop controls the rows; the inner loop should print one asterisk at a time, with a newline printed after the inner loop finishes each row.",
        solution: "for row in range(3):\n    line = \"\"\n    for col in range(3):\n        line += \"* \"\n    print(line)",
      },
    },
    {
      title: "enumerate(): index and value together",
      body: "Looping directly over a list gives you each value, but sometimes you also need to know each item's position - which row number it is, or where in the list it sits. Manually tracking a separate counter variable works, but Python has a cleaner built-in for exactly this: enumerate(my_list) hands back both the index and the value together on every pass, typically unpacked straight into two loop variables like for i, fruit in enumerate(fruits):. This avoids the easy-to-get-wrong pattern of maintaining a counter by hand, incrementing it manually on every single pass and hoping it never drifts out of sync with the actual position in the list.",
      altExplain: "enumerate(my_list) gives you both the position and the value together, so you don't have to track a counter variable yourself.",
      visual: null,
      pycode: "fruits = [\"apple\", \"banana\", \"mango\"]\nfor i, fruit in enumerate(fruits):\n    print(f\"{i}: {fruit}\")",
      refBox: {
        syntax: "for i, item in enumerate(iterable):",
        desc: "Yields (index, item) pairs together. enumerate(iterable, 1) starts counting from 1 instead of 0.",
        example: "for i, name in enumerate(names, 1):\n    print(i, name)",
      },
      keyTakeaway: "enumerate(my_list, 1) starts counting from 1 instead of 0 - handy any time you're numbering things for a human to read.",
      commonMistake: "Manually maintaining a separate counter variable (i = 0, then i += 1 at the end of the loop body) when enumerate() already does exactly that.",
      challenge: {
        prompt: "Loop over [\"gold\", \"silver\", \"bronze\"] and print each one numbered starting from 1, like '1: gold'.",
        hint: "enumerate() takes an optional second argument to control what number it starts counting from.",
        solution: "medals = [\"gold\", \"silver\", \"bronze\"]\nfor i, medal in enumerate(medals, 1):\n    print(f\"{i}: {medal}\")",
      },
    },
    {
      title: "zip(): looping over multiple sequences together",
      body: "enumerate() pairs a list with its own positions; zip() pairs two or more separate lists together, position by position, into one combined loop. for name, score in zip(names, scores): walks through both lists at exactly the same time, handing you the first item from each on the first pass, the second item from each on the second pass, and so on - it's the direct alternative to indexing both lists separately with a shared counter. If the lists aren't the same length, zip() simply stops as soon as the shortest one runs out, rather than raising an error, which is worth knowing so a silently truncated result doesn't catch you by surprise.",
      altExplain: "zip(list1, list2) loops over two lists at once, pairing up item 1 with item 1, item 2 with item 2, and so on.",
      visual: null,
      pycode: "names = [\"Ana\", \"Marco\", \"Liza\"]\nscores = [92, 85, 78]\nfor name, score in zip(names, scores):\n    print(f\"{name}: {score}\")",
      keyTakeaway: "zip() stops at the shortest input list without any error - a length mismatch fails silently, not loudly.",
      commonMistake: "Assuming zip() will complain if the two lists are different lengths - it won't, it just quietly drops the extra items from the longer list.",
      challenge: {
        prompt: "Given days = [\"Mon\", \"Tue\", \"Wed\"] and highs = [30, 32, 29], print each day with its high temperature, like 'Mon: 30'.",
        hint: "zip() pairs the two lists together position by position inside a single for loop.",
        solution: "days = [\"Mon\", \"Tue\", \"Wed\"]\nhighs = [30, 32, 29]\nfor day, high in zip(days, highs):\n    print(f\"{day}: {high}\")",
      },
    },
    {
      title: "List comprehensions: loops in one line",
      body: "A very common loop pattern is building a brand new list by transforming or filtering an existing one - looping over a list, doing something to each item, and appending the result to a fresh list. Python has a compact syntax that expresses exactly that pattern in a single line: [n * n for n in numbers] builds a new list of every number squared, without an empty list and an explicit .append() call anywhere in sight. Adding a condition at the end filters as it goes: [n for n in numbers if n % 2 == 0] keeps only the even numbers. A comprehension is really just a for loop with an implicit append, written in a different order - it doesn't do anything a regular loop couldn't, it's simply the more idiomatic way to write this one specific, extremely common pattern.",
      altExplain: "[expression for item in list] is a for loop and an append(), condensed into one line. Adding 'if condition' at the end filters which items make it in.",
      visual: null,
      pycode: "numbers = [1, 2, 3, 4, 5, 6]\nsquares = [n * n for n in numbers]\nevens = [n for n in numbers if n % 2 == 0]\nprint(squares)\nprint(evens)",
      refBox: {
        syntax: "[expression for item in iterable if condition]",
        desc: "Builds a new list. The 'if condition' part is optional, and filters which items are included.",
        example: "[n * 2 for n in range(5) if n > 1]  # [4, 6, 8]",
      },
      keyTakeaway: "A comprehension is a regular loop with an implicit append() - it's shorthand, not a different mechanism underneath.",
      commonMistake: "Cramming too much logic into one comprehension until it's harder to read than the plain loop it replaced - if it needs more than one condition or transformation, a regular loop is usually clearer.",
      challenge: {
        prompt: "Using a list comprehension, build a list of the cubes (n ** 3) of every number in [1, 2, 3, 4, 5] that's greater than 2.",
        hint: "The transformation (n ** 3) goes right after the opening bracket, and the filter condition goes at the very end.",
        solution: "numbers = [1, 2, 3, 4, 5]\ncubes = [n ** 3 for n in numbers if n > 2]\nprint(cubes)",
      },
    },
    {
      title: "Common loop mistakes and performance pitfalls",
      body: "A handful of loop mistakes come up constantly enough that they're worth naming directly. Modifying a list while looping over it directly - removing an item from my_list inside a for item in my_list: loop - causes items to be silently skipped, because the list's positions shift out from under the loop as it runs; looping over a copy (for item in my_list[:]:) or building a new list instead avoids this entirely. Using range(len(my_list)) purely to then index back into the same list with my_list[i] is nearly always unnecessary, since looping directly over the list (or using enumerate() if the index is genuinely needed) says the same thing more clearly. And nesting loops three or four levels deep to process large amounts of data can get slow fast, since the work multiplies at every level - it's worth asking whether a comprehension, a dictionary lookup, or a built-in function could replace an inner loop entirely before assuming a nested loop is the only option.",
      altExplain: "Three real traps: changing a list while looping over it skips items, range(len(x)) is usually unnecessary, and deeply nested loops get slow because the work multiplies at every level.",
      visual: null,
      pycode: "# Risky: modifying while iterating\nnums = [1, 2, 3, 4, 5]\nfor n in nums[:]:      # loop over a copy instead\n    if n % 2 == 0:\n        nums.remove(n)\nprint(nums)",
      keyTakeaway: "Looping over my_list[:] (a copy) instead of my_list directly is the standard fix when a loop needs to remove items from the list it's iterating.",
      commonMistake: "Calling my_list.remove(item) or del my_list[i] directly inside a for item in my_list: loop, which silently skips over items as the list shrinks mid-loop.",
      challenge: {
        prompt: "Rewrite this buggy loop so it safely removes every negative number from the list, without skipping any: for n in nums: if n < 0: nums.remove(n)",
        hint: "Loop over a copy of the list (nums[:]) while still calling .remove() on the original nums.",
        solution: "nums = [3, -1, 4, -2, 5, -7]\nfor n in nums[:]:\n    if n < 0:\n        nums.remove(n)\nprint(nums)",
      },
    },
    {
      title: "Practice: combining loop patterns",
      body: "Real code rarely uses just one loop pattern in isolation - a typical script combines several of the ideas from this module in the same few lines. The example here processes a small list of orders: it uses enumerate() to number each one for a printed receipt, a nested condition to flag any order over a spending threshold, and a running total built up the same way a sum is accumulated in any loop, regardless of language. Working through it line by line is a good way to check that for, enumerate(), and conditional logic inside a loop body all feel like one connected toolkit now, rather than separate, unrelated ideas you learned one at a time.",
      altExplain: "This last lesson doesn't introduce anything new - it's the same loop tools from this module, combined in one realistic example.",
      visual: null,
      pycode: "orders = [42.50, 108.00, 15.75, 230.10, 60.00]\ntotal = 0\nfor i, amount in enumerate(orders, 1):\n    flag = \" (large order)\" if amount > 100 else \"\"\n    print(f\"Order {i}: ${amount:.2f}{flag}\")\n    total += amount\n\nprint(f\"Total: ${total:.2f}\")",
      keyTakeaway: "Most real loops combine several patterns at once - enumerate() for numbering, a condition for logic, and a running total, all in the same loop body.",
      commonMistake: "Writing three separate loops over the same list (one to number items, one to flag large ones, one to total them) when a single pass through the data can do all three at once.",
      challenge: {
        prompt: "Given scores = [55, 92, 78, 40, 88], loop through them once, printing each one numbered from 1, and also print how many scores were 60 or above by the end.",
        hint: "Keep a running counter that only increments when the condition is met, similar to how the running total works in the example above.",
        solution: "scores = [55, 92, 78, 40, 88]\npassing = 0\nfor i, score in enumerate(scores, 1):\n    print(f\"{i}: {score}\")\n    if score >= 60:\n        passing += 1\n\nprint(f\"Passing: {passing}\")",
      },
    },
  ],
};

const PY_ENTRY_INTERMEDIATE = [
  { isModuleStub: true, moduleId: "python-loops" },
  {
    title: "Lists",
    body: "A Python list is a resizable, ordered collection that can hold any type of value - even mixed types in the same list - and is roughly Python's equivalent to C#'s List<T>, but built into the language itself with no import needed. Lists are created with square brackets: numbers = [90, 85, 77], and support .append() to add an item, .remove() to remove one by value, and indexing with [0] for the first item, [-1] for the last item (negative indexing is a distinctly Python convenience, letting you count from the end without knowing the list's length). Lists are mutable, meaning you can change their contents in place after creation.",
    altExplain: "A Python list holds an ordered, resizable collection of values, similar to C#'s List<T> but built in with no import needed. Negative indices count from the end - [-1] is the last item.",
    visual: null,
    pycode: "fruits = [\"apple\", \"banana\", \"mango\"]\nfruits.append(\"grape\")\nprint(fruits[0])\nprint(fruits[-1])\n\nfor fruit in fruits:\n    print(fruit)",
    keyTakeaway: "Negative indices count from the end of a list - [-1] is always the last item, regardless of the list's length.",
    commonMistake: "Trying to access an index beyond the list's actual length, which raises an IndexError rather than silently returning something like None.",
    challenge: {
      prompt: "Create a list of 4 favorite numbers and print their sum using a loop.",
      hint: "Use a for loop with a running total variable, or the built-in sum() function.",
      solution: "numbers = [3, 7, 12, 5]\ntotal = 0\nfor n in numbers:\n    total += n\nprint(total)",
    },
  },
  {
    title: "Dictionaries",
    body: "A Python dictionary stores key-value pairs, directly comparable to C#'s Dictionary<TKey, TValue> but with lighter syntax: ages = {\"Ana\": 28, \"Marco\": 34} creates one immediately, and ages[\"Ana\"] retrieves Ana's age. Adding a new key is as simple as ages[\"Liza\"] = 30 - no separate Add() method call needed. Checking whether a key exists before accessing it avoids a KeyError, either with the in keyword (if \"Liza\" in ages) or the safer .get() method, which returns None (or a default value you specify) instead of raising an error when the key is missing.",
    altExplain: "A Python dictionary stores key-value pairs, like C#'s Dictionary but with simpler syntax. Use .get() instead of direct indexing when a key might not exist, to avoid an error.",
    visual: null,
    pycode: "ages = {\"Ana\": 28, \"Marco\": 34}\nages[\"Liza\"] = 30\n\nprint(ages[\"Ana\"])\nprint(ages.get(\"Someone\", \"Not found\"))",
    keyTakeaway: "dictionary.get(key, default) is the safe way to look up a key that might not exist - it returns the default instead of raising a KeyError.",
    commonMistake: "Accessing a dictionary key directly (ages[\"missing\"]) without checking it exists first, causing a KeyError crash.",
    challenge: {
      prompt: "Create a dictionary of 2 people's favorite colors, then safely look up a third person who isn't in the dictionary using .get() with a default value.",
      hint: "colors.get(\"SomeoneElse\", \"unknown\")",
      solution: "colors = {\"Ana\": \"blue\", \"Marco\": \"green\"}\nprint(colors.get(\"Liza\", \"unknown\"))",
    },
  },
  {
    title: "Functions",
    body: "A function is defined with the def keyword, a name, and a parameter list in parentheses, with the function's body indented beneath it: def add(a, b): followed by an indented return a + b defines a reusable function named add. Unlike C#, Python doesn't require declaring a return type or parameter types up front - the function works with whatever types are actually passed in, as long as the operations inside make sense for those types. Parameters can have default values (def greet(name=\"friend\"):), letting a function be called with fewer arguments than it technically accepts.",
    altExplain: "def defines a function, with its body indented beneath. No return type or parameter types needed to declare. Parameters can have default values for optional arguments.",
    visual: null,
    pycode: "def add(a, b):\n    return a + b\n\ndef greet(name=\"friend\"):\n    print(f\"Hello, {name}!\")\n\nprint(add(3, 4))\ngreet()\ngreet(\"Ana\")",
    refBox: {
      syntax: "def name(param, default=value): / return ...",
      desc: "def defines a function - body is indented beneath. Default values make arguments optional.",
      example: "def square(n):\n    return n * n",
    },
    keyTakeaway: "A default parameter value (name=\"friend\") makes that argument optional - the function can be called with or without providing it.",
    commonMistake: "Forgetting that Python doesn't check argument types at all - passing the wrong type won't fail until the function actually tries to use it in an invalid way.",
    challenge: {
      prompt: "Write a function called square that takes a number and returns its square, then call it with 6.",
      hint: "def square(n): return n * n",
      solution: "def square(n):\n    return n * n\n\nprint(square(6))",
    },
  },
  {
    title: "Tuples & sets",
    body: "A tuple is an ordered collection like a list, but immutable - once created, its contents can't be changed, which makes it a good fit for values that shouldn't accidentally be modified, like a fixed coordinate pair: point = (3, 4). A set stores unique values with no particular order and no duplicates allowed, useful for fast membership checks or removing duplicates from a list: unique_numbers = set([1, 2, 2, 3]) automatically collapses to {1, 2, 3}. Choosing between list, tuple, and set based on whether you need order, mutability, and uniqueness is a routine early Python decision.",
    altExplain: "A tuple is like a list but can't be changed after creation - good for fixed values. A set holds unique values with no duplicates - good for removing duplicates or fast 'does this exist' checks.",
    visual: null,
    pycode: "point = (3, 4)\nprint(point[0])\n\nnumbers = [1, 2, 2, 3, 3, 3]\nunique_numbers = set(numbers)\nprint(unique_numbers)",
    keyTakeaway: "Converting a list to a set is a quick, common way to remove duplicates - set(my_list) collapses repeated values automatically.",
    commonMistake: "Trying to modify a tuple after creation (point[0] = 5) - this raises an error, since tuples are immutable by design.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "for item in iterable: / for i in range(n):", desc: "Iterates over a sequence, or over a count via range() (exclusive upper bound).", example: "for f in [\"a\",\"b\"]:\n    print(f)" },
      { syntax: "while cond: / break / continue", desc: "Repeats while true; break exits immediately; continue skips to the next iteration.", example: "while i < 3:\n    i += 1" },
      { syntax: "my_list = [1, 2, 3]", desc: "List literal - an ordered, mutable collection.", example: "my_list.append(4)" },
      { syntax: "my_dict = {\"key\": value}", desc: "Dictionary literal - key-value pairs.", example: "my_dict.get(\"key\", \"default\")" },
      { syntax: "def name(param, default=value): / return ...", desc: "Defines a function - no type declarations needed.", example: "def square(n):\n    return n * n" },
      { syntax: "(a, b) / {1, 2, 3}", desc: "Tuple (immutable, ordered) vs set (unique values, no order).", example: "point = (3, 4)\nunique = set([1,1,2])" },
    ],
  },
];

const PY_ENTRY_ADVANCED = [
  {
    title: "Classes & objects",
    body: "Python classes work conceptually like C#'s: a blueprint bundling data and behavior together. The __init__ method is Python's constructor, automatically called when an object is created, and self refers to the specific object instance being worked with - it must be the first parameter of every regular method, though Python passes it automatically when you call a method, so you never write it explicitly at the call site. class Dog: with a def __init__(self, name): storing self.name = name, and a def bark(self): method, together define a complete, usable class.",
    altExplain: "__init__ is Python's constructor, run automatically when an object is created. self refers to the specific object, similar to 'this' in C# - every method needs it as the first parameter.",
    visual: null,
    pycode: "class Dog:\n    def __init__(self, name):\n        self.name = name\n\n    def bark(self):\n        print(f\"{self.name} says Woof!\")\n\nmy_dog = Dog(\"Rex\")\nmy_dog.bark()",
    refBox: {
      syntax: "class Name: / def __init__(self, param): / self.x = x",
      desc: "__init__ is the constructor, run automatically on creation. self refers to the specific instance.",
      example: "class Dog:\n    def __init__(self, name):\n        self.name = name",
    },
    keyTakeaway: "self must be the first parameter of every regular method, but Python passes it automatically - you never include it when actually calling the method.",
    commonMistake: "Forgetting self as a method's first parameter, which causes a confusing 'missing argument' error when the method is later called normally.",
    challenge: {
      prompt: "Create a Cat class with a name and a meow method, then create and use a Cat object.",
      hint: "Same structure as the Dog example, just renamed.",
      solution: "class Cat:\n    def __init__(self, name):\n        self.name = name\n\n    def meow(self):\n        print(f\"{self.name} says Meow!\")\n\nmy_cat = Cat(\"Whiskers\")\nmy_cat.meow()",
    },
  },
  {
    title: "Inheritance",
    body: "A Python class inherits from another by listing it in parentheses after the class name: class Dog(Animal): makes Dog inherit everything Animal defines. Overriding a method is simpler than in C# - there's no virtual or override keyword required, a subclass just defines a method with the same name, and Python automatically uses the subclass's version when called on a subclass instance. The built-in super() function lets a subclass call its parent class's version of a method, which is especially useful inside __init__ when a subclass needs to run the parent's setup logic before adding its own.",
    altExplain: "A class inherits by listing the parent in parentheses: class Dog(Animal). No virtual/override keywords needed - just redefine the method in the subclass. super() calls the parent's version when needed.",
    visual: null,
    pycode: "class Animal:\n    def speak(self):\n        print(\"Some generic animal sound\")\n\nclass Dog(Animal):\n    def speak(self):\n        print(\"Woof!\")\n\na = Dog()\na.speak()",
    keyTakeaway: "Python needs no virtual/override keywords for method overriding - simply redefining a method with the same name in the subclass replaces the parent's version automatically.",
    commonMistake: "Forgetting to call super().__init__() in a subclass's constructor when the parent class's setup logic still needs to run.",
  },
  {
    title: "Exception handling",
    body: "Python's try/except mirrors C#'s try/catch, with slightly different keywords: a try block wraps risky code, except catches a specific exception type (or any exception if left unspecified, though catching specific types is better practice), and finally runs regardless of whether an exception occurred, just like in C#. Python raises exceptions with the raise keyword, and its exception type names are similarly descriptive - ValueError, TypeError, ZeroDivisionError - making it usually clear at a glance what actually went wrong.",
    altExplain: "try/except works like C#'s try/catch. except ValueError catches a specific error type. finally always runs afterward, error or not.",
    visual: null,
    pycode: "try:\n    result = 10 / int(\"0\")\n    print(result)\nexcept ZeroDivisionError:\n    print(\"Can't divide by zero!\")\nfinally:\n    print(\"Done trying.\")",
    refBox: {
      syntax: "try: / except ErrorType: / finally:",
      desc: "try wraps risky code; except catches a specific error type; finally always runs, error or not.",
      example: "try:\n    n = int(\"abc\")\nexcept ValueError:\n    print(\"not a number\")",
    },
    keyTakeaway: "Catching a specific exception type (like ValueError) rather than a bare except: is better practice - it avoids accidentally hiding an unrelated bug.",
    commonMistake: "Using a bare except: with no specific exception type, which silently catches and hides every possible error, including ones you didn't anticipate.",
    challenge: {
      prompt: "Try to convert the text \"abc\" to an int, catching the resulting ValueError with a friendly message.",
      hint: "int(\"abc\") raises a ValueError - except ValueError: ...",
      solution: "try:\n    n = int(\"abc\")\nexcept ValueError:\n    print(\"That wasn't a valid number.\")",
    },
  },
  {
    title: "List comprehensions",
    body: "A list comprehension is a compact, Python-specific way to build a new list by transforming or filtering an existing sequence, all in a single readable line: [n * 2 for n in numbers] doubles every number in a list, and [n for n in numbers if n > 10] keeps only the numbers greater than 10. This is functionally similar to LINQ's Select and Where in C#, condensed into one expression, and is generally considered more idiomatic Python than writing the equivalent multi-line for loop with .append() calls, once you're comfortable reading the syntax.",
    altExplain: "A list comprehension builds a new list in one line: [expression for item in list if condition]. It's Python's compact alternative to LINQ's Select/Where, or a manual loop with .append().",
    visual: null,
    pycode: "numbers = [3, 8, 15, 20, 4]\ndoubled = [n * 2 for n in numbers]\nbig_ones = [n for n in numbers if n > 10]\n\nprint(doubled)\nprint(big_ones)",
    keyTakeaway: "A list comprehension [expr for item in iterable if condition] combines transforming and filtering into one compact, readable line.",
    commonMistake: "Writing an overly complex, hard-to-read list comprehension with deeply nested logic, when a regular multi-line loop would actually be clearer.",
    challenge: {
      prompt: "Use a list comprehension to get only the even numbers from a list.",
      hint: "[n for n in numbers if n % 2 == 0]",
      solution: "numbers = [1, 2, 3, 4, 5, 6]\nevens = [n for n in numbers if n % 2 == 0]\nprint(evens)",
    },
  },
  {
    title: "Modules & the standard library",
    body: "Python ships with a large standard library of built-in modules covering common needs - math for mathematical functions, random for generating random numbers, datetime for working with dates and times - and importing one is as simple as writing import math at the top of a file, then accessing its contents with math.sqrt() or similar dot notation. Beyond the standard library, Python's package ecosystem (installed via pip, Python's package manager) is one of its biggest practical strengths, providing ready-made libraries for nearly anything - data analysis, web frameworks, machine learning - without needing to write that functionality from scratch.",
    altExplain: "import brings in a module's functionality, like import math for math.sqrt(). Python's standard library covers common needs, and pip installs additional packages beyond that.",
    visual: null,
    pycode: "import math\nimport random\n\nprint(math.sqrt(16))\nprint(random.randint(1, 10))",
    keyTakeaway: "The standard library (math, random, datetime, and more) is included with Python by default - no installation needed, just import and use.",
    commonMistake: "Writing custom code to solve a problem the standard library already solves well, like manual random number generation instead of using the random module.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "class Name: / def __init__(self, param): / self.x = x", desc: "__init__ is the constructor; self refers to the specific instance.", example: "class Dog:\n    def __init__(self, name):\n        self.name = name" },
      { syntax: "class Child(Parent): / super().method()", desc: "Inherits from a parent class - super() calls the parent's version of a method.", example: "class Dog(Animal):\n    def speak(self):\n        print(\"Woof!\")" },
      { syntax: "try: / except ErrorType: / finally:", desc: "Catches an error instead of crashing; finally always runs.", example: "try:\n    n = int(\"abc\")\nexcept ValueError:\n    pass" },
      { syntax: "[expr for item in iterable if cond]", desc: "List comprehension - builds a new list in one line.", example: "[n*2 for n in nums if n > 0]" },
      { syntax: "import module_name", desc: "Brings in a standard library or installed package's functionality.", example: "import math\nmath.sqrt(16)" },
    ],
  },
];

const PY_QUIZ_BANK = {
  basic: [
    { q: "What does Python use instead of curly braces to mark code blocks?", options: ["Parentheses", "Indentation", "Square brackets", "Semicolons"], correct: 1 },
    { q: "What does 7 / 2 evaluate to in Python 3?", options: ["3", "3.5", "4", "An error"], correct: 1 },
    { q: "Which prefix creates an f-string?", options: ["$", "f", "#", "%"], correct: 1 },
    { q: "input() always returns a...", options: ["int", "string", "bool", "float"], correct: 1 },
    { q: "Which symbol marks a comment in Python?", options: ["//", "#", "/*", "--"], correct: 1 },
  ],
  intermediate: [
    { q: "range(5) produces which numbers?", options: ["1 through 5", "0 through 5", "0 through 4", "1 through 4"], correct: 2 },
    { q: "Negative indexing like list[-1] refers to...", options: ["An error", "The first item", "The last item", "A random item"], correct: 2 },
    { q: "dictionary.get(key, default) is safer than direct indexing because it...", options: ["Is always faster", "Returns a default instead of raising an error for a missing key", "Only works with numbers", "Automatically adds the key"], correct: 1 },
    { q: "A default parameter value in a function definition makes that argument...", options: ["Required", "Optional when calling the function", "Always ignored", "A constant"], correct: 1 },
    { q: "A tuple differs from a list because it is...", options: ["Always empty", "Immutable (can't be changed after creation)", "Only for numbers", "Always sorted"], correct: 1 },
  ],
  advanced: [
    { q: "self in a Python method refers to...", options: ["The class itself", "The specific object instance", "A global variable", "Nothing, it's optional"], correct: 1 },
    { q: "To override a parent class's method in Python, you need...", options: ["The override keyword", "Just redefine the method with the same name", "The virtual keyword", "A special decorator, always"], correct: 1 },
    { q: "Which exception type would int(\"abc\") raise?", options: ["TypeError", "ValueError", "KeyError", "IndexError"], correct: 1 },
    { q: "A list comprehension like [n*2 for n in numbers] is similar to which C# concept?", options: ["A try/catch block", "LINQ's Select", "A class definition", "An exception"], correct: 1 },
    { q: "Python's standard library is...", options: ["Something you must install separately", "Included with Python by default", "Only for web development", "The same as pip packages"], correct: 1 },
  ],
};
