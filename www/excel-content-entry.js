// =========================================================================
// Excel - Entry Level
// =========================================================================
const XL_ENTRY_BASIC = [
  {
    title: "What is Excel & why it matters",
    body: "Excel is a spreadsheet application built around a grid of cells, where each cell can hold a number, text, or a formula that calculates something automatically from other cells. What makes spreadsheets genuinely powerful isn't storing data - a text file can do that - it's that a formula recalculates instantly the moment any cell it depends on changes, letting you build a living model instead of a static snapshot. This course uses a real formula engine, so every spreadsheet example you see can actually be edited and recalculated, not just read.",
    altExplain: "Excel is a grid of cells where formulas automatically recalculate when the numbers they depend on change - that's what makes it more than just a table of numbers.",
    visual: null,
    keyTakeaway: "The core power of a spreadsheet is automatic recalculation - change one number, and everything depending on it updates instantly.",
    commonMistake: "Treating a spreadsheet as just a place to type numbers, without using formulas to connect them - which throws away the main advantage spreadsheets have over a plain table.",
  },
  {
    title: "Cells, rows, and columns",
    body: "Every cell in a spreadsheet has an address made from its column letter and row number - A1 is the top-left cell, B2 is one column right and one row down. Columns run left to right, labeled A, B, C, and rows run top to bottom, labeled 1, 2, 3. This addressing system is what lets a formula refer precisely to another cell's value, like =A1+B1, rather than needing to know or repeat the actual number stored there. Try typing directly into the cells below - you're working with a real, live grid.",
    altExplain: "Every cell has an address, like A1 or B2, made from its column letter and row number. Formulas use these addresses to refer to other cells' values.",
    visual: null,
    sheet: [
      ["Item", "Price"],
      ["Coffee", "120"],
      ["Sandwich", "150"],
    ],
    keyTakeaway: "A cell's address (like B2) is how formulas refer to it - this is the foundation every formula in this course builds on.",
    commonMistake: "Confusing rows and columns when reading an address - the letter always comes first (column), then the number (row): B2, not 2B.",
  },
  {
    title: "Basic formulas: SUM, AVERAGE, COUNT",
    body: "Every formula in Excel starts with an equals sign, telling the spreadsheet 'calculate this, don't just display it as text.' SUM adds up a range of cells: =SUM(A1:A3) adds everything from A1 through A3. AVERAGE calculates the mean of a range the same way, and COUNT tells you how many cells in a range actually contain a number, ignoring empty or text cells. These three functions cover an enormous share of everyday spreadsheet work, and nearly every more advanced function builds on the same basic pattern: function name, then a range in parentheses.",
    altExplain: "Every formula starts with =. SUM adds a range of cells, AVERAGE finds the mean, COUNT tells you how many cells actually contain numbers.",
    visual: null,
    sheet: [
      ["10", "=SUM(A1:A3)"],
      ["20", "=AVERAGE(A1:A3)"],
      ["30", "=COUNT(A1:A3)"],
    ],
    refBox: {
      syntax: "=SUM(range) / =AVERAGE(range) / =COUNT(range)",
      desc: "Every formula starts with =. A range like A1:A3 means 'from A1 through A3, inclusive.'",
      example: "=SUM(A1:A3)",
    },
    keyTakeaway: "A range like A1:A3 means 'from A1 through A3, inclusive' - the colon is what defines a range rather than a single cell.",
    commonMistake: "Forgetting the equals sign at the start of a formula - without it, Excel treats what you typed as plain text instead of calculating anything.",
    challenge: {
      prompt: "Edit column A to have the values 5, 15, 25, and confirm the SUM, AVERAGE, and COUNT formulas update automatically.",
      hint: "Just type new numbers into the A column cells - the formulas in column B will recalculate on their own.",
      solution: "Type 5, 15, and 25 into A1, A2, and A3 - the formulas update instantly without needing to be rewritten.",
    },
  },
  {
    title: "Cell references: relative vs. absolute",
    body: "By default, a cell reference in a formula is relative - it describes a position relative to the formula's own cell, which is why copying a formula to a new location shifts its references automatically. Sometimes you need a reference that never shifts, no matter where the formula is copied - like a tax rate stored in one fixed cell that every other formula should point to. Adding dollar signs, like $A$1, makes a reference absolute, locking both the column and row so it stays fixed regardless of where the formula moves. Try changing the tax rate cell below and watch both formulas update, since both correctly reference it.",
    altExplain: "A normal reference (A1) shifts when you copy a formula elsewhere. An absolute reference ($A$1) stays locked to that exact cell no matter where the formula is copied.",
    visual: null,
    sheet: [
      ["Tax rate", "0.1"],
      ["Price", "Tax"],
      ["100", "=B3*$B$1"],
      ["200", "=A4*$B$1"],
    ],
    keyTakeaway: "Dollar signs ($A$1) lock a reference so it doesn't shift when a formula is copied elsewhere - essential for referencing one fixed value from many formulas.",
    commonMistake: "Forgetting to lock a reference meant to stay fixed (like a tax rate), so copying the formula elsewhere accidentally shifts it to point at the wrong cell.",
    challenge: {
      prompt: "Change the tax rate in B1 to 0.15 and confirm both tax calculations below update.",
      hint: "Just edit the value in the first row - both formulas reference it with $B$1.",
      solution: "Typing 0.15 into B1 updates both C3 and C4-equivalent cells automatically, since both lock onto B1.",
    },
  },
  {
    title: "More functions: MIN, MAX, ROUND",
    body: "MIN and MAX find the smallest and largest values in a range - =MAX(A1:A5) instantly tells you the highest number without manually scanning. ROUND controls how many decimal places a number displays as an actual calculated value (not just visually): =ROUND(A1, 2) rounds to 2 decimal places, while =ROUND(A1, 0) rounds to a whole number. These are especially useful together - calculating an average and then rounding it for a cleaner, more readable result is an extremely common combination.",
    altExplain: "MIN and MAX find the smallest and largest value in a range. ROUND controls decimal places in the actual calculated result, not just how it looks.",
    visual: null,
    sheet: [
      ["82", "=MAX(A1:A3)"],
      ["95", "=MIN(A1:A3)"],
      ["78", "=ROUND(AVERAGE(A1:A3), 1)"],
    ],
    keyTakeaway: "ROUND changes the actual stored calculated value, not just its visual display - important when that rounded result feeds into another formula.",
    commonMistake: "Confusing ROUND (which changes the actual value) with simply changing a cell's display formatting, which only affects how a number looks, not what it equals.",
    challenge: {
      prompt: "Find the average of 3 test scores and round it to the nearest whole number.",
      hint: "=ROUND(AVERAGE(range), 0)",
      solution: "With scores 88, 92, 79 in A1:A3, use =ROUND(AVERAGE(A1:A3), 0) to get a whole-number average.",
    },
  },
  {
    title: "The IF function",
    body: "IF tests a condition and returns one value if it's true, and another if it's false: =IF(A1 >= 60, \"Pass\", \"Fail\") checks whether A1 is at least 60, returning \"Pass\" or \"Fail\" accordingly. This is the spreadsheet equivalent of an if/else statement from a programming language, and it's the foundation for building spreadsheets that respond dynamically to changing data rather than requiring manual judgment calls for every row. IF can be combined with any comparison operator (>, <, >=, <=, =, <>) to build the exact condition you need.",
    altExplain: "IF tests a condition: =IF(condition, value_if_true, value_if_false). It's the spreadsheet version of if/else from programming.",
    visual: null,
    sheet: [
      ["Score", "Result"],
      ["75", "=IF(A2>=60,\"Pass\",\"Fail\")"],
      ["45", "=IF(A3>=60,\"Pass\",\"Fail\")"],
    ],
    refBox: {
      syntax: "=IF(condition, value_if_true, value_if_false)",
      desc: "Tests a condition and returns one value if true, another if false. Text results need quotation marks.",
      example: "=IF(A1>=18,\"Adult\",\"Minor\")",
    },
    keyTakeaway: "IF's three parts are always: the condition, what to return if true, what to return if false - in that exact order.",
    commonMistake: "Forgetting to wrap text results in quotation marks inside IF - =IF(A1>=60,Pass,Fail) causes an error, since Pass and Fail need quotes to be treated as text.",
    challenge: {
      prompt: "Write a formula that returns \"Adult\" if an age is 18 or over, otherwise \"Minor\".",
      hint: "=IF(A1>=18,\"Adult\",\"Minor\")",
      solution: "With an age in A1, use =IF(A1>=18,\"Adult\",\"Minor\") in another cell.",
    },
  },
  {
    title: "Sorting & filtering data",
    body: "Sorting rearranges rows based on a column's values - ascending (smallest/A-first) or descending (largest/Z-first) - without changing which values belong together in each row, since Excel moves entire rows together to keep records intact. Filtering temporarily hides rows that don't match a condition, letting you focus on a subset of data (like only 'Pending' orders) without deleting anything - the hidden rows are still there, just not currently displayed. Both are essential first steps before deeper analysis, since raw, unsorted, unfiltered data is often harder to spot patterns in than the same data organized meaningfully.",
    altExplain: "Sorting rearranges rows by a column's values, keeping each row intact. Filtering temporarily hides rows that don't match a condition, without deleting anything.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You sort a table of orders by date, and then filter to only show 'Pending' orders. What happens to the rows that don't match 'Pending'?",
      options: ["They are permanently deleted", "They are temporarily hidden, but still exist in the data", "They get moved to a new sheet", "Sorting and filtering can't be combined"],
      correct: 1,
      explanation: "Filtering only hides rows that don't match the current filter - the data isn't deleted or moved, and removing or changing the filter brings those rows back into view.",
    },
    keyTakeaway: "Filtering hides rows temporarily without deleting anything - it's always safe to filter, since the underlying data stays completely intact.",
    commonMistake: "Confusing a filtered-out row with a deleted one, and being surprised when clearing the filter brings 'missing' rows right back.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the formulas introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the formulas you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "=SUM(range) / =AVERAGE(range) / =COUNT(range)", desc: "Every formula starts with =. A range like A1:A3 means 'from A1 through A3, inclusive.'", example: "=SUM(A1:A3)" },
      { syntax: "$A$1 (absolute) vs A1 (relative)", desc: "A relative reference shifts when copied to a new cell; an absolute reference (with $) stays fixed.", example: "=A1*$B$1" },
      { syntax: "=MIN(range) / =MAX(range) / =ROUND(value, digits)", desc: "MIN/MAX find the smallest/largest value; ROUND rounds to a given number of decimal places.", example: "=ROUND(3.14159, 2)" },
      { syntax: "=IF(condition, value_if_true, value_if_false)", desc: "Tests a condition and returns one value if true, another if false.", example: "=IF(A1>=18,\"Adult\",\"Minor\")" },
      { syntax: "Sort & Filter (Data tab)", desc: "Filtering hides non-matching rows temporarily - the underlying data is never deleted.", example: "Clearing a filter brings hidden rows back." },
    ],
  },
];

const XL_ENTRY_INTERMEDIATE = [
  {
    title: "Text functions: CONCAT, LEFT, RIGHT, LEN",
    body: "CONCAT (or the older CONCATENATE) joins text from multiple cells together: =CONCAT(A1, \" \", B1) combines a first and last name with a space between them. LEFT and RIGHT extract a specific number of characters from the start or end of a text value - =LEFT(A1, 3) grabs the first 3 characters. LEN counts how many characters a text value contains, which is often used alongside LEFT/RIGHT to extract a dynamic-length piece of text rather than a fixed number of characters.",
    altExplain: "CONCAT joins text together. LEFT and RIGHT grab characters from the start or end of text. LEN counts how many characters a text value has.",
    visual: null,
    sheet: [
      ["First", "Last", "Full Name"],
      ["Ana", "Reyes", "=CONCAT(A2,\" \",B2)"],
      ["Marco", "Cruz", "=LEFT(B3,3)"],
    ],
    keyTakeaway: "LEN is often combined with LEFT/RIGHT to extract a piece of text of variable length, rather than always grabbing a fixed number of characters.",
    commonMistake: "Forgetting to include a space (\" \") when concatenating first and last names, resulting in text that reads 'AnaReyes' with no space.",
    challenge: {
      prompt: "Combine a first and last name with a space between them using CONCAT.",
      hint: "=CONCAT(A1,\" \",B1)",
      solution: "With \"Liza\" in A1 and \"Santos\" in B1, use =CONCAT(A1,\" \",B1) to get \"Liza Santos\".",
    },
  },
  {
    title: "Date & time functions",
    body: "TODAY() returns the current date, automatically updating every time the spreadsheet recalculates - useful for tracking things like 'days since' without manually updating a date each time. Dates in Excel are actually stored as numbers internally (counting days from a fixed starting point), which is exactly why subtracting one date from another gives you a meaningful number of days: =A2-A1 calculates the difference between two dates directly, no special date-math function required.",
    altExplain: "TODAY() gives the current date, always up to date. Dates are stored as numbers internally, so subtracting one date from another directly gives the number of days between them.",
    visual: null,
    sheet: [
      ["Order Date", "Days Ago"],
      ["2026-07-01", "=TODAY()-A2"],
    ],
    keyTakeaway: "Subtracting one date from another works directly in Excel, giving a day count - because dates are internally stored as plain numbers.",
    commonMistake: "Trying to manually calculate date differences by parsing text instead of just subtracting the date cells directly, which Excel already handles correctly.",
  },
  {
    title: "COUNTIF & SUMIF",
    body: "COUNTIF counts how many cells in a range meet a specific condition: =COUNTIF(A1:A10, \"Pending\") counts how many cells equal 'Pending'. SUMIF works the same way but adds up a corresponding range instead of counting: =SUMIF(A1:A10, \"Pending\", B1:B10) sums the values in column B, but only for rows where column A equals 'Pending'. These two functions are the spreadsheet equivalent of a WHERE clause combined with COUNT or SUM in SQL, letting you answer conditional questions without manually filtering first.",
    altExplain: "COUNTIF counts cells matching a condition. SUMIF adds up values from one range, but only where a different range matches a condition - like a WHERE clause combined with COUNT or SUM.",
    visual: null,
    sheet: [
      ["Status", "Amount"],
      ["Paid", "1200"],
      ["Pending", "800"],
      ["Paid", "1500"],
      ["=COUNTIF(A2:A4,\"Paid\")", "=SUMIF(A2:A4,\"Paid\",B2:B4)"],
    ],
    refBox: {
      syntax: "=COUNTIF(range, condition) / =SUMIF(range, condition, sum_range)",
      desc: "COUNTIF counts matching cells; SUMIF sums a different range wherever the condition matches.",
      example: "=SUMIF(A2:A10,\"Paid\",B2:B10)",
    },
    keyTakeaway: "SUMIF's three arguments are: the range to check the condition against, the condition itself, and the range to actually sum - in that order.",
    commonMistake: "Mixing up SUMIF's argument order - the range being summed comes last, not first, which is easy to get backward at first.",
    challenge: {
      prompt: "Count how many orders have status \"Pending\".",
      hint: "=COUNTIF(range, \"Pending\")",
      solution: "With statuses in A2:A4, use =COUNTIF(A2:A4,\"Pending\") to count matching rows.",
    },
  },
  {
    title: "VLOOKUP basics",
    body: "VLOOKUP looks up a value in the first column of a table and returns a corresponding value from another column in the same row: =VLOOKUP(\"Ana\", A2:C5, 3, FALSE) searches for \"Ana\" in the first column of that range, and returns the value from the 3rd column of that matching row. The FALSE at the end requests an exact match, which is almost always what you want - leaving it as TRUE (or omitting it) allows approximate matches, which can silently return the wrong row if your data isn't sorted a specific way.",
    altExplain: "VLOOKUP searches for a value in a table's first column and returns a value from another column in that matching row. Always use FALSE for an exact match.",
    visual: null,
    sheet: [
      ["Name", "Dept", "Salary"],
      ["Ana", "Sales", "32000"],
      ["Marco", "Engineering", "45000"],
      ["Result:", "=VLOOKUP(\"Marco\",A2:C3,3,FALSE)", ""],
    ],
    refBox: {
      syntax: "=VLOOKUP(lookup_value, table_range, col_index, FALSE)",
      desc: "Searches a table's first column and returns a value from another column in that row. Use FALSE for exact match.",
      example: "=VLOOKUP(\"Ana\",A2:C10,2,FALSE)",
    },
    keyTakeaway: "Always use FALSE as VLOOKUP's last argument for an exact match - leaving it as TRUE can silently return the wrong row.",
    commonMistake: "Omitting the FALSE argument or leaving it as TRUE, which allows approximate matching and can quietly return an incorrect result with no error shown.",
    challenge: {
      prompt: "Look up Ana's department using VLOOKUP.",
      hint: "=VLOOKUP(\"Ana\", table_range, 2, FALSE) - department is the 2nd column.",
      solution: "Using the same table, =VLOOKUP(\"Ana\",A2:C3,2,FALSE) returns \"Sales\".",
    },
  },
  {
    title: "Basic charts",
    body: "A chart turns a table of numbers into a visual comparison - a bar chart for comparing categories, a line chart for showing change over time - built directly from a selected range of cells, and automatically updating whenever the underlying data changes, exactly like a formula does. Choosing the right chart type follows the same principles covered in the Data Analytics course: bars for comparing distinct categories, lines for trends across a continuous sequence like time, and pie charts used sparingly, since they become hard to read with more than a handful of slices.",
    altExplain: "A chart visualizes a range of cells, updating automatically when the data changes - same chart-type principles as the Data Analytics course: bars for comparison, lines for trends over time.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You have monthly sales totals for the last 12 months and want to show the trend. Which chart type fits best?",
      options: ["Pie chart", "Line chart", "A single number with no chart", "There's no good way to chart this"],
      correct: 1,
      explanation: "A line chart is built for showing change across a continuous sequence like time, making the trend's direction immediately visible - the same principle covered in the Data Analytics course.",
    },
    keyTakeaway: "An Excel chart is linked to its source range - editing the underlying cells updates the chart automatically, just like a formula recalculates.",
    commonMistake: "Manually redrawing or recreating a chart every time the underlying data changes, instead of relying on the chart's automatic link to its source range.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the formulas introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the formulas you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "=CONCAT(text1, text2) / =LEFT(text, n) / =RIGHT(text, n) / =LEN(text)", desc: "Text functions - join strings, grab characters from either end, or measure length.", example: "=LEFT(\"Hello\",3)  // \"Hel\"" },
      { syntax: "=TODAY() / =DATEDIF(start, end, \"unit\")", desc: "Date functions - get the current date, or calculate the difference between two dates.", example: "=DATEDIF(A1,TODAY(),\"d\")" },
      { syntax: "=COUNTIF(range, condition) / =SUMIF(range, condition, sum_range)", desc: "COUNTIF counts matching cells; SUMIF sums a different range wherever the condition matches.", example: "=SUMIF(A2:A10,\"Paid\",B2:B10)" },
      { syntax: "=VLOOKUP(lookup_value, table_range, col_index, FALSE)", desc: "Searches a table's first column, returns a value from another column in that row.", example: "=VLOOKUP(\"Ana\",A2:C10,2,FALSE)" },
      { syntax: "Insert > Chart (linked to source range)", desc: "A chart stays linked to its data - editing cells updates the chart automatically.", example: "No manual redrawing needed after data changes." },
    ],
  },
];

const XL_ENTRY_ADVANCED = [
  {
    title: "Nested IF & AND/OR",
    body: "A nested IF places one IF function inside another, handling more than two possible outcomes: =IF(A1>=90,\"A\",IF(A1>=80,\"B\",\"C\")) checks for a grade of 90+ first, and if that's false, checks the next condition inside the nested IF. AND and OR combine multiple conditions into one: AND requires every condition to be true, while OR requires just one - =IF(AND(A1>=60,B1=\"Complete\"),\"Pass\",\"Fail\") only returns 'Pass' if both conditions hold at once.",
    altExplain: "A nested IF puts one IF inside another, for more than two outcomes. AND requires every condition to be true; OR requires just one - both combine multiple conditions into a single IF.",
    visual: null,
    sheet: [
      ["Score", "Grade"],
      ["95", "=IF(A2>=90,\"A\",IF(A2>=80,\"B\",\"C\"))"],
      ["82", "=IF(A3>=90,\"A\",IF(A3>=80,\"B\",\"C\"))"],
      ["65", "=IF(A4>=90,\"A\",IF(A4>=80,\"B\",\"C\"))"],
    ],
    refBox: {
      syntax: "=IF(cond, val, IF(cond2, val2, val3)) / =AND(c1,c2) / =OR(c1,c2)",
      desc: "Nested IF handles more than two outcomes. AND needs every condition true; OR needs just one.",
      example: "=IF(AND(A1>=60,B1=\"Complete\"),\"Pass\",\"Fail\")",
    },
    keyTakeaway: "In a nested IF, conditions are checked in order - the first one that's true wins, even if a later one would also technically match.",
    commonMistake: "Nesting too many IFs deep, making a formula hard to read - if you need many outcomes, a lookup table is often clearer than a long chain of nested IFs.",
    challenge: {
      prompt: "Write a nested IF that returns \"High\", \"Medium\", or \"Low\" based on a score: 80+ is High, 50-79 is Medium, below 50 is Low.",
      hint: "=IF(A1>=80,\"High\",IF(A1>=50,\"Medium\",\"Low\"))",
      solution: "With a score in A1, use =IF(A1>=80,\"High\",IF(A1>=50,\"Medium\",\"Low\")).",
    },
  },
  {
    title: "INDEX & MATCH",
    body: "MATCH finds the position of a value within a range: =MATCH(\"Marco\", A2:A4, 0) returns which row Marco is in, relative to the start of that range. INDEX returns the value at a given position within a range: =INDEX(C2:C4, 2) returns the 2nd value in that range. Combined, INDEX and MATCH replicate what VLOOKUP does, but more flexibly - unlike VLOOKUP, they can look leftward as well as rightward, and don't break if columns get rearranged, which is why many experienced spreadsheet users prefer this combination over VLOOKUP for anything beyond the simplest lookups.",
    altExplain: "MATCH finds a value's position in a range. INDEX returns a value at a given position. Combined, they work like VLOOKUP but more flexibly - they can look leftward and survive column reordering.",
    visual: null,
    sheet: [
      ["Name", "Dept", "Salary"],
      ["Ana", "Sales", "32000"],
      ["Marco", "Engineering", "45000"],
      ["Result:", "=INDEX(C2:C3,MATCH(\"Marco\",A2:A3,0))", ""],
    ],
    refBox: {
      syntax: "=INDEX(return_range, MATCH(lookup_value, lookup_range, 0))",
      desc: "MATCH finds a position; INDEX returns the value at that position. Together, a flexible VLOOKUP alternative.",
      example: "=INDEX(C2:C10,MATCH(\"Ana\",A2:A10,0))",
    },
    keyTakeaway: "INDEX and MATCH combined can look leftward (unlike VLOOKUP) and keep working even if columns get reordered - a genuine advantage for more complex spreadsheets.",
    commonMistake: "Assuming INDEX/MATCH is only useful for exact VLOOKUP replacement - it's also the standard fix when you need to look up a value to the left of your search column, which VLOOKUP can't do at all.",
  },
  {
    title: "Data validation",
    body: "Data validation restricts what can be typed into a cell - limiting a column to only whole numbers, only dates within a range, or only values from a specific dropdown list. This prevents a whole category of data entry mistakes at the source, like someone accidentally typing 'Febuary' instead of selecting 'February' from an approved list, which would otherwise silently break any formula or lookup depending on that value matching exactly. It's a proactive way of enforcing the same kind of data quality standards covered in the Data Analytics course's data cleaning lesson.",
    altExplain: "Data validation restricts what can be typed into a cell (a number range, a dropdown list), preventing typos and inconsistent entries before they ever happen.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A column should only ever contain 'Sales', 'Engineering', or 'Marketing'. What Excel feature best prevents typos like 'sales' or 'Enginering' from being entered?",
      options: ["Conditional formatting", "Data validation with a dropdown list", "A VLOOKUP formula", "Sorting the column alphabetically"],
      correct: 1,
      explanation: "Data validation with a dropdown list restricts entry to only the approved values, preventing typos and inconsistent casing before they're ever typed in, rather than trying to catch them afterward.",
    },
    keyTakeaway: "Data validation prevents bad data at the moment of entry - a proactive fix, compared to cleaning up inconsistent values after the fact.",
    commonMistake: "Relying entirely on manually checking for typos after data entry, instead of using data validation to prevent invalid entries from being typed in the first place.",
  },
  {
    title: "Conditional formatting",
    body: "Conditional formatting automatically changes a cell's visual appearance - background color, text color, icons - based on its value, without needing a separate formula cell to display the result. Highlighting every value over a certain threshold in red, or every negative number in a different color, makes patterns visually obvious at a glance, in a way that reading raw numbers in a table doesn't. Unlike a formula, conditional formatting doesn't change the underlying value at all - it only changes how that value is displayed based on rules you define.",
    altExplain: "Conditional formatting automatically colors or styles a cell based on its value - like highlighting numbers over a threshold in red - without changing the actual value at all.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You apply conditional formatting to highlight sales figures below target in red. Does this change the actual stored value in those cells?",
      options: ["Yes, it converts them to text", "No, it only changes how the value is displayed, not the value itself", "Yes, it rounds them automatically", "It deletes values below target"],
      correct: 1,
      explanation: "Conditional formatting is purely visual - it changes color, icons, or style based on a rule, but the actual numeric value stored in the cell never changes.",
    },
    keyTakeaway: "Conditional formatting only affects visual display, never the underlying value - a formula referencing that cell still sees the original, unformatted number.",
    commonMistake: "Assuming conditional formatting changes a value (like rounding or converting it) rather than purely its visual appearance.",
  },
  {
    title: "Introduction to PivotTables",
    body: "A PivotTable summarizes a large table of raw data by letting you drag fields into 'Rows,' 'Columns,' and 'Values' areas, automatically grouping and aggregating data without writing a single formula - conceptually the exact same idea as the pivot builder covered in the Data Analytics course, just with Excel's own drag-and-drop interface instead of dropdown menus. Choosing a category to group by, a number to summarize, and an aggregation method (sum, average, count) produces an instant summary table that would otherwise require several SUMIF or COUNTIF formulas to replicate manually.",
    altExplain: "A PivotTable summarizes raw data by dragging fields into Rows/Columns/Values areas - the same pivot-table concept from the Data Analytics course, with Excel's own drag-and-drop interface.",
    visual: null,
    widget: "pivot-builder",
    keyTakeaway: "A PivotTable can replicate what would otherwise take several separate SUMIF or COUNTIF formulas, built through a drag-and-drop interface instead of manual formula-writing.",
    commonMistake: "Manually writing many SUMIF formulas to build a summary table, when a single PivotTable would produce the same result faster and more flexibly.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the formulas introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the formulas you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "=IF(cond, val, IF(cond2, val2, val3)) / =AND(c1,c2) / =OR(c1,c2)", desc: "Nested IF handles more than two outcomes. AND needs every condition true; OR needs just one.", example: "=IF(AND(A1>=60,B1=\"Complete\"),\"Pass\",\"Fail\")" },
      { syntax: "=INDEX(return_range, MATCH(lookup_value, lookup_range, 0))", desc: "MATCH finds a position; INDEX returns the value there - a flexible VLOOKUP alternative.", example: "=INDEX(C2:C10,MATCH(\"Ana\",A2:A10,0))" },
      { syntax: "Data > Data Validation", desc: "Restricts what can be typed into a cell - numbers only, dates in range, or a dropdown list.", example: "Prevents typos like \"Febuary\" from breaking a lookup." },
      { syntax: "Home > Conditional Formatting", desc: "Automatically styles cells based on their value - highlighting outliers or thresholds visually.", example: "Highlight cells > 90 in green." },
      { syntax: "Insert > PivotTable", desc: "Summarizes raw data by dragging fields into Rows/Columns/Values - no formulas needed.", example: "Replaces several SUMIF formulas with one drag-and-drop table." },
    ],
  },
];

const XL_QUIZ_BANK = {
  basic: [
    { q: "Every formula in Excel must start with...", options: ["A letter", "An equals sign (=)", "A number", "A colon"], correct: 1 },
    { q: "A1:A3 refers to...", options: ["Just cell A1", "Cells A1 through A3", "Column A only", "Row 1 through 3 in every column"], correct: 1 },
    { q: "What does $A$1 (with dollar signs) do to a reference?", options: ["Nothing different from A1", "Locks it so it doesn't shift when the formula is copied", "Converts it to currency", "Deletes the reference"], correct: 1 },
    { q: "ROUND(A1, 2) changes...", options: ["Only how the number displays", "The actual calculated value to 2 decimal places", "The cell's color", "Nothing unless you press Enter twice"], correct: 1 },
    { q: "Filtering a table...", options: ["Permanently deletes non-matching rows", "Temporarily hides non-matching rows without deleting them", "Only works on numbers", "Sorts the data automatically"], correct: 1 },
  ],
  intermediate: [
    { q: "CONCAT is used to...", options: ["Delete text", "Join text from multiple cells together", "Count characters", "Sort text alphabetically"], correct: 1 },
    { q: "Why can you subtract one date from another directly in Excel?", options: ["You can't, it causes an error", "Dates are stored internally as numbers", "Dates are stored as special text", "Only TODAY() supports subtraction"], correct: 1 },
    { q: "SUMIF's three arguments are, in order...", options: ["Sum range, condition, criteria range", "Criteria range, condition, sum range", "Condition, sum range, criteria range", "There's only two arguments"], correct: 1 },
    { q: "In VLOOKUP, what does setting the last argument to FALSE do?", options: ["Disables the lookup entirely", "Requires an exact match", "Allows approximate matches", "Searches in reverse order"], correct: 1 },
    { q: "An Excel chart built from a range of cells...", options: ["Must be manually redrawn if data changes", "Updates automatically when the source data changes", "Can only show one data series", "Cannot be a line chart"], correct: 1 },
  ],
  advanced: [
    { q: "In a nested IF, conditions are evaluated...", options: ["All at once, randomly", "In order, stopping at the first true condition", "Only the last condition matters", "Alphabetically"], correct: 1 },
    { q: "A key advantage of INDEX/MATCH over VLOOKUP is...", options: ["It's the exact same thing", "It can look leftward and survives column reordering", "It only works with numbers", "It requires no arguments"], correct: 1 },
    { q: "Data validation is best used to...", options: ["Format cells with color", "Restrict what can be typed into a cell, preventing bad entries", "Create charts automatically", "Speed up calculations"], correct: 1 },
    { q: "Conditional formatting changes...", options: ["The actual stored value", "Only the visual display of a cell based on its value", "The formula itself", "The cell's data validation rules"], correct: 1 },
    { q: "A PivotTable is best described as...", options: ["A type of chart only", "A drag-and-drop tool for summarizing raw data without writing formulas", "A way to validate data entry", "A formula for text manipulation"], correct: 1 },
  ],
};
