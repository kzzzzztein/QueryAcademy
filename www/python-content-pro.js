// =========================================================================
// Python - Professional Level
// =========================================================================
const PY_PRO_BASIC = [
  {
    title: "String formatting in depth",
    body: "Beyond basic f-strings, Python offers precise formatting control for numbers inside them: f\"{price:.2f}\" formats a number to exactly 2 decimal places, useful for currency, and f\"{number:,}\" adds thousands separators for readability. The older .format() method and %-style formatting still appear in existing codebases, but f-strings are the modern, generally preferred approach since Python 3.6, since they're both more concise and more readable than the alternatives once you're used to the syntax.",
    altExplain: "f-strings support formatting specs like {price:.2f} for 2 decimal places, or {number:,} for thousands separators - handy for displaying currency or large numbers cleanly.",
    visual: null,
    pycode: "price = 1234.5\nprint(f\"{price:.2f}\")\nprint(f\"{price:,.2f}\")",
    keyTakeaway: "f\"{value:.2f}\" is the standard way to format a number to exactly 2 decimal places - extremely common for displaying money.",
    commonMistake: "Manually rounding and converting numbers to strings for display, instead of using an f-string format specifier that does it in one step.",
  },
  {
    title: "Working with dates & times",
    body: "The datetime module provides tools for working with dates and times, similar in purpose to date functions in SQL or C#. datetime.now() gets the current date and time, and dates support subtraction directly, returning a timedelta object representing the difference - useful for calculating someone's age, or how many days until a deadline. Formatting a date into a specific text layout uses .strftime() with format codes like %Y for a 4-digit year and %m for a 2-digit month, echoing the same style of codes used in SQL's strftime function from earlier courses.",
    altExplain: "datetime.now() gets the current date/time. Subtracting two dates gives you the difference. .strftime() formats a date into text using codes like %Y for year.",
    visual: null,
    pycode: "from datetime import datetime\n\nnow = datetime.now()\nprint(now.strftime(\"%Y-%m-%d\"))\n\nbirthday = datetime(2000, 1, 1)\ndifference = now - birthday\nprint(difference.days)",
    keyTakeaway: "Subtracting two datetime objects gives a timedelta, whose .days attribute tells you the difference in whole days.",
    commonMistake: "Trying to format a date by manually slicing its string representation instead of using .strftime() with proper format codes.",
  },
  {
    title: "Sets & set operations",
    body: "Beyond basic uniqueness, sets support mathematical set operations directly: union (|) combines two sets, intersection (&) finds values common to both, and difference (-) finds values in one set but not the other. These map directly onto set theory concepts and are genuinely useful for comparison tasks - finding which customers appear in both this month's and last month's active list, for example, is a one-line intersection once both groups are represented as sets.",
    altExplain: "Sets support union (|), intersection (&), and difference (-) - direct set-theory operations, useful for comparing two groups of unique values.",
    visual: null,
    pycode: "team_a = {\"Ana\", \"Marco\", \"Liza\"}\nteam_b = {\"Marco\", \"Pia\", \"Noel\"}\n\nprint(team_a & team_b)  # intersection\nprint(team_a | team_b)  # union\nprint(team_a - team_b)  # difference",
    keyTakeaway: "Set intersection (&) is a fast, one-line way to find values common to two groups - much simpler than manually looping and comparing.",
    commonMistake: "Manually writing nested loops to find common elements between two lists, when converting both to sets and using & does it in one line.",
  },
  {
    title: "Working with files",
    body: "Python's with statement, combined with open(), is the standard way to work with files: with open(\"file.txt\") as f: opens a file and automatically closes it when the block finishes, even if an error occurs inside - avoiding a common bug where a forgotten close() call leaves a file handle open. f.read() reads an entire file's contents as one string, while looping with for line in f: reads it line by line, which is more memory-efficient for large files. Writing works similarly, using open(\"file.txt\", \"w\") to open in write mode.",
    altExplain: "with open(...) as f: opens a file and closes it automatically when done, even if an error happens. This avoids forgetting to close a file, a common bug.",
    visual: null,
    pycode: "with open(\"notes.txt\", \"w\") as f:\n    f.write(\"Hello from Python!\\n\")\n\nwith open(\"notes.txt\") as f:\n    content = f.read()\n    print(content)",
    refBox: {
      syntax: "with open(path) as f: / f.read() / f.write(text)",
      desc: "Opens a file and closes it automatically when the block ends, even if an error occurs.",
      example: "with open(\"data.txt\") as f:\n    content = f.read()",
    },
    keyTakeaway: "The with statement guarantees a file gets closed automatically, even if an error occurs inside the block - always prefer it over manual open()/close() calls.",
    commonMistake: "Opening a file without with and forgetting to call .close() afterward, which can leave the file locked or lead to data not being fully written.",
  },
  {
    title: "Working with JSON",
    body: "JSON (JavaScript Object Notation) is a widely-used text format for structured data, and Python's built-in json module converts smoothly between JSON text and Python's own dictionaries and lists. json.dumps() converts a Python dictionary into a JSON-formatted string, and json.loads() does the reverse, parsing a JSON string back into a Python dictionary. This is especially relevant since JSON is the standard format most web APIs use to send and receive data, making the json module one of the most frequently used standard library tools in real-world Python work.",
    altExplain: "JSON is a common text format for structured data, especially from web APIs. json.dumps() converts Python data to JSON text; json.loads() converts JSON text back into Python data.",
    visual: null,
    pycode: "import json\n\ndata = {\"name\": \"Ana\", \"age\": 28}\njson_text = json.dumps(data)\nprint(json_text)\n\nparsed = json.loads(json_text)\nprint(parsed[\"name\"])",
    refBox: {
      syntax: "json.dumps(obj) / json.loads(text)",
      desc: "dumps converts Python data to JSON text; loads converts JSON text back to Python data.",
      example: "json.dumps({\"a\": 1})  # '{\"a\": 1}'",
    },
    keyTakeaway: "json.dumps() goes from Python data to JSON text; json.loads() goes from JSON text back to Python data - the two directions are easy to mix up by name alone.",
    commonMistake: "Confusing dumps (Python to JSON text) with loads (JSON text to Python) - the similar names make this an easy slip.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "f\"{value:.2f}\" / f\"{value:>10}\"", desc: "Format specifiers inside an f-string - decimal places, alignment, and more.", example: "f\"{3.14159:.2f}\"  # '3.14'" },
      { syntax: "datetime.now() / timedelta(days=n)", desc: "Gets the current date/time; timedelta represents a duration for date math.", example: "datetime.now() - birthday" },
      { syntax: "set([1, 2, 2]) / a & b / a | b", desc: "A set holds unique values; & is intersection, | is union.", example: "{1,2} & {2,3}  # {2}" },
      { syntax: "with open(path) as f: / f.read() / f.write(text)", desc: "Opens a file, closing it automatically when the block ends.", example: "with open(\"data.txt\") as f:\n    content = f.read()" },
      { syntax: "json.dumps(obj) / json.loads(text)", desc: "Converts between Python data and JSON text.", example: "json.dumps({\"a\": 1})" },
    ],
  },
];

const PY_PRO_INTERMEDIATE = [
  {
    title: "Lambda functions",
    body: "Python's lambda creates a small, unnamed function inline, similar in purpose to C#'s lambda expressions: lambda x: x * 2 is a function that doubles its input, usable anywhere a function is expected without a separate def statement. Lambdas are most commonly used as a short argument to another function, like the key parameter of sorted(), which controls what value each item is compared by during sorting - sorted(people, key=lambda p: p[\"age\"]) sorts a list of dictionaries by their age value.",
    altExplain: "lambda x: expression creates a tiny unnamed function inline, most often used as an argument to another function - like telling sorted() what to sort by.",
    visual: null,
    pycode: "people = [{\"name\": \"Ana\", \"age\": 28}, {\"name\": \"Marco\", \"age\": 34}]\nsorted_people = sorted(people, key=lambda p: p[\"age\"])\n\nfor p in sorted_people:\n    print(p[\"name\"])",
    refBox: {
      syntax: "lambda params: expression",
      desc: "Creates a small unnamed function inline - commonly used as a one-off argument, like sorted()'s key.",
      example: "sorted(people, key=lambda p: p[\"age\"])",
    },
    keyTakeaway: "A lambda is a shorthand for a tiny function, most useful as a one-off argument (like sorted()'s key) rather than for complex logic.",
    commonMistake: "Writing an overly complex, hard-to-read lambda with multiple conditions, when a properly named def function would be much clearer.",
  },
  {
    title: "Map, filter, and functional tools",
    body: "map() applies a function to every item in a sequence, and filter() keeps only items where a function returns true - conceptually similar to LINQ's Select and Where in C#, or Python's own list comprehensions from earlier. map(lambda n: n * 2, numbers) doubles every number, and filter(lambda n: n > 10, numbers) keeps only values above 10; both return a special iterator that's usually converted to a list with list(...) to actually see the results. Many Python developers prefer list comprehensions over map/filter for simple cases, since they're often considered more readable, but map and filter remain common, especially when the transformation is already a named function.",
    altExplain: "map() transforms every item in a sequence; filter() keeps only items matching a condition - similar to LINQ's Select/Where. List comprehensions often do the same job more readably.",
    visual: null,
    pycode: "numbers = [3, 8, 15, 20, 4]\n\ndoubled = list(map(lambda n: n * 2, numbers))\nbig_ones = list(filter(lambda n: n > 10, numbers))\n\nprint(doubled)\nprint(big_ones)",
    keyTakeaway: "map() and filter() return iterators, not lists directly - wrap the result in list(...) if you need to see or reuse the actual values.",
    commonMistake: "Forgetting to wrap map()/filter() results in list(), then being confused when printing shows a generic iterator object instead of the actual values.",
  },
  {
    title: "Decorators",
    body: "A decorator is a function that wraps another function to add behavior before or after it runs, without modifying the original function's own code - written with an @ symbol directly above a function definition. @timer above a function might measure and print how long that function took to run, every time it's called, without that timing logic being mixed into the function's actual business logic. This is a distinctly Python idiom, commonly used for logging, timing, access control, and caching, and it works by having the decorator function accept the original function as an argument and return a new, wrapped version of it.",
    altExplain: "A decorator (written with @) wraps a function to add extra behavior - like timing or logging - without changing the function's own code. Common for cross-cutting concerns.",
    visual: null,
    pycode: "def shout(func):\n    def wrapper():\n        result = func()\n        return result.upper()\n    return wrapper\n\n@shout\ndef greet():\n    return \"hello\"\n\nprint(greet())",
    refBox: {
      syntax: "@decorator_name / def func(): ...",
      desc: "Wraps a function to add behavior before/after it runs, without changing its own code.",
      example: "@timer\ndef slow_task():\n    ...",
    },
    keyTakeaway: "A decorator receives the original function and returns a new function that wraps it - the @decorator_name syntax is just a shorthand for this wrapping.",
    commonMistake: "Writing a decorator that forgets to return the wrapper function, which silently breaks the decorated function (it ends up returning None).",
  },
  {
    title: "Generators & yield",
    body: "A generator function looks like a regular function but uses yield instead of return, producing a sequence of values one at a time, on demand, rather than computing and storing the entire sequence in memory all at once. This matters enormously for large or even infinite sequences - a generator that yields numbers one by one can represent a sequence far too large to fit in memory as an actual list, since each value is only produced right when it's needed. Calling a generator function doesn't run its code immediately; it returns a generator object, and the function's body only actually executes as you iterate over it, pausing at each yield until the next value is requested.",
    altExplain: "A generator uses yield instead of return, producing values one at a time on demand instead of building a whole list in memory at once - crucial for very large sequences.",
    visual: null,
    pycode: "def count_up_to(n):\n    i = 1\n    while i <= n:\n        yield i\n        i += 1\n\nfor number in count_up_to(5):\n    print(number)",
    keyTakeaway: "A generator produces values lazily, one at a time, as they're requested - this uses far less memory than building a full list upfront for large sequences.",
    commonMistake: "Using a regular list for a very large or unbounded sequence when a generator would produce the same values without holding them all in memory at once.",
  },
  {
    title: "Working with APIs (requests basics)",
    body: "The popular third-party requests library (installed via pip, not part of the standard library) makes calling web APIs straightforward: requests.get(url) sends a request and returns a response object, and .json() on that response parses a JSON reply directly into a Python dictionary. This combination - requests for the HTTP call, json handling built directly into requests' response object - is the standard way Python programs talk to web services, whether that's fetching weather data, posting to a service, or integrating with virtually any modern API.",
    altExplain: "The requests library (a popular third-party package) makes calling web APIs simple: requests.get(url) fetches data, and .json() parses a JSON response into a Python dictionary.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Why is requests considered a 'third-party' library rather than part of Python's standard library?",
      options: ["It doesn't actually work with Python", "It must be installed separately via pip, unlike modules like math or datetime", "It only works with one specific website", "There's no real difference between standard and third-party libraries"],
      correct: 1,
      explanation: "The standard library ships with Python automatically. Third-party libraries like requests need to be installed separately (typically with pip install requests) before they can be imported and used.",
    },
    keyTakeaway: "requests needs to be installed via pip before use - unlike modules like math or json, it isn't bundled with Python by default.",
    commonMistake: "Assuming every useful Python library is already available without installation, when many of the most popular ones (requests, pandas, numpy) are third-party packages requiring pip install first.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "lambda params: expression", desc: "A small unnamed function inline - commonly used as a one-off argument.", example: "sorted(people, key=lambda p: p[\"age\"])" },
      { syntax: "map(fn, iterable) / filter(fn, iterable)", desc: "map transforms every item; filter keeps only matching items. Both return an iterator.", example: "list(map(lambda n: n*2, nums))" },
      { syntax: "@decorator_name / def func(): ...", desc: "Wraps a function to add behavior before/after it runs.", example: "@timer\ndef slow_task():\n    ..." },
      { syntax: "def gen(): / yield value", desc: "A generator function - produces values lazily, one at a time.", example: "def countdown(n):\n    while n > 0:\n        yield n\n        n -= 1" },
      { syntax: "requests.get(url).json()", desc: "Fetches a URL and parses its JSON response into a Python dictionary.", example: "requests.get(api_url).json()" },
    ],
  },
];

const PY_PRO_ADVANCED = [
  {
    title: "Args and kwargs",
    body: "*args lets a function accept any number of positional arguments, collected into a tuple inside the function, and **kwargs does the same for keyword arguments, collected into a dictionary. def total(*args): return sum(args) can be called as total(1, 2), total(1, 2, 3), or with any other number of arguments, since args simply captures whatever was passed. This flexibility is especially useful for writing wrapper functions (like decorators) that need to pass through an arbitrary set of arguments to another function without knowing in advance exactly what that function's signature looks like.",
    altExplain: "*args collects any number of extra positional arguments into a tuple. **kwargs does the same for keyword arguments, into a dictionary. Useful for functions that need to accept a flexible number of inputs.",
    visual: null,
    pycode: "def total(*args):\n    return sum(args)\n\ndef describe(**kwargs):\n    for key, value in kwargs.items():\n        print(f\"{key}: {value}\")\n\nprint(total(1, 2, 3, 4))\ndescribe(name=\"Ana\", age=28)",
    refBox: {
      syntax: "def func(*args, **kwargs):",
      desc: "*args collects extra positional args into a tuple; **kwargs collects keyword args into a dict.",
      example: "def total(*args):\n    return sum(args)\ntotal(1, 2, 3)",
    },
    keyTakeaway: "*args becomes a tuple of positional arguments inside the function; **kwargs becomes a dictionary of keyword arguments - both let a function accept a flexible number of inputs.",
    commonMistake: "Overusing *args and **kwargs for functions with a small, well-defined set of parameters, which makes the function's actual expected inputs harder to see at a glance.",
  },
  {
    title: "Context managers",
    body: "The with statement you used for file handling is actually a general Python feature called a context manager, and you can build your own by defining a class with __enter__ and __exit__ methods - __enter__ runs when entering the with block, and __exit__ runs when leaving it, even if an exception occurred inside. This pattern generalizes the 'guaranteed setup and cleanup' idea beyond just files - database connections, network sockets, and locks in multithreaded code all commonly use custom context managers to ensure resources are properly released no matter what happens inside the block.",
    altExplain: "with works with any class defining __enter__ and __exit__ methods - not just files. This 'guaranteed setup and cleanup' pattern is used for database connections, locks, and other resources needing cleanup.",
    visual: null,
    pycode: "class Timer:\n    def __enter__(self):\n        print(\"Starting...\")\n        return self\n\n    def __exit__(self, exc_type, exc_value, traceback):\n        print(\"Done!\")\n\nwith Timer():\n    print(\"Doing some work\")",
    refBox: {
      syntax: "class C: / def __enter__(self): / def __exit__(self, *args):",
      desc: "Defines a custom context manager - __enter__ runs on entry, __exit__ runs on exit, even on error.",
      example: "with Timer():\n    print(\"work\")",
    },
    keyTakeaway: "__exit__ runs even if an exception occurs inside the with block - this guarantee is exactly what makes context managers reliable for cleanup logic.",
    commonMistake: "Manually managing setup/cleanup with try/finally when a custom context manager (__enter__/__exit__) would express the same guarantee more clearly and reusably.",
  },
  {
    title: "Type hints",
    body: "Although Python remains dynamically-typed at runtime, type hints let you optionally annotate what types a function expects and returns: def add(a: int, b: int) -> int: documents that add expects two integers and returns one. Python's interpreter doesn't enforce these hints at runtime - they're purely documentation and tooling support - but external tools (like a type checker, or an editor's autocomplete) can use them to catch mistakes before the code even runs, capturing some of the safety net C#'s compiler provides by default, as an opt-in choice rather than a requirement.",
    altExplain: "Type hints (def add(a: int, b: int) -> int) document expected types, but Python doesn't enforce them at runtime - they help tools and editors catch mistakes, as an opt-in choice.",
    visual: null,
    pycode: "def add(a: int, b: int) -> int:\n    return a + b\n\nprint(add(3, 4))\nprint(add(\"3\", \"4\"))  # still runs - hints aren't enforced at runtime",
    keyTakeaway: "Type hints are not enforced by Python itself at runtime - they're documentation that external tools (type checkers, editors) can use to catch mistakes before running the code.",
    commonMistake: "Assuming type hints will actually prevent a function from being called with the wrong type - Python runs it anyway, since hints are advisory, not enforced.",
  },
  {
    title: "Working with virtual environments & pip",
    body: "A virtual environment is an isolated Python installation for a specific project, keeping that project's installed packages separate from other projects (and from the system-wide Python installation), which avoids version conflicts between projects needing different versions of the same package. python -m venv myenv creates one, and activating it makes pip install commands affect only that isolated environment. requirements.txt is the standard way to record exactly which packages (and versions) a project needs, so anyone else can recreate the same environment with pip install -r requirements.txt.",
    altExplain: "A virtual environment keeps one project's installed packages separate from others, avoiding version conflicts. requirements.txt records exactly which packages a project needs.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Why use a virtual environment instead of installing every package globally on your system?",
      options: ["Virtual environments are required by Python to run any code", "It keeps each project's package versions isolated, avoiding conflicts between projects", "It makes code run faster", "There's no real benefit, it's just a convention"],
      correct: 1,
      explanation: "Different projects often need different versions of the same package. A virtual environment isolates each project's dependencies, so installing one project's requirements never breaks another project's setup.",
    },
    keyTakeaway: "requirements.txt lets anyone recreate a project's exact package environment with one command - essential for sharing Python projects reliably.",
    commonMistake: "Installing packages globally for every project instead of using isolated virtual environments, which eventually causes version conflicts between projects needing different package versions.",
  },
  {
    title: "Testing with assert & pytest concepts",
    body: "The assert statement checks that a condition is true, and raises an AssertionError if it isn't - a simple building block for verifying your code behaves as expected: assert add(2, 3) == 5 fails loudly if add is broken. pytest, a popular third-party testing framework, builds on this same idea at a larger scale: test functions (conventionally named test_something) use assert statements to check expected behavior, and pytest automatically discovers and runs every test function in a project, reporting which passed and which failed - mirroring the same Arrange-Act-Assert thinking covered in the C# unit testing lesson, just with Python's own tooling.",
    altExplain: "assert checks a condition and fails loudly if it's wrong - a basic building block for testing. pytest builds on this, automatically running many test functions and reporting pass/fail results.",
    visual: null,
    pycode: "def add(a, b):\n    return a + b\n\ndef test_add():\n    assert add(2, 3) == 5\n    assert add(-1, 1) == 0\n    print(\"All tests passed!\")\n\ntest_add()",
    keyTakeaway: "assert is the simple building block behind most Python testing - pytest and similar frameworks are largely automation and reporting built on top of the same core idea.",
    commonMistake: "Writing tests that only check the expected 'happy path' with assert, without testing edge cases like zero, negative numbers, or empty input.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "def func(*args, **kwargs):", desc: "*args collects extra positional args into a tuple; **kwargs into a dict.", example: "def total(*args):\n    return sum(args)" },
      { syntax: "class C: / def __enter__(self): / def __exit__(self, *args):", desc: "A custom context manager - guaranteed setup/cleanup, used with with.", example: "with Timer():\n    print(\"work\")" },
      { syntax: "def add(a: int, b: int) -> int:", desc: "Type hints - documentation for tools/editors, not enforced at runtime.", example: "def add(a: int, b: int) -> int:\n    return a + b" },
      { syntax: "python -m venv myenv / pip install -r requirements.txt", desc: "Creates an isolated environment; requirements.txt records exact package versions.", example: "pip install -r requirements.txt" },
      { syntax: "assert condition", desc: "Raises AssertionError if the condition is false - the core building block of testing.", example: "assert add(2, 3) == 5" },
    ],
  },
];

const PY_PRO_QUIZ_BANK = {
  basic: [
    { q: "f\"{price:.2f}\" formats a number to...", options: ["An integer", "Exactly 2 decimal places", "Scientific notation", "A percentage"], correct: 1 },
    { q: "Subtracting two datetime objects returns a...", options: ["string", "timedelta", "int", "list"], correct: 1 },
    { q: "Which operator finds values common to two sets?", options: ["|", "&", "-", "+"], correct: 1 },
    { q: "The 'with open(...) as f' pattern guarantees...", options: ["Faster file reading", "The file closes automatically, even if an error occurs", "The file is always empty first", "No errors can ever occur"], correct: 1 },
    { q: "json.dumps() converts...", options: ["JSON text to Python data", "Python data to JSON text", "A file to JSON", "Nothing, it's for reading only"], correct: 1 },
  ],
  intermediate: [
    { q: "A lambda is best described as...", options: ["A full class definition", "A small, unnamed inline function", "A type of loop", "A built-in exception"], correct: 1 },
    { q: "map() and filter() return...", options: ["A list directly", "An iterator, often wrapped in list() to view", "A dictionary", "A string"], correct: 1 },
    { q: "A decorator (written with @) is used to...", options: ["Delete a function", "Wrap a function to add behavior without changing its code", "Create a new class", "Import a module"], correct: 1 },
    { q: "A generator uses which keyword instead of return?", options: ["yield", "async", "lambda", "with"], correct: 0 },
    { q: "The requests library is used for...", options: ["File compression", "Making calls to web APIs", "Database queries", "Unit testing"], correct: 1 },
  ],
  advanced: [
    { q: "*args inside a function collects extra positional arguments into a...", options: ["dictionary", "tuple", "set", "string"], correct: 1 },
    { q: "A custom context manager needs which two methods?", options: ["start and stop", "__enter__ and __exit__", "open and close", "init and destroy"], correct: 1 },
    { q: "Python type hints are...", options: ["Enforced strictly at runtime", "Advisory documentation that tools can use, not enforced by Python itself", "Required for all functions", "The same as C#'s type system"], correct: 1 },
    { q: "A virtual environment's main purpose is to...", options: ["Make code run faster", "Isolate a project's package versions from other projects", "Replace the need for Python entirely", "Encrypt your code"], correct: 1 },
    { q: "assert add(2,3) == 5 will raise an error if...", options: ["add(2,3) actually equals 5", "add(2,3) does not equal 5", "add is never called", "Python isn't installed"], correct: 1 },
  ],
};
