// =========================================================================
// Power BI - Professional Level
// =========================================================================
const PBI_PRO_BASIC = [
  {
    title: "Power Query in Power BI",
    body: "Power Query in Power BI works exactly like its Excel counterpart from the Excel course: connecting to a data source, then applying a recorded sequence of cleaning and transformation steps - removing columns, splitting text, filtering rows - that automatically re-runs every time the data refreshes. The Power Query Editor shows this sequence as an 'Applied Steps' list, letting you review, reorder, or remove any individual transformation step without needing to redo the whole process from scratch, and letting you understand exactly what happened to the data at a glance.",
    altExplain: "Power Query in Power BI works exactly like the Excel version: recorded cleaning steps that automatically re-run on refresh, visible and editable in an 'Applied Steps' list.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "You need to remove one specific transformation step from the middle of a Power Query sequence without redoing all the other steps. What does the Applied Steps list let you do?",
      options: ["Nothing, the whole sequence must be rebuilt", "Delete just that one step directly from the list, while the others remain intact", "Only the very last step can ever be removed", "Applied Steps can't be edited once created"],
      correct: 1,
      explanation: "The Applied Steps list shows every transformation as an individual, editable step - you can delete, reorder, or modify any specific step directly, without needing to rebuild the entire sequence from scratch.",
    },
    keyTakeaway: "The Applied Steps list makes Power Query's transformations fully transparent and editable - any individual step can be reviewed or removed without redoing the whole sequence.",
    commonMistake: "Rebuilding an entire Power Query sequence from scratch to fix one problematic step, instead of directly editing or removing just that step in the Applied Steps list.",
  },
  {
    title: "Data transformation best practices",
    body: "Applying transformations in a sensible order matters: filtering out unneeded rows early reduces the amount of data every subsequent step has to process, rather than transforming everything first and filtering last. Naming query steps clearly (rather than leaving Power Query's generic auto-generated names) makes a complex transformation sequence far easier for anyone - including your future self - to understand later. Splitting a very complex transformation into multiple smaller, clearly-named queries referencing each other is generally more maintainable than one enormous, deeply nested single query.",
    altExplain: "Filter out unneeded rows early to reduce processing load on later steps. Name query steps clearly instead of leaving generic auto-names. Split complex transformations into smaller, clearly-named queries rather than one giant one.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A Power Query sequence applies 10 transformation steps to a full dataset, then filters out 90% of the rows as the very last step. What's the more efficient approach?",
      options: ["This ordering is already optimal", "Filter out the unneeded 90% of rows earlier, so subsequent steps process far less data", "Remove the filter entirely", "Filtering order never affects performance"],
      correct: 1,
      explanation: "Filtering early reduces the amount of data every subsequent transformation step needs to process - applying the filter last means all 10 prior steps unnecessarily processed the full dataset, including the 90% that ends up discarded anyway.",
    },
    keyTakeaway: "Filtering out unneeded rows as early as possible in a Power Query sequence reduces the processing load on every subsequent step - ordering genuinely affects performance.",
    commonMistake: "Applying a filter as the very last step in a long transformation sequence, forcing every earlier step to unnecessarily process rows that end up discarded anyway.",
  },
  {
    title: "Calculated columns vs. measures deep dive",
    body: "Building on the Entry-level introduction, the choice between a calculated column and a measure has real performance and correctness implications. A calculated column is computed once, at data refresh time, and stored physically in the model - taking up memory, but computed only once regardless of how many times it's viewed. A measure computes on demand, every time it's needed by a visual, using whatever filters currently apply - using no extra storage, but requiring computation on every interaction. As a general rule: use a measure whenever the calculation should respond to filtering and grouping (which is most of the time), and a calculated column only when you specifically need a fixed, per-row value, such as one used to build a relationship or a slicer.",
    altExplain: "A calculated column computes once at refresh time and is stored (uses memory, computed once). A measure computes on demand based on current filters (no extra storage, computed repeatedly). Use measures for most report calculations; columns mainly for relationships or slicers.",
    visual: null,
    dax: "SUM(Sales[Amount])",
    keyTakeaway: "Use a measure whenever a calculation should respond to filtering and grouping (the vast majority of report calculations) - reserve calculated columns for cases needing a fixed per-row value, like a relationship key.",
    commonMistake: "Building a calculated column for a value that should actually respond to report filtering, producing a number that stays fixed regardless of what slicers or visuals are currently applied.",
  },
  {
    title: "DIVIDE & handling errors in DAX",
    body: "Dividing by zero in DAX using the plain / operator produces an error that can break an entire visual. DIVIDE(numerator, denominator, [alternate result]) handles this gracefully, returning a specified fallback value (or blank, by default) instead of an error whenever the denominator is zero - directly mirroring the IFERROR concept from the Excel course, but built specifically for division, DAX's single most common source of runtime errors. Using DIVIDE by default, rather than a plain / that might occasionally divide by zero, is standard defensive practice in real-world DAX measures.",
    altExplain: "Plain division by zero in DAX causes an error that can break a whole visual. DIVIDE() handles this gracefully with a fallback value instead - the DAX equivalent of Excel's IFERROR, built specifically for division.",
    visual: null,
    dax: "DIVIDE(SUM(Sales[Amount]), COUNTROWS(Sales))",
    refBox: {
      syntax: "DIVIDE(numerator, denominator, [alternate_result])",
      desc: "Divides safely - returns a fallback (or blank) instead of an error when the denominator is zero.",
      example: "DIVIDE(SUM(Sales[Amount]), COUNTROWS(Sales), 0)",
    },
    keyTakeaway: "Using DIVIDE instead of a plain / operator is standard defensive DAX practice - it gracefully handles the zero-denominator case that plain division cannot.",
    commonMistake: "Using plain division (/) in a DAX measure without considering what happens if the denominator is ever zero, risking a broken visual in production.",
    challenge: {
      prompt: "Try DIVIDE(SUM(Sales[Quantity]), COUNTROWS(Sales)) to find the average quantity per sale.",
      hint: "This calculates total quantity divided by number of rows, safely.",
      solution: "DIVIDE(SUM(Sales[Quantity]), COUNTROWS(Sales))",
    },
  },
  {
    title: "Variables in DAX (VAR/RETURN)",
    body: "A DAX measure can define named intermediate variables with VAR, then use RETURN to specify the final expression using those variables - like calculating a total once with VAR, then referencing that stored result multiple times in the RETURN expression, rather than recalculating the same subexpression repeatedly. This improves both readability (breaking a complex formula into clearly-named logical steps) and performance (avoiding redundant recalculation of the same intermediate value multiple times within one formula).",
    altExplain: "VAR lets you name an intermediate calculation once, then RETURN uses it - avoiding recalculating the same thing multiple times in one formula, and making complex measures much more readable.",
    visual: null,
    refBox: {
      syntax: "VAR name = expression RETURN expression_using_name",
      desc: "Names an intermediate calculation once, reused via RETURN - avoids redundant recalculation, improves readability.",
      example: "VAR total = SUM(Sales[Amount]) RETURN total / 1000",
    },
    widget: "insight-check",
    insightCheck: {
      question: "A DAX measure calculates the same intermediate subtotal three separate times within one formula, using it differently each time. What would VAR/RETURN improve here?",
      options: ["Nothing, DAX doesn't support intermediate variables", "It calculates the subtotal once with VAR, then reuses it via RETURN, avoiding redundant recalculation", "It would make the formula run slower", "VAR only works with text values"],
      correct: 1,
      explanation: "VAR lets you calculate an intermediate value exactly once and store it under a name, which RETURN can then reference as many times as needed - avoiding the DAX engine redundantly recalculating that same subexpression multiple times within the formula.",
    },
    keyTakeaway: "VAR/RETURN avoids redundant recalculation within a single measure and makes complex, multi-step DAX formulas dramatically more readable.",
    commonMistake: "Repeating the same complex subexpression multiple times within one DAX measure instead of calculating it once with VAR and reusing it via RETURN.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the DAX and concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Power Query: Applied Steps (repeatable transforms)", desc: "Each transformation step is recorded and repeatable - refreshing re-runs the whole sequence.", example: "Remove columns, filter rows, then rename - all saved as steps." },
      { syntax: "Clean at the source, not downstream", desc: "Fix data quality issues in Power Query, not with patchwork DAX formulas later.", example: "Trim whitespace in Power Query, not with a measure hack." },
      { syntax: "Calculated column (fixed) vs Measure (dynamic)", desc: "A measure recalculates based on current filters; a column stores one fixed value per row.", example: "Total Sales = SUM(Sales[Amount])  // a measure" },
      { syntax: "DIVIDE(numerator, denominator, [alternate_result])", desc: "Divides safely - returns a fallback instead of an error on a zero denominator.", example: "DIVIDE(SUM(Sales[Amount]), COUNTROWS(Sales), 0)" },
      { syntax: "VAR name = expression RETURN expression_using_name", desc: "Names an intermediate calculation once, reused via RETURN.", example: "VAR total = SUM(Sales[Amount]) RETURN total / 1000" },
    ],
  },
];

const PBI_PRO_INTERMEDIATE = [
  {
    title: "Advanced relationships",
    body: "Most relationships are one-to-many with a single filter direction, but some scenarios need more nuance. A many-to-many relationship connects two tables where neither side has unique matching values, common when linking through an intermediate bridge table. Bidirectional filtering lets a filter applied to either side of a relationship affect the other, rather than only flowing one direction - useful in specific scenarios, but applied too broadly, it can cause ambiguous or unexpectedly duplicated results, which is why Power BI defaults to single-direction filtering and requires deliberately enabling bidirectional filtering when it's genuinely needed.",
    altExplain: "Many-to-many relationships link tables where neither side has unique matching values, often via a bridge table. Bidirectional filtering lets a filter flow both directions across a relationship - powerful, but risky if overused, since it defaults off for good reason.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A report shows unexpectedly duplicated or inflated numbers after enabling bidirectional filtering on several relationships 'just in case.' What's the likely cause?",
      options: ["This is unrelated to the relationship settings", "Bidirectional filtering applied too broadly can cause ambiguous filter propagation, leading to inflated results", "Bidirectional filtering always improves accuracy", "The data source itself must be corrupted"],
      correct: 1,
      explanation: "Bidirectional filtering, when enabled broadly without a specific need, can cause filters to propagate in unintended, ambiguous ways across the model, often producing inflated or duplicated results - exactly why it defaults to single-direction and should be enabled deliberately, not by default.",
    },
    keyTakeaway: "Bidirectional filtering should be enabled deliberately for a specific, understood need - applying it broadly 'just in case' is a common source of confusing, inflated report numbers.",
    commonMistake: "Enabling bidirectional filtering on relationships without a specific need, causing unexpected and hard-to-diagnose numerical inflation elsewhere in the report.",
  },
  {
    title: "Time intelligence functions deep dive",
    body: "Beyond basic year-to-date calculations, TOTALYTD(SUM(Sales[Amount]), 'Date'[Date]) accumulates a total from the start of the year through the current date context. SAMEPERIODLASTYEAR('Date'[Date]) shifts the current date filter back exactly one year, letting you build direct year-over-year comparisons: CALCULATE(SUM(Sales[Amount]), SAMEPERIODLASTYEAR('Date'[Date])) computes last year's equivalent figure for whatever period is currently being viewed. These functions all depend on that properly configured Date table covered at the Entry level - without it, they simply won't calculate correctly, regardless of how correctly the formula itself is written.",
    altExplain: "TOTALYTD accumulates a running year-to-date total. SAMEPERIODLASTYEAR shifts the current period back one year for direct comparisons. Both depend entirely on a properly configured Date table to work correctly.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A correctly-written SAMEPERIODLASTYEAR formula still returns incorrect results. The Date table exists but isn't marked as an official 'Date table' in the model settings. What's the likely fix?",
      options: ["Rewrite the entire formula from scratch", "Properly mark the table as a Date table in the model settings", "Time intelligence functions simply don't work reliably, ever", "Switch to Import mode"],
      correct: 1,
      explanation: "Time intelligence functions require the date table to be explicitly marked as an official 'Date table' in Power BI's model settings - simply having a table with dates in it isn't sufficient for these functions to calculate correctly.",
    },
    keyTakeaway: "Time intelligence functions require the Date table to be explicitly marked as such in model settings - a table merely containing dates isn't automatically sufficient.",
    commonMistake: "Assuming any table containing date values will work correctly with time intelligence functions, without explicitly marking it as the model's official Date table.",
  },
  {
    title: "Building interactive dashboards",
    body: "A well-designed Power BI dashboard follows the same principles from the Data Analytics and Excel dashboard lessons: the most important KPIs prominently placed, related visuals grouped logically, and slicers giving viewers self-service exploration without needing to understand the underlying model. Power BI adds cross-filtering as a built-in behavior: clicking a data point in one visual automatically filters every other visual on the page that shares a related field, creating an interconnected, exploratory experience without needing to build that interactivity manually.",
    altExplain: "A good Power BI dashboard follows the same design principles from Data Analytics and Excel - important KPIs prominent, related visuals grouped, slicers for self-service. Cross-filtering is built in: clicking one visual automatically filters related ones.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A viewer clicks a specific product bar in one chart, and every other visual on the page automatically updates to reflect just that product, without any custom configuration. What Power BI behavior is this?",
      options: ["A bug that should be disabled", "Built-in cross-filtering between visuals sharing related fields", "Row-level security", "A bookmark being triggered"],
      correct: 1,
      explanation: "Cross-filtering is a built-in Power BI behavior: clicking a data point in one visual automatically filters other visuals on the page that share a related field, creating interactive exploration without any custom setup required.",
    },
    keyTakeaway: "Cross-filtering between visuals is automatic and built into Power BI - it's a feature to design around and take advantage of, not something that needs to be manually built.",
    commonMistake: "Disabling or fighting against cross-filtering behavior instead of designing a dashboard's layout to take advantage of this automatic, built-in interactivity.",
  },
  {
    title: "Custom visuals & the marketplace",
    body: "Beyond Power BI's built-in visual types, the AppSource marketplace offers custom visuals built by Microsoft and third parties - specialized chart types, advanced KPI displays, and industry-specific visualizations not available out of the box. Custom visuals can be imported directly into a report and used exactly like built-in ones, though it's worth vetting a custom visual's source and reviews before relying on it heavily, since quality and ongoing support vary considerably across the marketplace's many contributors.",
    altExplain: "The AppSource marketplace offers custom visual types beyond Power BI's built-in ones, from Microsoft and third parties - useful for specialized needs, but worth vetting quality and support before relying on one heavily.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A report needs a highly specialized chart type not available among Power BI's built-in visuals. What's a reasonable option?",
      options: ["This need can never be met in Power BI", "Look for an appropriate custom visual in the AppSource marketplace", "Rebuild the entire report in a different tool", "Custom visuals aren't supported in Power BI"],
      correct: 1,
      explanation: "The AppSource marketplace offers a wide range of custom visuals for specialized needs beyond Power BI's built-in chart types, importable directly into a report and used like any other visual.",
    },
    keyTakeaway: "Custom visuals from AppSource extend Power BI beyond its built-in visual types, but quality and support vary - vet a visual's source and reviews before relying on it in a critical report.",
    commonMistake: "Adopting a custom visual for a critical report without checking its source, reviews, or ongoing support, risking reliability issues down the line.",
  },
  {
    title: "Performance optimization: reducing model size",
    body: "A large or slow Power BI model often benefits from removing columns that aren't actually used in any visual or calculation - every unused column still consumes memory and slows refresh times, even if it never appears in a report. Reducing the granularity of imported data (importing daily summaries instead of every individual transaction, when daily detail is sufficient) and disabling auto date/time hierarchies for fields that don't need them are further common techniques - the general principle is importing only the data and detail level actually needed, not everything available just because it's there.",
    altExplain: "Removing unused columns, reducing data granularity when full detail isn't needed, and disabling unnecessary auto-generated hierarchies all shrink model size - the general principle: import only what's actually needed, not everything available.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A Power BI model imports 40 columns from a source table, but only 12 are actually used across all visuals and measures. What's a straightforward performance improvement?",
      options: ["Nothing can be improved without changing visuals", "Remove the 28 unused columns from the import, reducing model size and refresh time", "Add more columns to be thorough", "Switch to DirectQuery to fix this automatically"],
      correct: 1,
      explanation: "Every imported column, whether used or not, consumes memory and adds to refresh time. Removing the 28 genuinely unused columns directly reduces the model's size and improves performance, with no loss of actual reporting capability.",
    },
    keyTakeaway: "Importing only the columns and detail level actually needed - not everything available by default - is one of the simplest, most effective Power BI performance improvements.",
    commonMistake: "Importing every available column from a source 'just in case it's needed later,' unnecessarily bloating the model's size and slowing refresh performance.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Bidirectional / many-to-many relationships", desc: "More advanced relationship types beyond simple one-to-many - use carefully, can cause ambiguity.", example: "A many-to-many bridge table linking students to courses." },
      { syntax: "TOTALYTD / SAMEPERIODLASTYEAR (need a Date table)", desc: "Time intelligence deep dive - these require a properly marked calendar table.", example: "Compare this quarter to the same quarter last year." },
      { syntax: "Cross-filtering + drill-down (interactive dashboards)", desc: "Clicking one visual filters others automatically - the core of dashboard interactivity.", example: "Click a region bar, every other visual filters to that region." },
      { syntax: "AppSource custom visuals", desc: "Extends beyond built-in visual types with community/certified add-ons.", example: "A specialized Gantt chart or word cloud visual." },
      { syntax: "Import only needed columns/detail level", desc: "One of the simplest, most effective performance improvements.", example: "Remove 28 unused columns from a 40-column import." },
    ],
  },
];

const PBI_PRO_ADVANCED = [
  {
    title: "Power BI Service: workspaces & apps",
    body: "A workspace in the Power BI Service is a shared container for a team's related reports, dashboards, and datasets, with role-based access controlling who can view versus edit content within it. Once content in a workspace is ready for a broader audience, it can be packaged into an app - a curated, read-only collection of reports presented with clean navigation, distinct from the workspace itself where the underlying editable content lives. This separation lets a team collaborate and iterate freely within a workspace, while presenting a polished, stable app experience to the actual business audience consuming the finished reports.",
    altExplain: "A workspace is a shared container for a team's reports and datasets, with role-based access. An app packages finished workspace content into a clean, read-only experience for a broader audience - separating collaboration from consumption.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A team wants to iterate and edit reports freely among themselves, while presenting only the polished, finished versions to executives who shouldn't see work-in-progress content. What Power BI structure supports this?",
      options: ["Everyone works and views in the same single workspace", "The team edits in a workspace, then publishes a curated app for the executive audience", "Row-level security alone solves this", "This separation isn't possible in Power BI"],
      correct: 1,
      explanation: "A workspace supports the team's collaborative editing, while a published app presents a curated, polished, read-only view to a broader audience - keeping work-in-progress content separate from what executives actually see.",
    },
    keyTakeaway: "Workspaces support team collaboration and editing; apps present a polished, curated, read-only experience to a broader audience - the two serve genuinely different purposes.",
    commonMistake: "Sharing direct workspace access with a broad business audience instead of publishing a curated app, exposing work-in-progress content that wasn't meant for that audience yet.",
  },
  {
    title: "Scheduled refresh & gateways",
    body: "A scheduled refresh automatically updates a published report's imported data on a defined schedule (like every morning at 6 AM), so viewers always see reasonably current data without anyone manually triggering a refresh. When the underlying data source is on-premises (not directly reachable from the cloud-based Power BI Service), an on-premises data gateway acts as a secure bridge, letting the Power BI Service reach that internal data source for scheduled refreshes without exposing it directly to the public internet.",
    altExplain: "Scheduled refresh automatically updates a report's data on a set schedule, so viewers see reasonably current data without manual triggering. An on-premises gateway securely bridges cloud-based Power BI to internal, on-premises data sources.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A report's data source is an internal, on-premises database not directly reachable from the internet. How does the cloud-based Power BI Service perform scheduled refreshes against it?",
      options: ["This is not possible at all", "Through an on-premises data gateway acting as a secure bridge", "By exposing the database directly to the public internet", "Only Import mode can ever be used, never DirectQuery"],
      correct: 1,
      explanation: "An on-premises data gateway securely bridges the cloud-based Power BI Service to an internal data source, letting scheduled refreshes (or DirectQuery) reach that source without needing to expose it directly to the public internet.",
    },
    keyTakeaway: "An on-premises data gateway is the standard, secure way for the cloud-based Power BI Service to reach internal data sources that aren't directly internet-accessible.",
    commonMistake: "Assuming an on-premises data source can't be used with Power BI at all, when a gateway specifically solves this exact connectivity need securely.",
  },
  {
    title: "Row-level security deep dive",
    body: "Beyond the basic static RLS covered at the Entry level, dynamic row-level security uses a DAX expression referencing the currently logged-in user (via the USERPRINCIPALNAME function) to automatically filter data based on who's viewing the report, without needing to define a separate static role for every individual user - a single dynamic role definition scales correctly to any number of users, as long as the underlying data includes a way to match each row to the appropriate user or group, like a column mapping employee email addresses to their assigned region.",
    altExplain: "Dynamic row-level security uses USERPRINCIPALNAME() to automatically filter data based on who's currently viewing the report - one dynamic rule scales to any number of users, unlike defining a separate static role per person.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A company has 200 regional managers, each needing to see only their own region's data. Defining 200 separate static RLS roles would be impractical. What's the better approach?",
      options: ["It's not possible to scale RLS beyond a handful of users", "Dynamic RLS using USERPRINCIPALNAME(), matched against a column identifying each user's region", "Give everyone full access instead", "Build 200 separate reports"],
      correct: 1,
      explanation: "Dynamic row-level security uses a single DAX rule referencing the logged-in user's identity (via USERPRINCIPALNAME), matched against a column in the data - this one dynamic rule scales correctly to any number of users, unlike maintaining 200 separate static roles.",
    },
    keyTakeaway: "Dynamic RLS (using USERPRINCIPALNAME) scales to any number of users with one rule, unlike static RLS which requires a separately defined role for each distinct group.",
    commonMistake: "Attempting to build individual static RLS roles for a large number of users, when dynamic RLS would handle the same requirement with a single scalable rule.",
  },
  {
    title: "Power BI Premium & capacity",
    body: "Power BI Premium (or Premium Per User) provides dedicated computing capacity for an organization, offering larger model size limits, more frequent refresh schedules, and advanced features not available on the standard shared capacity most individual users share. This becomes worthwhile once an organization's Power BI usage genuinely outgrows shared capacity's limits - large models, frequent refresh needs, or a need for advanced governance features - rather than being a default upgrade every organization needs regardless of actual usage patterns.",
    altExplain: "Power BI Premium provides dedicated capacity with larger model limits, more frequent refreshes, and advanced features - worthwhile once actual usage genuinely outgrows shared capacity limits, not a default upgrade for everyone.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A small team with modest reporting needs is considering Power BI Premium mainly because it sounds more advanced. What's a more sensible approach?",
      options: ["Always upgrade to Premium regardless of actual needs", "Evaluate whether their actual usage (model size, refresh frequency, governance needs) genuinely exceeds shared capacity limits first", "Premium is required for any Power BI usage", "Downgrade to a completely different tool instead"],
      correct: 1,
      explanation: "Power BI Premium is worthwhile once actual usage patterns genuinely exceed shared capacity's limits - evaluating real needs first avoids paying for capacity and features a smaller team's usage doesn't actually require.",
    },
    keyTakeaway: "Power BI Premium is a genuine upgrade for organizations that have outgrown shared capacity limits - it's not a default 'more advanced' option worth adopting regardless of actual usage.",
    commonMistake: "Upgrading to Power BI Premium based on perceived prestige or advanced features alone, without first confirming actual usage genuinely exceeds standard shared capacity's real limits.",
  },
  {
    title: "Paginated reports overview",
    body: "Standard Power BI reports are optimized for interactive, exploratory, on-screen viewing, but some needs - a precisely formatted invoice, a detailed multi-page printed statement - call for pixel-perfect, print-ready formatting instead. Paginated reports (built with the separate Power BI Report Builder tool) are specifically designed for this: formatted to fit exact page dimensions, ideal for printing or exporting to PDF, at the cost of the flexible, resizable, interactive exploration standard Power BI reports offer. Choosing between the two comes down to matching the actual output need - interactive on-screen exploration versus precisely formatted print output.",
    altExplain: "Standard Power BI reports suit interactive on-screen exploration. Paginated reports (built with Report Builder) suit pixel-perfect, print-ready output like invoices or formal statements - choose based on the actual output need.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A finance team needs to generate precisely formatted, printable monthly invoices for each customer. Which Power BI report type fits this need better?",
      options: ["A standard interactive Power BI report", "A paginated report, built for pixel-perfect, print-ready output", "Neither type supports this use case", "A dashboard specifically"],
      correct: 1,
      explanation: "Paginated reports are specifically designed for pixel-perfect, print-ready formatted output like invoices - standard interactive reports are optimized for on-screen exploration, not precise printed page layout.",
    },
    keyTakeaway: "Paginated reports and standard interactive reports serve genuinely different needs - precise print-ready formatting versus flexible on-screen exploration - choosing the right one depends on the actual output requirement.",
    commonMistake: "Trying to force a standard interactive Power BI report into pixel-perfect print formatting, when a paginated report is specifically built for that exact requirement.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Workspace \u2192 App (publish/distribute)", desc: "A workspace is where reports are built collaboratively; an app is the polished, distributed package.", example: "Multiple reports and dashboards bundled into one app for viewers." },
      { syntax: "Scheduled refresh + on-premises data gateway", desc: "Keeps imported data current automatically; a gateway securely bridges to on-prem sources.", example: "Refresh nightly from an on-prem SQL Server database." },
      { syntax: "RLS roles + DAX filter expressions", desc: "Defines exactly which rows each role can see, enforced automatically.", example: "[Region] = USERPRINCIPALNAME() restricts by logged-in user." },
      { syntax: "Premium capacity (dedicated resources)", desc: "Removes per-user licensing limits and adds larger model/refresh capabilities.", example: "Needed for very large models or frequent refreshes." },
      { syntax: "Paginated reports (pixel-perfect, print-ready)", desc: "Built for precise formatted output like invoices - different from standard interactive reports.", example: "A formatted monthly invoice per customer." },
    ],
  },
];

const PBI_PRO_QUIZ_BANK = {
  basic: [
    { q: "The Applied Steps list in Power Query shows...", options: ["A list of report visuals", "Each transformation step, individually editable", "User access permissions", "DAX measure definitions"], correct: 1 },
    { q: "Filtering rows early in a Power Query sequence generally...", options: ["Has no effect on performance", "Reduces the amount of data subsequent steps need to process", "Always breaks the query", "Only matters for very small datasets"], correct: 1 },
    { q: "A measure, compared to a calculated column, is...", options: ["Stored once at refresh time", "Computed on demand based on current filters and grouping", "Never usable in a chart", "Always slower in every scenario"], correct: 1 },
    { q: "DIVIDE() is preferred over plain / mainly because it...", options: ["Runs faster in all cases", "Gracefully handles division by zero instead of causing an error", "Only works with whole numbers", "Removes the need for CALCULATE"], correct: 1 },
    { q: "VAR/RETURN in DAX is used to...", options: ["Create a new table", "Name and reuse an intermediate calculation within a measure", "Publish a report", "Define a relationship"], correct: 1 },
  ],
  intermediate: [
    { q: "Bidirectional filtering, applied too broadly, can cause...", options: ["Faster report performance always", "Ambiguous filter propagation and inflated or duplicated results", "Automatic row-level security", "No effect at all"], correct: 1 },
    { q: "SAMEPERIODLASTYEAR requires...", options: ["No special setup", "A properly configured Date table", "Row-level security to be disabled", "DirectQuery mode only"], correct: 1 },
    { q: "Cross-filtering between visuals in Power BI is...", options: ["A custom feature requiring manual setup", "Built in automatically for visuals sharing related fields", "Only available with Power BI Premium", "The same thing as a bookmark"], correct: 1 },
    { q: "Custom visuals from AppSource should be...", options: ["Used without any consideration", "Vetted for source quality and reviews before relying on them heavily", "Avoided entirely, they're never reliable", "Only used in paginated reports"], correct: 1 },
    { q: "Removing unused columns from a Power BI model primarily improves...", options: ["Visual color schemes", "Model size and refresh performance", "Row-level security", "DAX syntax highlighting"], correct: 1 },
  ],
  advanced: [
    { q: "An 'app' in the Power BI Service is best described as...", options: ["The same thing as a workspace", "A curated, read-only collection of reports for a broader audience", "A type of DAX function", "A custom visual"], correct: 1 },
    { q: "An on-premises data gateway is used to...", options: ["Format numbers in a report", "Securely bridge the cloud-based Power BI Service to internal data sources", "Create row-level security roles", "Replace the need for Power Query"], correct: 1 },
    { q: "Dynamic row-level security typically uses...", options: ["A separate static role for every single user", "USERPRINCIPALNAME() matched against a column identifying the user", "Only works with Power BI Premium", "Conditional formatting rules"], correct: 1 },
    { q: "Power BI Premium is most worthwhile when...", options: ["For every organization regardless of usage", "Actual usage genuinely exceeds shared capacity's limits", "A report has more than 3 visuals", "Row-level security is needed"], correct: 1 },
    { q: "Paginated reports are best suited for...", options: ["Interactive on-screen exploration", "Pixel-perfect, print-ready formatted output", "Real-time DirectQuery dashboards", "Custom visual development"], correct: 1 },
  ],
};
