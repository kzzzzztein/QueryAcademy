// =========================================================================
// Power BI - Master Level
// =========================================================================
const PBI_MASTER_BASIC = [
  {
    title: "Advanced DAX: iterators (SUMX, AVERAGEX)",
    body: "Regular aggregate functions like SUM operate on a single column directly, but iterator functions like SUMX evaluate a custom expression for every single row, then aggregate the results - SUMX(Sales, Sales[Amount] * Sales[Quantity]) calculates a per-row revenue figure (price times quantity) for every row, then sums all those individual results together, something a plain SUM alone can't do, since SUM only totals one existing column, not a row-by-row calculated expression. This row-by-row evaluation is more computationally expensive than a plain aggregate, so iterators should be used deliberately, when a genuine row-level calculation is actually needed.",
    altExplain: "SUMX evaluates a custom expression for every row, then sums the results - unlike plain SUM, which only totals one existing column. Useful for calculations needing row-by-row logic, like price times quantity, but more computationally expensive.",
    visual: null,
    dax: "SUMX(Sales, Sales[Amount]*Sales[Quantity])",
    refBox: {
      syntax: "SUMX(table, expression) / AVERAGEX(table, expression)",
      desc: "Evaluates an expression row-by-row, then aggregates the results - unlike plain SUM, which only totals an existing column.",
      example: "SUMX(Sales, Sales[Amount]*Sales[Quantity])",
    },
    keyTakeaway: "SUMX (and other X-suffixed iterators) evaluate a row-by-row expression before aggregating - use them specifically when a calculation genuinely needs per-row logic that a plain SUM can't express.",
    commonMistake: "Reaching for SUMX by default for a calculation that a plain, more efficient SUM could already handle, adding unnecessary computational cost.",
    challenge: {
      prompt: "Calculate the average per-row revenue (Amount times Quantity) using AVERAGEX-style logic - here, use SUMX and divide by COUNTROWS.",
      hint: "DIVIDE(SUMX(Sales, Sales[Amount]*Sales[Quantity]), COUNTROWS(Sales))",
      solution: "DIVIDE(SUMX(Sales, Sales[Amount]*Sales[Quantity]), COUNTROWS(Sales))",
    },
  },
  {
    title: "Context transition deep dive",
    body: "DAX has two distinct kinds of context: row context (the current row being evaluated, as inside an iterator like SUMX) and filter context (the combination of filters currently applied, from slicers, visuals, or CALCULATE). Context transition is what happens when CALCULATE converts a row context into an equivalent filter context - this subtle mechanism is exactly why a measure referencing a calculated column can behave correctly inside an iterator, even though row context and filter context are conceptually different things. Understanding context transition is often the single hardest, but most valuable, conceptual leap in mastering advanced DAX.",
    altExplain: "DAX has row context (the current row) and filter context (currently applied filters). Context transition is CALCULATE converting a row context into an equivalent filter context - a subtle mechanism behind much of DAX's more advanced, sometimes confusing behavior.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A measure behaves correctly when called from inside a row-by-row iterator, converting the current row into filters as though CALCULATE had explicitly restricted the data to just that row. What DAX concept explains this?",
      options: ["This is a bug in Power BI", "Context transition, where CALCULATE (implicit or explicit) converts row context into filter context", "This only happens with time intelligence functions", "It's unrelated to any documented DAX behavior"],
      correct: 1,
      explanation: "Context transition is the specific DAX mechanism where a row context gets converted into an equivalent filter context - typically triggered by CALCULATE, whether written explicitly or implied by calling a measure - explaining this exact behavior.",
    },
    keyTakeaway: "Context transition - converting row context into filter context via CALCULATE - is one of the most important, if initially confusing, concepts for truly mastering advanced DAX.",
    commonMistake: "Getting unexpected results from a measure used inside an iterator without understanding that context transition is silently converting row context into filter context behind the scenes.",
  },
  {
    title: "ALL, ALLEXCEPT, and filter removal",
    body: "ALL(Table) removes all filters currently applied to a table within a CALCULATE expression, useful for calculating something like 'percent of total,' where the denominator needs to ignore whatever filter is currently narrowing the numerator - CALCULATE(SUM(Sales[Amount]), ALL(Sales)) computes the grand total regardless of any active slicer or visual filter. ALLEXCEPT(Table, Table[Column]) removes all filters except the ones specified, letting you selectively preserve just one dimension's filtering while clearing everything else - a common pattern for calculating a value relative to a specific, chosen grouping.",
    altExplain: "ALL(Table) removes every filter on a table - useful for a 'percent of total' calculation that needs an unfiltered denominator. ALLEXCEPT keeps specific filters while clearing everything else - for calculations relative to just one chosen grouping.",
    visual: null,
    dax: "CALCULATE(SUM(Sales[Amount]), ALL(Sales))",
    refBox: {
      syntax: "ALL(table) / ALLEXCEPT(table, column)",
      desc: "ALL removes every filter on a table - useful for an unfiltered grand-total. ALLEXCEPT keeps only specified filters.",
      example: "CALCULATE(SUM(Sales[Amount]), ALL(Sales))",
    },
    keyTakeaway: "ALL is the standard technique for calculating an unfiltered 'grand total' denominator for percent-of-total style measures, regardless of whatever filter is currently narrowing the numerator.",
    commonMistake: "Building a 'percent of total' measure without ALL, causing the denominator to incorrectly shrink along with whatever filter is currently applied, rather than representing a true, stable grand total.",
  },
  {
    title: "Composite models overview",
    body: "A composite model combines multiple data connection modes within one single Power BI model - some tables in Import mode for speed, others in DirectQuery mode for genuinely live data, and even data from separate Power BI datasets connected together. This flexibility lets you match each table's connection mode to its actual real-world requirement, rather than forcing an entire model into one single mode that might be ideal for some tables but genuinely wrong for others with different freshness or performance needs.",
    altExplain: "A composite model mixes connection modes within one Power BI model - some tables Import for speed, others DirectQuery for live data - matching each table's mode to its actual specific requirement, instead of one mode forced on everything.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A model has a large historical sales table that rarely changes, and a small, frequently-updated live inventory table. What composite model approach fits best?",
      options: ["Force both tables into DirectQuery mode", "Import the historical sales table for speed, and use DirectQuery for the live inventory table", "This mixing of modes isn't possible in Power BI", "Force both tables into Import mode"],
      correct: 1,
      explanation: "A composite model lets each table use the connection mode matching its actual needs - Import mode for the stable historical data (fast performance, refreshed periodically), DirectQuery for the frequently-changing inventory table (always current).",
    },
    keyTakeaway: "A composite model matches each table's connection mode to its actual real-world freshness and performance requirements, rather than forcing one mode onto an entire model uniformly.",
    commonMistake: "Forcing an entire model into a single connection mode (all Import or all DirectQuery) when different tables genuinely have different freshness needs that a composite model could address individually.",
  },
  {
    title: "DirectQuery vs. Import mode deep dive",
    body: "Import mode loads a compressed, highly optimized copy of the data into Power BI's own in-memory engine, delivering excellent performance and full DAX function support, at the cost of data being only as fresh as the last scheduled refresh. DirectQuery sends live queries to the source on every interaction, ensuring genuinely current data, but with meaningfully more performance dependency on the source system's own speed, and some DAX functions and modeling features working differently or not at all. The choice comes down to a real tradeoff between performance and freshness, rather than DirectQuery being a strictly 'better' or more advanced option.",
    altExplain: "Import mode loads an optimized copy for excellent performance, at the cost of only being as fresh as the last refresh. DirectQuery queries live data on every interaction, but depends heavily on the source's own speed and has some DAX limitations - a genuine tradeoff, not one mode simply being better.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A team defaults to DirectQuery for a new report, assuming it's the more 'advanced' choice, even though the data only needs to refresh once daily and report performance has become noticeably slow. What's the likely issue?",
      options: ["DirectQuery is always faster, so this shouldn't happen", "Import mode would likely perform better here, since daily freshness doesn't require DirectQuery's live-query overhead", "The slowness is unrelated to the connection mode choice", "This is a Power BI bug"],
      correct: 1,
      explanation: "DirectQuery's live-query overhead is only worth its performance cost when genuinely current, up-to-the-second data is required. With only a daily freshness need, Import mode's optimized, pre-loaded performance would likely serve this report better.",
    },
    keyTakeaway: "DirectQuery isn't simply the more 'advanced' choice - it's the right choice specifically when genuine data freshness outweighs Import mode's substantial performance advantages.",
    commonMistake: "Choosing DirectQuery by default assuming it's the more sophisticated option, without weighing its real performance tradeoffs against an actual, genuine need for live data freshness.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the DAX and concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "SUMX(table, expression) / AVERAGEX(table, expression)", desc: "Evaluates an expression row-by-row, then aggregates the results.", example: "SUMX(Sales, Sales[Amount]*Sales[Quantity])" },
      { syntax: "Row context vs. Filter context", desc: "Context transition (via CALCULATE) converts row context into filter context - the hardest DAX concept to master.", example: "Lets a measure referencing a column work correctly inside an iterator." },
      { syntax: "ALL(table) / ALLEXCEPT(table, column)", desc: "ALL removes every filter - useful for grand-total denominators. ALLEXCEPT keeps only specified filters.", example: "CALCULATE(SUM(Sales[Amount]), ALL(Sales))" },
      { syntax: "Composite model: mixed Import + DirectQuery tables", desc: "Matches each table's connection mode to its actual freshness/performance requirement.", example: "Historical data Import, live inventory DirectQuery." },
      { syntax: "DirectQuery (live, slower) vs Import (cached, faster)", desc: "DirectQuery is worth its overhead only when genuine live freshness is required.", example: "Daily-refresh data usually performs better as Import." },
    ],
  },
];

const PBI_MASTER_INTERMEDIATE = [
  {
    title: "Performance tuning concepts",
    body: "Diagnosing a slow Power BI report benefits from understanding where time is actually being spent: the DAX query engine (evaluating formula logic) versus the storage engine (retrieving data from the model). Tools like DAX Studio (a free, separate community tool) let you examine query execution plans and timing in detail, revealing whether a slow measure is spending its time on formula complexity or on how much raw data it's scanning - a diagnosis that shapes whether the fix is rewriting the DAX formula itself, or restructuring the underlying data model.",
    altExplain: "Diagnosing slow reports means understanding whether time is spent in the DAX formula engine or the storage engine retrieving data. Tools like DAX Studio reveal this breakdown, shaping whether the fix is rewriting a formula or restructuring the data model.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A performance analysis tool reveals that a slow measure spends almost all its time in the storage engine scanning a huge, unfiltered table, not in complex DAX logic. What does this suggest as the more effective fix?",
      options: ["Rewrite the DAX formula's logic, since that's always the bottleneck", "Focus on restructuring the data model or adding filtering to reduce what's being scanned, since that's where the real time is spent", "Nothing can be done to improve this", "Switch to a completely different reporting tool"],
      correct: 1,
      explanation: "If the storage engine (data retrieval) is where the real time is being spent, rewriting DAX formula logic won't meaningfully help - the more effective fix addresses the data model structure or filtering to reduce the amount of data actually being scanned.",
    },
    keyTakeaway: "Correctly diagnosing whether slowness comes from the DAX formula engine or the storage engine determines whether the effective fix is rewriting a formula or restructuring the data model.",
    commonMistake: "Assuming every performance problem is caused by DAX formula complexity, when the actual bottleneck is frequently in how much raw data the storage engine has to scan.",
  },
  {
    title: "Aggregations for large datasets",
    body: "For genuinely massive datasets, Power BI's aggregations feature lets you define a smaller, pre-summarized table (like daily sales totals) that Power BI automatically uses instead of the full-detail table whenever a query can be answered from that summary alone, transparently falling back to the full-detail table only when genuinely necessary detail is requested. This can dramatically improve performance for typical dashboard-level queries (which usually don't need row-by-row detail) while still preserving the ability to drill into full detail on demand when a viewer genuinely needs it.",
    altExplain: "Aggregations let Power BI automatically use a smaller, pre-summarized table for typical dashboard queries, falling back to full detail only when genuinely needed - dramatically improving performance on massive datasets without losing drill-down capability.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A dashboard on a massive dataset is slow, even though most visuals only ever show summarized totals, not row-level detail. What feature could improve this without losing the ability to drill into detail when needed?",
      options: ["Deleting the detailed data entirely", "Aggregations, which let summary queries use a pre-summarized table automatically, falling back to detail when genuinely needed", "Switching every table to DirectQuery", "Removing all visuals from the dashboard"],
      correct: 1,
      explanation: "Aggregations let Power BI transparently serve summary-level queries from a smaller, pre-summarized table for speed, while still preserving the underlying full-detail table for the cases where genuine row-level drill-down is actually needed.",
    },
    keyTakeaway: "Aggregations improve performance on massive datasets for typical summary-level queries, while still preserving full-detail drill-down capability when genuinely needed.",
    commonMistake: "Assuming the only way to speed up a massive dataset's dashboard is to permanently discard detailed data, when aggregations preserve both speed and drill-down capability together.",
  },
  {
    title: "Dataflows & shared datasets",
    body: "A dataflow captures a reusable set of Power Query transformations in the Power BI Service itself (rather than inside one specific report's file), letting multiple reports and datasets reference the same cleaned, transformed data without duplicating that transformation logic separately in each one. A shared dataset similarly lets multiple reports build on top of one centrally maintained, governed data model, rather than each report author independently rebuilding their own version of essentially the same underlying model - both concepts extend the 'build once, reuse consistently' principle to an entire organization's reporting.",
    altExplain: "A dataflow captures reusable Power Query transformations centrally in the Power BI Service, so multiple reports share the same cleaned data without duplicating logic. A shared dataset does the same for an entire data model, not just cleaning steps.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Five different Power BI reports each independently rebuild the same data cleaning and transformation logic against the same source data. What Power BI feature would eliminate this duplication?",
      options: ["Row-level security", "A shared dataflow, letting all five reports reference the same centrally-maintained transformation logic", "Paginated reports", "Custom visuals"],
      correct: 1,
      explanation: "A dataflow centralizes a reusable set of transformation steps in the Power BI Service itself, letting multiple reports reference the same cleaned, transformed data without each one independently duplicating the same logic.",
    },
    keyTakeaway: "Dataflows and shared datasets extend the 'build once, reuse consistently' principle across an entire organization's reports, avoiding duplicated transformation logic or inconsistent data models.",
    commonMistake: "Letting each report independently rebuild the same data transformations, when a shared dataflow would centralize that logic once for consistent reuse across every report that needs it.",
  },
  {
    title: "Power BI & Azure integration",
    body: "Power BI integrates closely with several Azure cloud services covered in the Cloud Computing course: Azure Synapse Analytics for large-scale data warehousing that Power BI can query directly, Azure Data Factory for building the broader data pipelines that feed into Power BI's models, and Azure Active Directory (now Microsoft Entra ID) for managing user identity and access consistently across an organization's entire Microsoft ecosystem, Power BI included. Understanding this integration matters especially for organizations already invested in Azure, where Power BI functions as one connected piece of a much larger cloud data platform, not an isolated standalone tool.",
    altExplain: "Power BI integrates with Azure services from the Cloud Computing course: Synapse Analytics for large-scale warehousing, Data Factory for broader data pipelines, and Azure AD for consistent identity management - Power BI as one piece of a larger cloud platform, not standalone.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "An organization already uses Azure Data Factory to build data pipelines feeding a data warehouse. How does Power BI typically fit into this existing setup?",
      options: ["Power BI can't integrate with Azure services at all", "Power BI connects to and reports on the data these Azure services have already prepared, as one integrated piece of the platform", "Power BI would need to replace Azure Data Factory entirely", "This integration only works with on-premises data"],
      correct: 1,
      explanation: "Power BI typically connects to and visualizes data that Azure services like Data Factory and Synapse have already prepared - functioning as an integrated reporting layer on top of an organization's broader Azure data platform, not a replacement for those services.",
    },
    keyTakeaway: "In an Azure-invested organization, Power BI functions as one integrated piece of a larger cloud data platform - connecting to and visualizing data that other Azure services have already prepared.",
    commonMistake: "Treating Power BI as a completely isolated, standalone tool in an organization that has significant existing investment in a broader Azure data platform it could integrate with.",
  },
  {
    title: "Deployment pipelines",
    body: "A deployment pipeline manages a report's lifecycle across Development, Test, and Production stages, letting changes be built and validated in Development, verified in Test, and only then promoted to Production where the actual business audience relies on stable, tested reports - directly mirroring the same staged deployment discipline from the CI/CD lessons in the Cloud Computing course, applied specifically to Power BI content instead of application code. This prevents untested changes from reaching production reports directly, protecting the business audience from the disruption of an unvetted change breaking something they depend on daily.",
    altExplain: "A deployment pipeline manages Dev/Test/Production stages for reports, so changes are validated before reaching production - the same staged deployment discipline as CI/CD from the Cloud Computing course, applied to Power BI content.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A team currently edits Power BI reports directly in the same workspace the business audience actively relies on for daily decisions, occasionally breaking something with an untested change. What would a deployment pipeline improve here?",
      options: ["Nothing, direct editing in production is always fine", "Changes would be built and tested in separate Dev/Test stages before being promoted to Production, protecting the live reports", "It would only affect DAX formula syntax", "Deployment pipelines are unrelated to report reliability"],
      correct: 1,
      explanation: "A deployment pipeline separates Development and Test stages from Production, ensuring changes are validated before ever reaching the live reports the business audience actually relies on - directly addressing the risk of untested changes breaking production.",
    },
    keyTakeaway: "A deployment pipeline protects production reports from untested changes, mirroring the same staged deployment discipline covered in the Cloud Computing course's CI/CD lessons.",
    commonMistake: "Editing and testing changes directly within the same workspace the business audience actively relies on, risking disruption from an untested change reaching production directly.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "VertiPaq Analyzer / Performance Analyzer", desc: "Tools for diagnosing exactly what's slow in a model or report - which visual, which measure.", example: "Identify the single visual causing most of a page's load time." },
      { syntax: "Pre-aggregated summary tables", desc: "Stores rolled-up totals so huge datasets don't need to be scanned row-by-row for common queries.", example: "A daily-summary table instead of scanning millions of transaction rows." },
      { syntax: "Dataflows (reusable, shared ETL)", desc: "Centralizes data prep once, reused across multiple reports/datasets instead of duplicated per report.", example: "One cleaned customer table, used by five different reports." },
      { syntax: "Power BI + Azure (Synapse, Data Factory, etc.)", desc: "Integrates Power BI with broader Azure data platform services for larger-scale pipelines.", example: "A Data Factory pipeline feeds a Power BI dataset automatically." },
      { syntax: "Dev \u2192 Test \u2192 Production (deployment pipeline)", desc: "Validates changes in earlier stages before they ever reach the live reports.", example: "Protects production from an untested change breaking things." },
    ],
  },
];

const PBI_MASTER_ADVANCED = [
  {
    title: "Power BI governance & administration",
    body: "At an organizational scale, Power BI governance covers who can create workspaces, what data sources are approved for connection, how sensitive data is labeled and protected, and how usage is monitored across the entire tenant - typically managed by Power BI administrators through the admin portal, rather than left to each individual report author's own discretion. Effective governance balances genuine self-service flexibility (letting business users build their own reports without needing IT for every request) against necessary organizational control (ensuring sensitive data is protected and quality standards are maintained) - too much control stifles the self-service value Power BI is meant to provide, while too little risks data sprawl and inconsistency.",
    altExplain: "Power BI governance covers workspace creation rules, approved data sources, data sensitivity labeling, and usage monitoring across an organization - balancing self-service flexibility against necessary organizational control.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "An organization locks down Power BI so tightly that business users must wait weeks for IT approval to build even a simple new report. What governance principle is likely being violated?",
      options: ["Governance should always be this strict", "Balancing self-service flexibility against control - overly strict governance stifles the self-service value Power BI is meant to provide", "This has nothing to do with governance", "Governance only applies to data sources, not report creation"],
      correct: 1,
      explanation: "Effective governance balances necessary control against genuine self-service flexibility. Locking things down so tightly that simple reports require weeks of approval undermines the self-service value Power BI is specifically meant to enable for business users.",
    },
    keyTakeaway: "Effective Power BI governance balances organizational control against genuine self-service flexibility - erring too far in either direction undermines the platform's real value.",
    commonMistake: "Implementing governance so restrictive that it eliminates the self-service flexibility that makes Power BI valuable to business users in the first place.",
  },
  {
    title: "Embedding Power BI in applications",
    body: "Power BI Embedded lets developers integrate Power BI reports directly into a custom application, rather than requiring users to visit the separate Power BI Service - a SaaS product might embed a usage dashboard directly within its own interface, so customers see their analytics without ever leaving that application or needing their own separate Power BI license. This requires the embedding application's developers to handle authentication and licensing correctly (often through a service principal or 'embed for your customers' licensing approach), which is a meaningfully more involved technical setup than simply sharing a standard Power BI report link.",
    altExplain: "Power BI Embedded lets developers integrate reports directly into a custom application, so users see analytics without leaving that app or needing their own Power BI license - a more involved technical setup than sharing a standard report link.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A SaaS company wants their customers to see usage analytics directly within the company's own web application, without customers needing their own Power BI accounts. What Power BI capability supports this?",
      options: ["Standard report sharing links", "Power BI Embedded, integrating reports directly into the custom application", "Row-level security alone", "Paginated reports"],
      correct: 1,
      explanation: "Power BI Embedded is specifically designed for this scenario - integrating Power BI reports directly into a third-party application's own interface, so end users see analytics without needing their own separate Power BI license or account.",
    },
    keyTakeaway: "Power BI Embedded serves a genuinely different need than standard report sharing - integrating analytics directly into a custom application for users who shouldn't need their own Power BI accounts.",
    commonMistake: "Assuming standard Power BI report sharing links can achieve the same seamless, license-free integration that Power BI Embedded is specifically designed to provide.",
  },
  {
    title: "AI features in Power BI",
    body: "Power BI includes several AI-powered features that reduce the need for manual analysis in specific situations. The Q&A visual lets a viewer type a plain-language question ('total sales by region last quarter') and get an automatically generated visual answer, without needing to build the visual manually. Key Influencers analyzes what factors most strongly correlate with a metric of interest - directly echoing the correlation concepts from the Data Analytics course, automated and surfaced directly within a report. These features genuinely lower the barrier for less technical users to explore data themselves, though they work best as a starting point for exploration, not a replacement for a properly modeled, deliberately designed report.",
    altExplain: "Power BI's Q&A visual answers plain-language questions automatically with a generated visual. Key Influencers automatically surfaces what factors most correlate with a metric - the same correlation concept from Data Analytics, automated within a report.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A business user without Power BI modeling skills wants to quickly explore what factors are most associated with customer churn in a dataset. What AI feature is designed for this?",
      options: ["A standard bar chart", "Key Influencers, which automatically analyzes correlating factors", "Row-level security", "A paginated report"],
      correct: 1,
      explanation: "Key Influencers is specifically designed to automatically analyze and surface which factors most strongly correlate with a chosen metric, directly supporting this kind of quick, self-service exploratory analysis without requiring manual statistical modeling.",
    },
    keyTakeaway: "Power BI's AI features (Q&A, Key Influencers) lower the barrier for self-service exploration, but work best as a starting point rather than a full replacement for a deliberately designed, properly modeled report.",
    commonMistake: "Relying entirely on AI-generated features for a critical, polished business report, rather than treating them as a helpful exploratory starting point alongside deliberate report design.",
  },
  {
    title: "Power BI vs. other BI tools",
    body: "Tableau, another major BI platform, is often noted for particularly strong, highly flexible visual customization, while Power BI's tight integration with Excel, Azure, and the broader Microsoft ecosystem makes it a natural fit for organizations already invested there - and its licensing is generally more accessible for smaller teams. Looker (now part of Google Cloud) emphasizes a centralized, code-based data modeling layer that enforces consistent business logic across an entire organization's reporting. No single tool is universally 'best' - the right choice depends on an organization's existing technology investments, team skill sets, and specific reporting needs, echoing the same 'right tool for the actual situation' principle covered throughout the Cloud Computing course.",
    altExplain: "Tableau offers particularly flexible visual customization. Power BI integrates tightly with Microsoft's ecosystem and Excel. Looker emphasizes centralized, code-based modeling. No tool is universally best - the right choice depends on existing investments and specific needs.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "An organization heavily invested in Microsoft 365, Excel, and Azure is choosing a BI tool. What's a reasonable factor favoring Power BI specifically?",
      options: ["Power BI is universally the best tool for every organization", "Its tight integration with the existing Microsoft ecosystem the organization already relies on", "Tableau and Looker cannot be used by any organization", "BI tool choice never depends on existing technology investments"],
      correct: 1,
      explanation: "Power BI's tight integration with Excel, Azure, and the broader Microsoft ecosystem is a genuine, practical advantage for an organization already invested there - not because Power BI is universally 'the best' tool, but because it fits this organization's specific existing context particularly well.",
    },
    keyTakeaway: "No BI tool is universally best - the right choice depends on an organization's existing technology investments, team skills, and specific reporting needs, not a fixed ranking of tools.",
    commonMistake: "Choosing a BI tool based on generic reputation alone, without considering how well it actually fits the organization's specific existing technology investments and team skill sets.",
  },
  {
    title: "Capstone: designing an enterprise BI solution",
    body: "Every concept across this course - data modeling and star schemas, DAX measures and time intelligence, dashboard design, workspaces and apps, governance, and performance optimization - comes together when designing a genuine enterprise BI solution. A well-designed solution starts from the actual business questions stakeholders need answered, builds a properly structured data model to support those questions efficiently, applies consistent governance and security across workspaces and apps, and optimizes performance deliberately rather than as an afterthought once problems already surface. This is the real practice of BI architecture: not using every advanced feature everywhere, but building a solution that actually, reliably serves the specific organization and audience in front of you.",
    altExplain: "A complete enterprise BI solution combines everything from this course - modeling, DAX, dashboards, governance, and performance - starting from real business questions, not from showcasing every available advanced feature.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A team designs an elaborate Power BI solution with every advanced feature covered in this course, but stakeholders still can't get clear answers to their actual business questions. What's the likely core issue?",
      options: ["Not enough advanced features were used", "The solution wasn't actually designed starting from the stakeholders' real business questions", "DAX formulas are always too complex to be useful", "Power BI is fundamentally unsuited to enterprise use"],
      correct: 1,
      explanation: "A BI solution's value comes from actually answering real business questions clearly - using every advanced feature available doesn't substitute for starting the design process from stakeholders' genuine, specific information needs.",
    },
    keyTakeaway: "The strongest BI solutions start from real business questions and build backward to the model, visuals, and governance needed to answer them - not from showcasing every available advanced feature.",
    commonMistake: "Designing a technically sophisticated BI solution that uses many advanced features impressively, without first grounding the design in stakeholders' actual, specific business questions.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the concepts introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of what you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "Governance: workspace roles + certification + endorsement", desc: "Controls who can publish/edit, and signals which reports are trustworthy at scale.", example: "A 'certified' badge on an official, vetted report." },
      { syntax: "Power BI Embedded (embed reports in apps)", desc: "Embeds interactive reports directly into a custom application, not just the Power BI Service.", example: "A SaaS product's dashboard is actually an embedded Power BI report." },
      { syntax: "Q&A (natural language) / Key Influencers / Decomposition Tree", desc: "AI-assisted features for exploring data without writing DAX or building visuals manually.", example: "Type \"total sales by region\" and get an instant visual." },
      { syntax: "Power BI vs. Tableau vs. Looker (tool tradeoffs)", desc: "Different BI tools suit different needs - ecosystem fit, pricing model, and team skills all matter.", example: "Power BI's tight Excel/Azure integration vs. Tableau's visual flexibility." },
      { syntax: "Start from business questions, build backward", desc: "The strongest BI solutions design from real stakeholder needs, not from showcasing features.", example: "Model, visuals, and governance follow from the actual questions being answered." },
    ],
  },
];

const PBI_MASTER_QUIZ_BANK = {
  basic: [
    { q: "SUMX differs from plain SUM by...", options: ["Only working with text columns", "Evaluating a custom expression per row before aggregating", "Being exactly the same function", "Never being usable in a measure"], correct: 1 },
    { q: "Context transition refers to...", options: ["Changing a report's color theme", "CALCULATE converting row context into an equivalent filter context", "Publishing a report to a new workspace", "A type of relationship"], correct: 1 },
    { q: "ALL(Table) inside CALCULATE is commonly used for...", options: ["Deleting a table", "Removing filters to calculate an unfiltered grand total, useful for percent-of-total measures", "Creating a new relationship", "Formatting numbers"], correct: 1 },
    { q: "A composite model allows...", options: ["Only one connection mode across the entire model", "Mixing Import and DirectQuery modes across different tables in one model", "Automatic row-level security", "Combining multiple visual types only"], correct: 1 },
    { q: "DirectQuery's main tradeoff compared to Import mode is...", options: ["It's always faster with no downside", "Live data freshness at the cost of more performance dependency on the source system", "It removes the need for a data model", "It only works with Excel files"], correct: 1 },
  ],
  intermediate: [
    { q: "DAX Studio helps diagnose whether slowness comes from...", options: ["Report color schemes", "The DAX formula engine versus the storage engine", "Row-level security settings", "Custom visual licensing"], correct: 1 },
    { q: "Power BI's aggregations feature primarily helps by...", options: ["Deleting detailed data permanently", "Serving summary queries from a smaller pre-summarized table, falling back to detail when needed", "Disabling all visuals", "Replacing the need for a data model"], correct: 1 },
    { q: "A dataflow centralizes...", options: ["Report color themes", "Reusable Power Query transformation logic shared across multiple reports", "Row-level security roles only", "Custom visual code"], correct: 1 },
    { q: "In an Azure-invested organization, Power BI typically functions as...", options: ["A completely isolated, standalone tool", "One integrated piece of a broader Azure cloud data platform", "A replacement for Azure Data Factory", "Unrelated to any Azure services"], correct: 1 },
    { q: "A deployment pipeline (Dev/Test/Production) mainly protects against...", options: ["Slow DAX formulas", "Untested changes reaching and disrupting production reports directly", "Poor visual color choices", "Row-level security misconfiguration only"], correct: 1 },
  ],
  advanced: [
    { q: "Effective Power BI governance balances...", options: ["Maximum restriction with no exceptions", "Organizational control against genuine self-service flexibility", "Cost against report color schemes", "Only technical concerns, never business ones"], correct: 1 },
    { q: "Power BI Embedded is designed for...", options: ["Standard report sharing via a link", "Integrating reports directly into a custom third-party application", "Row-level security configuration only", "Paginated report printing"], correct: 1 },
    { q: "Key Influencers automatically...", options: ["Formats numbers as currency", "Analyzes and surfaces factors most correlated with a chosen metric", "Creates new relationships", "Publishes a report"], correct: 1 },
    { q: "Choosing between Power BI, Tableau, and Looker should mainly depend on...", options: ["Which tool is universally ranked best", "An organization's existing technology investments, team skills, and specific needs", "Which tool has the most visual themes", "Random preference"], correct: 1 },
    { q: "A strong enterprise BI solution design process starts from...", options: ["Using the maximum number of advanced features available", "The actual business questions stakeholders need answered", "The most complex DAX formulas possible", "Whichever visuals look most impressive"], correct: 1 },
  ],
};

// Wire this course into the shared registry (COURSE_CONTENT is declared in data.js,
// loaded before this file - we're adding to it, not replacing it).
COURSE_CONTENT.powerbi = {
  entry: {
    basic: PBI_ENTRY_BASIC,
    intermediate: PBI_ENTRY_INTERMEDIATE,
    advanced: PBI_ENTRY_ADVANCED,
    quiz: PBI_QUIZ_BANK,
  },
  professional: {
    basic: PBI_PRO_BASIC,
    intermediate: PBI_PRO_INTERMEDIATE,
    advanced: PBI_PRO_ADVANCED,
    quiz: PBI_PRO_QUIZ_BANK,
  },
  master: {
    basic: PBI_MASTER_BASIC,
    intermediate: PBI_MASTER_INTERMEDIATE,
    advanced: PBI_MASTER_ADVANCED,
    quiz: PBI_MASTER_QUIZ_BANK,
  },
};
