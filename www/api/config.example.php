<?php
/**
 * Copy this file to config.php and fill in your real values.
 * config.php should NEVER be committed to git or shared publicly —
 * add it to .gitignore.
 *
 * Get an API key from: https://console.anthropic.com/settings/keys
 */

define('ANTHROPIC_API_KEY', 'sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx');

/**
 * Database credentials for accounts (registration/login).
 * In hPanel: Databases -> MySQL Databases -> create a database and a
 * database user, then fill in the exact names it gives you below.
 * DB_HOST is almost always "localhost" on Hostinger shared hosting.
 */
define('DB_HOST', 'localhost');
define('DB_NAME', 'your_db_name');
define('DB_USER', 'your_db_user');
define('DB_PASS', 'your_db_password');
