// =========================================================================
// Python - Master Level
// =========================================================================
const PY_MASTER_BASIC = [
  {
    title: "Mutable vs immutable objects",
    body: "Every Python object is either mutable (can be changed after creation, like lists and dictionaries) or immutable (cannot, like numbers, strings, and tuples). This distinction explains a classic beginner surprise: passing a list into a function and modifying it inside the function actually changes the original list too, since the function receives a reference to the same object - but passing an integer and 'modifying' it inside a function never affects the original, since integers are immutable and any change creates a brand new integer object instead. Understanding this is essential once you start writing functions that take collections as arguments.",
    altExplain: "Mutable objects (lists, dicts) can change in place - modifying one inside a function affects the original too. Immutable objects (numbers, strings, tuples) can't - a 'change' actually creates a new object.",
    visual: null,
    pycode: "def add_item(lst):\n    lst.append(\"new\")\n\nmy_list = [\"a\", \"b\"]\nadd_item(my_list)\nprint(my_list)  # includes \"new\" - lists are mutable\n\ndef increment(n):\n    n = n + 1\n\nnum = 5\nincrement(num)\nprint(num)  # still 5 - ints are immutable",
    keyTakeaway: "Passing a mutable object (list, dict) into a function lets that function modify the original - passing an immutable one (int, string, tuple) never does.",
    commonMistake: "Assuming all function arguments behave the same way in Python - whether a function can modify the original value depends entirely on whether that type is mutable.",
  },
  {
    title: "Design patterns: Singleton",
    body: "Python implements the Singleton pattern - ensuring only one instance of a class ever exists - a bit differently than C#, often using the __new__ method, which controls object creation itself, rather than a private constructor. Overriding __new__ to check whether an instance already exists, and returning that existing instance instead of creating a new one, achieves the same 'exactly one instance, one global access point' guarantee. As in C#, Singleton is best reserved for things that are genuinely unique application-wide, like a shared configuration object, rather than used as a default pattern for convenience.",
    altExplain: "Singleton guarantees only one instance of a class ever exists. In Python, this is often done by overriding __new__ to return an existing instance instead of creating a new one.",
    visual: null,
    pycode: "class ConfigManager:\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n            cls._instance.setting = \"default\"\n        return cls._instance\n\nconfig1 = ConfigManager()\nconfig1.setting = \"production\"\nconfig2 = ConfigManager()\nprint(config2.setting)  # \"production\" - same instance",
    refBox: {
      syntax: "class C: / def __new__(cls): / if cls._instance is None: ...",
      desc: "__new__ controls object creation itself, before __init__ runs - the hook used to enforce a Singleton.",
      example: "class Config:\n    _instance = None\n    def __new__(cls):\n        ...",
    },
    keyTakeaway: "__new__ controls object creation itself, before __init__ even runs - that's what makes it the right hook for enforcing a Singleton in Python.",
    commonMistake: "Overusing Singleton for convenience rather than genuine application-wide uniqueness, introducing hidden global state that makes testing harder.",
  },
  {
    title: "Design patterns: Factory",
    body: "A Factory function in Python centralizes object-creation logic, exactly like C#'s Factory pattern, but often expressed as a simple function rather than requiring a dedicated class: def create_shape(shape_type): returning different objects based on the input keeps that decision logic in one place, so the rest of the program just asks for what it needs conceptually. Python's flexibility means a factory can be as simple as a function with a dictionary mapping names to classes, avoiding even the if/elif chain a more rigid language might require.",
    altExplain: "A Factory function centralizes object-creation decisions in one place - in Python this is often just a regular function, sometimes using a dictionary to map names to classes.",
    visual: null,
    pycode: "class Circle:\n    def draw(self):\n        print(\"Drawing a circle\")\n\nclass Square:\n    def draw(self):\n        print(\"Drawing a square\")\n\ndef create_shape(shape_type):\n    shapes = {\"circle\": Circle, \"square\": Square}\n    return shapes[shape_type]()\n\nshape = create_shape(\"circle\")\nshape.draw()",
    refBox: {
      syntax: "def create_x(type): / options = {\"a\": ClassA}; return options[type]()",
      desc: "A factory function centralizes object-creation decisions - often a dict lookup from name to class.",
      example: "shapes = {\"circle\": Circle}\nshapes[\"circle\"]()",
    },
    keyTakeaway: "A factory using a dictionary lookup (name to class) is a common, clean Python alternative to a long if/elif chain for choosing which type to construct.",
    commonMistake: "Scattering 'if type == X, create Y' logic across a program instead of centralizing it in one factory function.",
  },
  {
    title: "Pythonic principles: The Zen of Python",
    body: "Typing import this in a Python interpreter reveals 'The Zen of Python,' a short set of guiding principles for writing good Python code - 'Readability counts,' 'Simple is better than complex,' 'There should be one obvious way to do it.' These aren't enforced by the language, but they shape what experienced Python developers mean by 'Pythonic' code: code that favors clarity and simplicity over cleverness, uses the language's own idioms (like list comprehensions and context managers) rather than fighting against them, and generally optimizes for the next person reading the code, not just for the computer executing it.",
    altExplain: "'The Zen of Python' is a short set of guiding principles favoring readability and simplicity. 'Pythonic' code embraces the language's own idioms rather than working against them.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Two solutions solve the same problem: one is a clever one-liner that's hard to read, the other is a slightly longer but very clear version. Which does 'Pythonic' generally favor?",
      options: ["Always the shortest possible code, regardless of readability", "The clearer version - readability is a core Python value", "Whichever runs a few milliseconds faster", "There's no preference either way"],
      correct: 1,
      explanation: "The Zen of Python explicitly states 'Readability counts.' Pythonic code generally favors clarity over cleverness, even if a more clever version is technically shorter.",
    },
    keyTakeaway: "'Pythonic' code favors clarity and simplicity, using the language's own idioms - it's a style philosophy, not just a syntax rule.",
    commonMistake: "Writing deliberately clever, compressed one-liners to show off, when clearer code would serve the next reader (including your future self) much better.",
  },
  {
    title: "Big O notation in Python",
    body: "Big O notation applies to Python exactly as it does to any language, but it's worth knowing how common Python operations actually perform: checking if a value is in a list is O(n) - it may need to check every element - while checking membership in a set or dictionary key is O(1) on average, since they use a hash-based lookup internally. This is precisely why converting a list to a set before doing many 'is this in here' checks can meaningfully speed up code, especially as the collection grows large - the underlying data structure's performance characteristics matter just as much as the algorithm built on top of it.",
    altExplain: "Checking 'is this in a list' is O(n) - slow on large lists. Checking a set or dictionary key is O(1) on average, much faster - because of how they're stored internally.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "A program repeatedly checks 'is this value in my_collection' inside a loop, on a collection with 100,000 items. Which is the better structure for my_collection?",
      options: ["A list, since it's simplest", "A set, since membership checks are much faster (O(1) vs O(n))", "It makes no difference at all", "A tuple, since it's immutable"], correct: 1,
      explanation: "Checking membership in a list requires scanning through elements (O(n)), while a set uses hashing for average O(1) lookups - a massive difference once the collection is large and checked repeatedly.",
    },
    keyTakeaway: "For repeated membership checks, a set (or dictionary keys) is far faster than a list - this is one of the most impactful, easy Python performance wins.",
    commonMistake: "Using a list for a large collection that's repeatedly checked with 'in', when converting it to a set would make each check dramatically faster.",
  },
  {
    title: "Cheatsheet: Basic track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "tuple / str are immutable; list / dict are mutable", desc: "Immutable objects can't be changed after creation; mutable ones can.", example: "point = (3, 4)  # can't change point[0]" },
      { syntax: "class C: / def __new__(cls): / if cls._instance is None: ...", desc: "The Singleton pattern - ensures only one instance of a class ever exists.", example: "config1 = ConfigManager()\nconfig2 = ConfigManager()  # same object" },
      { syntax: "def create_x(type): / options = {\"a\": ClassA}; return options[type]()", desc: "A factory function - centralizes object-creation decisions, often via dict lookup.", example: "shapes = {\"circle\": Circle}\nshapes[\"circle\"]()" },
      { syntax: "import this", desc: "Prints the Zen of Python - guiding principles for idiomatic, readable code.", example: "\"Simple is better than complex.\"" },
      { syntax: "x in list_ (O(n)) vs x in set_/dict_ (O(1))", desc: "Membership checks are far faster on a set/dict than a list.", example: "if x in my_set: ..." },
    ],
  },
];

const PY_MASTER_INTERMEDIATE = [
  {
    title: "Recursion in Python",
    body: "Recursion in Python works the same conceptually as in C#: a function calls itself with a smaller version of the problem, until a base case stops it. Python does impose a default recursion limit (typically 1000 calls deep) to prevent runaway recursion from crashing the interpreter with a cryptic error, which is worth knowing about if you ever hit a RecursionError on a problem that genuinely needs deep recursion - in those rare cases, converting to an iterative (loop-based) solution is usually the better fix rather than raising the limit.",
    altExplain: "Recursion works the same as in C# - a function calling itself until a base case stops it. Python has a default recursion depth limit, so extremely deep recursion needs a loop-based rewrite instead.",
    visual: null,
    pycode: "def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))",
    refBox: {
      syntax: "def f(n): / if base_case: return x / return f(n - 1)",
      desc: "Recursion - a function calling itself, with a base case that stops it.",
      example: "def factorial(n):\n    if n == 0: return 1\n    return n * factorial(n-1)",
    },
    keyTakeaway: "Python's default recursion limit (around 1000 calls deep) means very deep recursion needs an iterative rewrite rather than just 'more recursion.'",
    commonMistake: "Writing deeply recursive solutions for problems better suited to a simple loop, risking a RecursionError on large inputs.",
    challenge: {
      prompt: "Write a recursive function that sums numbers from 1 to n.",
      hint: "Base case: n == 0 returns 0. Recursive case: n + sum_to(n - 1).",
      solution: "def sum_to(n):\n    if n == 0:\n        return 0\n    return n + sum_to(n - 1)\n\nprint(sum_to(5))",
    },
  },
  {
    title: "Sorting & the sorted() function",
    body: "Python's built-in sorted() function uses Timsort, a highly optimized, real-world sorting algorithm with O(n log n) time complexity - dramatically faster than a simple bubble sort on large data. sorted(my_list) returns a new sorted list without modifying the original, while my_list.sort() sorts a list in place. Both accept a key parameter (often a lambda) controlling what value each item is compared by, and reverse=True flips the order, covering the vast majority of real sorting needs without ever needing to hand-write a sorting algorithm yourself.",
    altExplain: "sorted() uses a fast, well-optimized algorithm (O(n log n)) built into Python. sorted(list) returns a new sorted list; list.sort() sorts in place. Both accept key= and reverse= for control.",
    visual: null,
    pycode: "numbers = [5, 2, 8, 1, 9]\nprint(sorted(numbers))\nprint(sorted(numbers, reverse=True))\n\npeople = [{\"name\": \"Ana\", \"age\": 28}, {\"name\": \"Marco\", \"age\": 22}]\nprint(sorted(people, key=lambda p: p[\"age\"]))",
    keyTakeaway: "Python's built-in sorted() is already highly optimized (O(n log n)) - there's essentially never a good reason to hand-write a sorting algorithm for real use.",
    commonMistake: "Implementing a custom sort (like bubble sort) for production code when the built-in sorted() is faster, well-tested, and already available.",
  },
  {
    title: "Searching efficiently",
    body: "Linear search through a list is O(n), but Python's bisect module provides efficient binary search - O(log n) - on data that's already sorted, exactly like the binary search concept covered in the C# course, just using a ready-made standard library tool instead of hand-writing the algorithm. bisect.bisect_left(sorted_list, target) finds the position where a value would belong in a sorted list, which can be used to check for existence or find an insertion point, without needing to implement the binary search logic yourself from scratch.",
    altExplain: "The bisect module provides fast binary search (O(log n)) on sorted data - the same concept as the C# binary search lesson, but ready-made in Python's standard library.",
    visual: null,
    pycode: "import bisect\n\nnumbers = [1, 3, 5, 7, 9, 11]\nposition = bisect.bisect_left(numbers, 7)\nprint(position)\nprint(numbers[position] == 7)",
    keyTakeaway: "bisect provides ready-made binary search functionality - no need to hand-implement it, as long as your data is already sorted.",
    commonMistake: "Using bisect (or any binary search) on data that isn't actually sorted - its logic assumes sorted order and gives unreliable results otherwise.",
  },
  {
    title: "Iterators & the iterator protocol",
    body: "A for loop in Python works by calling iter() on whatever you're looping over to get an iterator, then repeatedly calling next() on that iterator until it's exhausted - this is the iterator protocol, and it's what makes generators (covered earlier), lists, dictionaries, and many other types all loopable with the same for syntax. You can build your own iterable class by defining __iter__ (returning an iterator) and __next__ (returning the next value, or raising StopIteration when done), giving custom objects the same natural looping behavior as Python's built-in types.",
    altExplain: "A for loop works by calling iter() then next() repeatedly behind the scenes - this 'iterator protocol' is what makes lists, generators, and custom objects all loopable the same way.",
    visual: null,
    pycode: "class CountUp:\n    def __init__(self, limit):\n        self.limit = limit\n        self.current = 0\n\n    def __iter__(self):\n        return self\n\n    def __next__(self):\n        if self.current >= self.limit:\n            raise StopIteration\n        self.current += 1\n        return self.current\n\nfor number in CountUp(5):\n    print(number)",
    refBox: {
      syntax: "class C: / def __iter__(self): return self / def __next__(self): ...",
      desc: "Implements the iterator protocol - makes a custom class loopable with for. Raise StopIteration when done.",
      example: "def __next__(self):\n    if done: raise StopIteration\n    return value",
    },
    keyTakeaway: "Defining __iter__ and __next__ on a class makes it work naturally in a for loop, the same as any built-in Python sequence.",
    commonMistake: "Forgetting to raise StopIteration in a custom __next__ method, which causes an infinite loop instead of a normal, expected end to iteration.",
  },
  {
    title: "Testing concepts: mocking",
    body: "When testing a piece of code that depends on something slow, unreliable, or external - a network call, a database, the current time - a mock replaces that dependency with a fake, controllable stand-in for the duration of a test. Python's unittest.mock module provides tools for this: Mock() creates a fake object that can stand in for anything, recording how it was called and letting you specify what it should return, so a test can verify your code behaves correctly without actually needing the real, slow, or unpredictable dependency to be available.",
    altExplain: "Mocking replaces a slow or unreliable dependency (like a network call) with a fake, controllable stand-in during a test, so the test doesn't depend on that real thing being available.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Why would you mock a network API call in a unit test instead of letting the test make a real call?",
      options: ["Real calls are always faster", "Real calls make tests slow, unreliable, and dependent on external services being available", "Mocking is required by Python syntax", "There's no benefit to mocking"],
      correct: 1,
      explanation: "Real network calls in tests are slow, can fail due to network issues, and depend on an external service actually being up. Mocking lets you test your own code's logic reliably and quickly, independent of that external dependency.",
    },
    keyTakeaway: "Mocking lets you test your code's logic in isolation, without depending on slow, unreliable, or unavailable external systems during the test itself.",
    commonMistake: "Writing tests that make real network or database calls, making the test suite slow and prone to failing for reasons unrelated to the actual code being tested.",
  },
  {
    title: "Cheatsheet: Intermediate track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "def f(n): / if base_case: return x / return f(n - 1)", desc: "Recursion - a function calling itself, with a base case that stops it.", example: "def factorial(n):\n    if n == 0: return 1\n    return n * factorial(n-1)" },
      { syntax: "sorted(list, key=fn, reverse=True)", desc: "Returns a new sorted list (O(n log n)) - list.sort() does the same in place.", example: "sorted(people, key=lambda p: p[\"age\"])" },
      { syntax: "bisect.bisect_left(sorted_list, target)", desc: "Binary search (O(log n)) on already-sorted data.", example: "import bisect\nbisect.bisect_left(nums, 7)" },
      { syntax: "class C: / def __iter__(self): return self / def __next__(self): ...", desc: "Implements the iterator protocol - makes a class loopable with for.", example: "raise StopIteration  # when done" },
      { syntax: "from unittest.mock import Mock", desc: "Creates a fake stand-in object for a slow/unreliable dependency during testing.", example: "mock_api = Mock(return_value={\"ok\": True})" },
    ],
  },
];

const PY_MASTER_ADVANCED = [
  {
    title: "Dependency injection in Python",
    body: "Dependency Injection works the same conceptually in Python as in C#: a class or function receives what it depends on from the outside, rather than creating it internally, making testing and swapping implementations easier. Python's dynamic typing actually makes this even more lightweight than in C# - there's no need for a formal interface, since Python's duck typing means any object with the right methods can be passed in, and it'll work as long as it 'quacks like' what's expected, without needing to formally declare that it implements a particular contract.",
    altExplain: "Dependency Injection means a class receives what it needs from outside instead of creating it. Python's duck typing makes this even lighter than C# - no formal interface needed, just matching methods.",
    visual: null,
    pycode: "class EmailService:\n    def send(self, message):\n        print(f\"Emailing: {message}\")\n\nclass Notifier:\n    def __init__(self, service):\n        self.service = service\n\n    def notify(self, msg):\n        self.service.send(msg)\n\nnotifier = Notifier(EmailService())\nnotifier.notify(\"Hello!\")",
    keyTakeaway: "Python's duck typing means dependency injection doesn't require a formal interface - any object with the right method names works, checked only when actually called.",
    commonMistake: "Creating dependencies directly inside a class with a hardcoded instantiation, instead of receiving them from outside, making the class harder to test in isolation.",
  },
  {
    title: "Design patterns: Observer",
    body: "The Observer pattern in Python works exactly as it conceptually does in C#: a subject maintains a list of observer callbacks and calls each one whenever something changes, without needing to know any specifics about what each observer actually does in response. Python's first-class functions make this especially lightweight - a list of plain functions (or lambdas) can serve as observers directly, without needing a formal Observer interface or base class the way a more rigid language might require.",
    altExplain: "Observer lets a subject notify a list of listeners when something changes. In Python, the observers can just be plain functions in a list - no formal interface needed.",
    visual: null,
    pycode: "class StockTicker:\n    def __init__(self):\n        self.observers = []\n\n    def subscribe(self, callback):\n        self.observers.append(callback)\n\n    def set_price(self, price):\n        for callback in self.observers:\n            callback(price)\n\nticker = StockTicker()\nticker.subscribe(lambda price: print(f\"Price changed to {price}\"))\nticker.set_price(105.50)",
    refBox: {
      syntax: "self.observers.append(callback) / for cb in self.observers: cb(data)",
      desc: "The Observer pattern - a list of plain callbacks, no formal interface needed in Python.",
      example: "ticker.subscribe(lambda p: print(p))\nticker.set_price(105.5)",
    },
    keyTakeaway: "Python's first-class functions let the Observer pattern be implemented with a plain list of callbacks - no formal interface or base class required.",
    commonMistake: "Building an unnecessarily formal Observer interface hierarchy in Python when a simple list of callback functions would accomplish the same thing more simply.",
  },
  {
    title: "Memory management & reference counting",
    body: "Python manages memory primarily through reference counting: every object tracks how many references point to it, and when that count drops to zero - no variable, list, or other object references it anymore - it's immediately deallocated. This differs from C#'s garbage collector, which runs periodically rather than immediately, though Python also has a supplementary cycle-detecting garbage collector to handle reference cycles (two objects referencing each other, which would otherwise never reach a zero count on their own even when nothing else references either of them).",
    altExplain: "Python primarily frees memory via reference counting - an object is deleted the instant nothing references it anymore. A supplementary garbage collector handles reference cycles that counting alone can't catch.",
    visual: null,
    widget: "insight-check",
    insightCheck: {
      question: "Two objects reference each other (A points to B, B points to A), and nothing else references either of them. Why doesn't simple reference counting free them?",
      options: ["Reference counting doesn't exist in Python", "Each object still has a reference count of 1 (from the other), even though nothing external references either", "This situation is impossible in Python", "Python crashes immediately when this happens"],
      correct: 1,
      explanation: "Since A references B and B references A, each has a reference count of at least 1, even though the pair is unreachable from anywhere else in the program. This is exactly the case Python's supplementary cycle-detecting garbage collector exists to handle.",
    },
    keyTakeaway: "Reference counting alone can't clean up reference cycles - that's specifically why Python has a supplementary garbage collector on top of basic reference counting.",
    commonMistake: "Assuming Python's memory management is identical to C#'s garbage collection model, when reference counting (with cycle detection as a backup) is actually the primary mechanism.",
  },
  {
    title: "The Global Interpreter Lock (GIL)",
    body: "Python's standard implementation (CPython) has a Global Interpreter Lock, which allows only one thread to execute Python bytecode at a time, even on a multi-core machine. This means multithreading in Python doesn't speed up CPU-bound work (heavy calculation) the way it might in C#, though it still helps with I/O-bound work (waiting on a file, a network request), since a thread can release the GIL while waiting. For genuine CPU-bound parallelism, Python's multiprocessing module runs separate processes instead of threads, each with its own Python interpreter and GIL, sidestepping the limitation entirely at the cost of more memory overhead.",
    altExplain: "The GIL means only one thread runs Python code at a time, even on multiple cores - so threading doesn't speed up heavy calculations, only waiting-heavy tasks. multiprocessing uses separate processes to truly parallelize CPU work.",
    visual: null,
    refBox: {
      syntax: "from multiprocessing import Process",
      desc: "Runs separate processes (each with its own interpreter) to sidestep the GIL for true CPU parallelism.",
      example: "p = Process(target=heavy_calc)\np.start()",
    },
    widget: "insight-check",
    insightCheck: {
      question: "A Python program needs to do heavy CPU-bound calculations faster using multiple cores. What's the appropriate tool?",
      options: ["Regular threading, since it always speeds up any workload", "The multiprocessing module, since it sidesteps the GIL with separate processes", "There's no way to use multiple cores in Python", "Adding more variables to the calculation"], correct: 1,
      explanation: "Because of the GIL, regular threads don't provide true parallelism for CPU-bound work in Python. The multiprocessing module runs separate processes, each with its own interpreter, genuinely using multiple cores.",
    },
    keyTakeaway: "For CPU-bound parallelism in Python, reach for multiprocessing, not threading - the GIL prevents threads from truly running Python code simultaneously.",
    commonMistake: "Using threading for a CPU-heavy task and being confused when it doesn't actually run faster on a multi-core machine - the GIL is the reason why.",
  },
  {
    title: "Capstone: putting it together",
    body: "Every concept from this course - classes, dependency injection, decorators, context managers, and proper exception handling - comes together in any real Python application. A well-structured small program might define a class with clear responsibilities, accept its dependencies through its constructor rather than creating them internally, use a context manager for any resource that needs guaranteed cleanup, and wrap risky operations in specific exception handling rather than a broad catch-all. This is the actual practice of Python development: combining the language's own idioms - the ones that make Python feel 'Pythonic' - to fit the specific problem in front of you, rather than applying any single technique everywhere by default.",
    altExplain: "Real Python programs combine everything from this course - classes, dependency injection, context managers, proper error handling - chosen to fit the problem, using Python's own idioms rather than fighting against them.",
    visual: null,
    pycode: "class ConsoleNotifier:\n    def notify(self, message):\n        print(f\"[Notify] {message}\")\n\nclass TaskManager:\n    def __init__(self, notifier):\n        self.tasks = []\n        self.notifier = notifier\n\n    def add_task(self, task):\n        self.tasks.append(task)\n        self.notifier.notify(f\"Task added: {task}\")\n\nmanager = TaskManager(ConsoleNotifier())\nmanager.add_task(\"Finish the course\")",
    keyTakeaway: "Real Python engineering combines the right idioms - DI, context managers, specific error handling - to fit a specific problem, using the language's own natural style rather than importing patterns wholesale from another language.",
    commonMistake: "Writing Python code that reads like a direct translation from another language, instead of embracing Python's own idioms (comprehensions, context managers, duck typing) where they genuinely fit better.",
  },
  {
    title: "Cheatsheet: Advanced track",
    body: "A quick reference for the syntax introduced in this track - bookmark this lesson and come back to it any time you need a quick reminder, rather than re-reading full lessons.",
    altExplain: "This lesson is just a summary table of the syntax you've already learned in this track - nothing new to learn here.",
    visual: null,
    isCheatsheet: true,
    refTable: [
      { syntax: "def __init__(self, dependency): self.dependency = dependency", desc: "Dependency injection - receive what you depend on from outside, don't create it internally.", example: "Notifier(EmailService())" },
      { syntax: "self.observers.append(callback) / for cb in self.observers: cb(data)", desc: "The Observer pattern - a list of plain callbacks, no formal interface needed.", example: "ticker.subscribe(lambda p: print(p))" },
      { syntax: "reference count reaches 0 \u2192 object freed", desc: "Python's primary memory management - objects are freed the instant nothing references them.", example: "A cycle detector handles reference cycles counting alone can't catch." },
      { syntax: "from multiprocessing import Process", desc: "Runs separate processes to sidestep the GIL for true CPU parallelism.", example: "p = Process(target=heavy_calc)\np.start()" },
      { syntax: "class + DI + context manager + specific except", desc: "Real Python programs combine idioms to fit the problem, not one technique everywhere.", example: "See the capstone lesson for a full example." },
    ],
  },
];

const PY_MASTER_QUIZ_BANK = {
  basic: [
    { q: "Which of these is mutable in Python?", options: ["int", "string", "list", "tuple"], correct: 2 },
    { q: "Python's Singleton pattern is often implemented by overriding...", options: ["__init__ only", "__new__", "__str__", "__len__"], correct: 1 },
    { q: "A Python factory function commonly uses a dictionary to...", options: ["Store constant values only", "Map names to classes for cleaner construction logic", "Replace all classes entirely", "Slow down object creation intentionally"], correct: 1 },
    { q: "'Pythonic' code generally favors...", options: ["The shortest code regardless of clarity", "Readability and using the language's own idioms", "Avoiding all built-in functions", "Mimicking other languages exactly"], correct: 1 },
    { q: "Checking membership with 'in' is fastest on average for...", options: ["A list", "A set or dictionary", "A tuple", "All are equally fast"], correct: 1 },
  ],
  intermediate: [
    { q: "Python's default recursion limit exists to...", options: ["Make recursion faster", "Prevent runaway recursion from crashing with a cryptic error", "Disable recursion entirely", "Only allow one recursive call ever"], correct: 1 },
    { q: "Python's built-in sorted() function has what general time complexity?", options: ["O(n^2)", "O(n log n)", "O(1)", "O(n^3)"], correct: 1 },
    { q: "The bisect module provides...", options: ["Random shuffling", "Efficient binary search on sorted data", "String formatting", "File compression"], correct: 1 },
    { q: "A for loop works by calling which two things behind the scenes?", options: ["open() and close()", "iter() and next()", "start() and stop()", "map() and filter()"], correct: 1 },
    { q: "Mocking in testing is used to...", options: ["Make tests run against real, live services", "Replace a slow or unreliable dependency with a controllable fake", "Delete unnecessary tests", "Skip writing tests entirely"], correct: 1 },
  ],
  advanced: [
    { q: "Python's duck typing means dependency injection...", options: ["Is impossible", "Doesn't require a formal interface - matching methods are enough", "Only works with abstract classes", "Requires more boilerplate than C#"], correct: 1 },
    { q: "Python's Observer pattern can often be implemented with just...", options: ["A formal interface hierarchy", "A list of plain callback functions", "No code at all", "A database"], correct: 1 },
    { q: "Python primarily manages memory through...", options: ["Manual allocation only", "Reference counting, with a supplementary cycle-detecting collector", "No memory management at all", "The same exact system as C#"], correct: 1 },
    { q: "The GIL means Python threading is most useful for...", options: ["Heavy CPU-bound calculations", "I/O-bound waiting tasks, not CPU-heavy parallel work", "Nothing at all", "Only single-threaded programs"], correct: 1 },
    { q: "Real-world Python engineering typically involves...", options: ["Using exactly one pattern for every situation", "Combining Python's own idioms to fit each specific problem", "Avoiding all classes", "Writing code identical to other languages"], correct: 1 },
  ],
};

// Wire this course into the shared registry (COURSE_CONTENT is declared in data.js,
// loaded before this file - we're adding to it, not replacing it).
COURSE_CONTENT.python = {
  entry: {
    basic: PY_ENTRY_BASIC,
    intermediate: PY_ENTRY_INTERMEDIATE,
    advanced: PY_ENTRY_ADVANCED,
    quiz: PY_QUIZ_BANK,
  },
  professional: {
    basic: PY_PRO_BASIC,
    intermediate: PY_PRO_INTERMEDIATE,
    advanced: PY_PRO_ADVANCED,
    quiz: PY_PRO_QUIZ_BANK,
  },
  master: {
    basic: PY_MASTER_BASIC,
    intermediate: PY_MASTER_INTERMEDIATE,
    advanced: PY_MASTER_ADVANCED,
    quiz: PY_MASTER_QUIZ_BANK,
  },
};
